# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## מטרת הפרויקט

סקירה יומית של מאמרי פרשנות גאופוליטית מתוך PDF-ים של עיתונים מובילים באנגלית וגרמנית, ניתוח דגש ומגמות לאורך זמן, והפקת דוחות יומיים/שבועיים/חודשיים (PDF + HTML אינטראקטיבי), המתפרסמים באתר ב-GitHub Pages. כל דוח וכן האתר עצמו זמינים בשתי גרסאות שפה מלאות - עברית ואנגלית, עם מעבר שפה באתר. דף הבית (`docs/index.html`) הוא דף בית אינטראקטיבי - מפת עולם, ציר זמן, וכניסה לדוח האחרון, כולם קוראים מ-`docs/assets/data/manifest.json` בזמן ריצה (לא build-time) - הארכיון הכרונולוגי הישן זמין בנפרד ב-`archive.html`.

## מבנה הקבצים המרכזיים

- `src/common/db.py` - מודול ה-DB המשותף לכל שלבי ה-Pipeline (`data/processed/tracker.db`). הסכמה מוצהרת פעם אחת ב-`TABLE_COLUMNS`; `init_db()` יוצר טבלאות חסרות **וגם** מוסיף עמודות חסרות לטבלאות קיימות (`PRAGMA table_info` + `ALTER TABLE`). מכיל גם את כלל שיוך-מאמר-לדוח (`downloaded_files.report_date`, `REPORT_DATE_SQL`, `report_date_for_download()`). ראו "החלטות ארכיטקטוניות קבועות".
- `src/ingestion/fetch.py` - משיכת PDF-ים מערוץ הטלגרם `demagazinesharing` (telethon), מסנן רק קבצים שמזוהים כאחד מ-10 העיתונים הפעילים כרגע (ראו "כללי עבודה קבועים" למטה לרשימה המלאה ולהסבר על NYT המושעה) (`guess_newspaper()` - מנרמל `_`/`-` לרווח לפני ההשוואה, כדי שתבניות-שם שונות מאותו מקור לא יפספסו; מזהה גם צורת-כתיב "suddeutsche" ללא-ניקוד לצד "süddeutsche"/"sueddeutsche" הקיימים - וריאנט נדיר שנצפה פעם אחת ב-10.9.2026, ראו PROJECT_LOG 4.32, אך נשמר כתבנית קבועה כי שום דבר לא שולל הישנות; The Economist מזוהה **רק** דרך דפוס-השם של ה-Web Edition שלו - שאר המהדורות האזוריות שהערוץ מפיץ תחתיו התבררו ככפילות-תוכן או פגומות, לא מקור-מידע ייחודי, ראו PROJECT_LOG 4.26). **NYT מושעה לצמיתות בכל מהדורה** (לא רק הבינלאומית) - ניסיון מעבר למהדורה הביתית ב-2026-09-12 נבדק-תוכן-בפועל (pdfplumber, מדגם-עמודים) ונמצא **גם הוא פגום מקצה-לקצה** (תבנית-שחיתות tradingref.com, אותו דפוס בדיוק שכבר נצפה 6/6 פעמים בבינלאומית) - כנראה תכונה של האופן שהערוץ מקורות תוכן-NYT בכלל, לא ספציפית למהדורה. **The Washington Post נוסף במקומו** כמקור-MVP חדש (נבדק-תוכן ואומת תקין לפני הוספה) - ראו PROJECT_LOG 4.34 לפרטים המלאים. `compute_scan_limit()` מחשב את גודל-חלון-הסריקה (`iter_messages(limit=...)`) אוטומטית לפי `MAX(published_at)` הקיים מול הרגע הנוכחי - לא קבוע (200) עוד; מבוסס על "זמן מאז הרשומה **האחרונה**", לא על זיהוי-חור לא-רציף באמצע ההיסטוריה (ראו PROJECT_LOG action item 35 למגבלה הידועה). כל הורדה מדפיסה heartbeat לפני שהיא מתחילה (`downloading: {file_name}...`) ומטפלת ב-`FloodWaitError` במפורש עם הודעת-שינה גלויה - כדי שסטגנציה/תקיעה תמיד תשאיר סימן ברור בלוג של איפה בדיוק זה נעצר.
- `src/extraction/extract.py` - חילוץ טקסט גולמי מה-PDF (pdfplumber) לעמוד, גם ל-DB וגם לעותק טקסט תחת `data/processed/extracted/` (לא נכנס ל-git).
- `src/analysis/` - סיווג וניתוח באמצעות Claude API, בשני שלבים עצמאיים: `screen.py` (סינון רחב) ו-`analyze.py` (ניתוח מעמיק, כולל תיוג country_codes/conflict_zones - ראו "החלטות ארכיטקטוניות קבועות"). `geo_tag_backfill.py` - כלי-עזר קבוע (לא חד-פעמי) שמתייג בדיעבד מאמרים שנותחו לפני שהתיוג הגיאוגרפי נוסף, בקבוצות (batch) עם Haiku זול - לא קורא מחדש את כל שלב ה-Analysis.
- `src/common/geo_taxonomy.py` - שלושה מיפויים קבועים לתיוג גיאוגרפי: `COUNTRY_LIST` (רשימת מדינות סגורה, קוד ISO + שם עברי/אנגלי), `COUNTRY_TO_REGION` (מדינה → אחד מ-8 אזורים קבועים), `CONFLICT_ZONE_LABELS` (3 קונפליקטים סגורים). באותה רוח כמו `NEWSPAPER_DISPLAY_NAMES` - מיפוי קבוע מוזרק לפרומפט, לא ניחוש חופשי.
- `src/reporting/synthesize.py` - שלב א' של Reporting, בנוי כשני תתי-שלבים (ראו "החלטות ארכיטקטוניות קבועות" לפרטים המלאים ולנימוק): קריאת Sonnet **בודדת** לכל מאמרי היום שמקבצת אותם לנושאים בלבד (שדות קומפקטיים), ואז קריאת Sonnet **נפרדת לכל נושא**, מקבילית עם Prompt Caching, שכותבת את ההשוואה הדו-לשונית. כתיבה ל-`reports`/`report_sections`/`report_section_articles`, כולל רשת ביטחון דו-שכבתית (סף-יחס על שלב א' + retry-קצר-לכל-נושא בשלב ב') + fallback section דטרמיניסטי שמבטיח כיסוי מלא של כל מאמר בכל מקרה. `reports.sources_included` (`get_sources_for_date()` ב-`db.py`) מחושב **פעם אחת כאן** מ-JOIN דרך `articles` בפועל (לא מ-`downloaded_files` בלבד - מקור שירד אך לא תרם אף מאמר, כמו PDF סרוק-בלבד, לא נכלל) ונשמר כעמודה - `render.py`/`publish.py` קוראים אותה כמו-שהיא, לא מחשבים מחדש.
- `src/reporting/render.py` - שלב ב' של Reporting: הופך שורות `report_sections` ל-4 קבצים (HTML+PDF × עברית+אנגלית) תחת `reports/{he,en}/`, כולל קידוד צבעוני לפי `category` (`category_css()` - פומבי, משוכפל לשימוש גם בדף הבית ב-`publish.py`), גופן **Assistant** מוטמע מקומית (`src/reporting/assets/fonts/`, מועתק אוטומטית גם ל-`reports/assets/fonts/`; `font_face_css(relative_path)` - פונקציה פומבית משותפת שבונה את בלוק ה-`@font-face`, נקראת מ-5 מקומות: דוח/`about.html`/`archive.html`/דף הבית/`topic.html` - ראו "החלטות ארכיטקטוניות קבועות"), ועוגן `id="section-{id}"` על כל section (כולל fallback) - נדרש כדי שקישורי-עומק מהמניפסט יעבדו. `build_nav_html(back_href, other_lang_href, lang, pdf_href=None)` מקבל hrefs מפורשים (לא גוזר מ-`report_date`) - כדי שגם `about.html`/`topic.html` (ב-`publish.py`) יוכלו לעשות בו שימוש חוזר; `pdf_href` (אופציונלי, ברירת-מחדל `None`) מוסיף קישור-הורדת-PDF שלישי בנאב, רק כשמסופק - הקריאות הקיימות מ-`about.html`/`archive.html` לא שוברות. כל section כולל גם תיבת-ציטוט סגורה-כברירת-מחדל ("מראי מקום") עם עיתון+תאריך+עמוד לכל article שמרכיב אותו (`get_section_citations()` ב-`db.py`), כותרת-מקוצרת מתווספת רק כשעיתון תורם 2+ מאמרים לאותו section; תוכן-הציטוט מקבל `direction:ltr` מפורש (כמעט-כולו לטיני/מספרי) כדי לא להישבר ב-bidi בתוך עמוד RTL. כולל גם לוגו (56px) מקושר ל-`../index.html`, זהה בכל הצריכות (דוח/about/archive/topic). **`build_footer_html(lang, accessibility_href, terms_href)`** ו-**`shared_chrome_css()`** (פומביים, `publish.py` מייבא את שניהם) - פוטר משותף (קישורים ל-`accessibility.html`/`terms.html`) וסגנון `:focus-visible` גלובלי, זהים בכל 5 סוגי-העמוד - ראו "החלטות ארכיטקטוניות קבועות". **`favicon_links_html(asset_prefix)`** (פומבי, אותה מוסכמה כמו `font_face_css`) - תגי `<link rel="icon">` לכל 4 קובצי-הפאביקון (`favicon.ico`/`favicon-32x32.png`/`favicon-16x16.png`/`apple-touch-icon.png`, נגזרים מ-`MS_Logo.png` הקיים - לא לוגו חדש), מוזרק ל-`<head>` בכל 6 נקודות-הקריאה (דוח + 5 בוני-העמודים ב-`publish.py`). **נושאים ממוינים לפי רוחב-כיסוי** (`section_coverage()`, פומבי - משמש גם `publish.py`) - ראו "החלטות ארכיטקטוניות קבועות". כל section הוא **accordion** (`.section-header`/`.section-body`, ברירת-מחדל פתוח, לחיצה/מקלדת לקיפול) עם badge-רוחב-כיסוי ("N מקורות · M שפות") ליד תג-הקטגוריה, כפתור-שיתוף-קישור (`navigator.clipboard`) ליד ה"מראי מקום", ותפריט-קטגוריות דביק (sticky) מתחת לפס-הניווט עם כפתור "הרחב הכל / כווץ הכל" - הכפתור קורא מצב-DOM חי (לא דגל-עצמאי), כך שלא יכול להתבדות ממה שבאמת מוצג.
- `src/common/about_content.py` - תוכן משותף (עברית+אנגלית: doc_meta/identity/title/subtitle/sections) ל-`scripts/build_background_doc.py` ול-`build_about_html()` ב-`publish.py` - מקור אמת יחיד, לא כפילות. `scripts/` יכול לייבא מ-`common/`; `publish.py` (חלק מה-Pipeline) לעולם לא מייבא מ-`scripts/` (חד-פעמי). כולל סעיף **"גילוי נאות: שימוש בבינה מלאכותית"** (אחרי "שיטת העבודה") - חלק מחבילת-ציות רגולטורי (ראו PROJECT_LOG 4.38).
- `src/publishing/publish.py` - שלב Publishing: בונה `docs/` (מקור ה-GitHub Pages) מתוך `reports/` - מעתיק HTML/PDF וגופן Assistant, בונה `archive.html` (ארכיון כרונולוגי, לכל שפה + עותק-שורש, כולל לוגו מקושר ל-`index.html` ב-top-nav) ו-`about.html` (עמוד אתר רגיל, דו-לשוני, אותו לוגו-בנאב + לוגו נוסף גדול יותר - 96px - בכרטיס "אודות הכותב" בתחתית) ו-`manifest.json` סטטי (`assets/data/manifest.json`, גם ב-`docs/` וגם ב-`reports/`), ובונה **דף בית אינטראקטיבי** (`index.html`, לכל שפה + עותק-שורש) - מפת עולם SVG מוטמעת inline (`src/publishing/assets/map/world.svg`, Public Domain, מועתקת גם כנכס עצמאי ל-`assets/map/`) בשורה מלאה של רוחב העמוד, ציר זמן וכרטיס-דוח-אחרון זה-לצד-זה מעליה, וצ'יפים לסינון לפי אזור **וגם לפי אזור-קונפליקט** (שורה שנייה, אותו מודול - לא רביעי נפרד) מתחת למפה, כולם JS+`fetch()` שקוראים מ-`manifest.json` בזמן ריצה, בלי כותרות-מודול מילוליות (העיצוב עצמו מתקשר את התפקיד). צ'יפ-קונפליקט מנווט ישירות ל-`topic.html?conflict=X`; בחירת-אזור-קיימת (inline) מקבלת גם קישור-משני "פתח כדוח מלא" לאותו עמוד. ובונה גם **`topic.html`** (בכל שפה, בלי עותק-שורש - מוסכמת `about.html`) - עמוד-תצוגה נושאי חוצה-דוחות שקורא `?category=&region=&conflict=&from=&to=` (הכל אופציונלי ומשתלב) ומסנן את `manifest.json` בצד-לקוח בלבד (predicate ישיר על שדות-section קיימים - בלי אינדקס-חדש); `build_manifest()` כולל גם `newspaper_display_names` (מראה ל-`NEWSPAPER_DISPLAY_NAMES`, לתצוגה בלבד ב-`topic.html`). תמיד דורס, בלי `--force` (כמו `render.py`). ובונה גם **`accessibility.html`**/**`terms.html`** (בכל שפה, בלי עותק-שורש - אותה מוסכמה כמו `about.html`) - ראו "החלטות ארכיטקטוניות קבועות" לפרטי חבילת-הציות המלאה. **`_copy_favicons()`** (אותה מוסכמה כמו `_copy_logo()`) מעתיק את 4 קובצי-הפאביקון ל-`docs/assets/images/` - נוצרו פעם אחת (חד-פעמי, `scripts/generate_favicons.py`, Pillow) מתוך `MS_Logo.png` הקיים. **מפת דף-הבית מתויגת לנגישות בזמן-build** (`_load_map_svg_inline(lang, countries)`) - ראו "החלטות ארכיטקטוניות קבועות" לחריגה-מודעת-ומוסברת מעיקרון "הכל נקרא בזמן-ריצה".
- `data/raw/` - PDF-ים גולמיים (לא נכנס ל-git).
- `data/processed/` - `tracker.db` (מצב כל ה-Pipeline) ו-`extracted/` (עותקי טקסט) - שניהם לא נכנסים ל-git.
- `reports/he/`, `reports/en/` - דוחות שנוצרים ע"י `render.py` (מקור אמת). `docs/` - נבנה מהם ע"י `publish.py`; זהו המיקום שממנו GitHub Pages מגיש בפועל (כולל `docs/CNAME` לדומיין המותאם `geopolitics.meirshemesh.com`) - **אין לערוך קבצים בתוך `docs/` ידנית**, הם נדרסים בכל הרצת `publish.py`.
- `tests/` - טסטים, `unittest` מהספרייה הסטנדרטית (בלי תלות חדשה; כל טסט בונה DB זמני ולא נוגע ב-`tracker.db`). כרגע: `test_report_date_rule.py` - כלל שיוך-מאמר-לדוח.
- `scripts/` - סקריפטים חד-פעמיים, מחוץ ל-5 שלבי ה-Pipeline הראשי (כל אחד עומד בפני עצמו, לא מורץ שוטף). למשל `build_background_doc.py` - מפיק מסמך רקע/מותג עצמאי דו-לשוני (PDF+HTML לכל שפה) בגופן Assistant (מסמך זה בחר Assistant לפני שהאתר כולו אימץ אותו - כעת שני הצדדים בגופן זהה במקרה, אך `scripts/` עדיין לא תלוי ב-`src/`: קובץ הגופן שלו תחת `scripts/assets/` הוא עותק נפרד, לא ייבוא מ-`src/reporting/assets/fonts/`) ופלטת הצבעים הבסיסית מ-`render.py`, עם נכסי המקור שלו תחת `scripts/assets/` (למשל `MS_Logo.png`). תוצרי ה-build עצמם (`scripts/output/*.pdf`, `*.html`) לא נכנסים ל-git - נבנים מחדש מהסקריפט, כמו `docs/`/`reports/`. לחלופין, `scripts/` יכול גם להכיל תיקוני-למפרע חד-פעמיים ל-DB (למשל `fix_sources_included.py` - מתקן עמודת מטא-דאטה שגויה ישירות ב-`UPDATE`, בלי קריאת API) - אותה מוסכמה כמו `geo_tag_backfill.py` (ב-`src/analysis/`, כלי-עזר **קבוע**), רק ש`scripts/` מתאים לתיקון **חד-פעמי** שלא צפוי לרוץ שוב. `generate_favicons.py` (Pillow) - חד-פעמי דומה: הפיק את 4 קובצי-הפאביקון (`docs/assets/images/`) מ-`MS_Logo.png` הקיים בלבד (resize, לא לוגו חדש) - הפלט (`favicon.ico`/`favicon-32x32.png`/`favicon-16x16.png`/`apple-touch-icon.png`) כן נכנס ל-git (בניגוד ל-`scripts/output/` הרגיל), כי הוא נכס-אתר קבוע שמועתק בכל הרצת `publish.py` (`_copy_favicons()`), לא נבנה-מחדש מהסקריפט בכל פעם. `backfill_report_date.py` - חד-פעמי (בוצע 2026-09-20): מילא `report_date` ל-20 הקבצים שהחזיקו מאמרים יתומים, באותה לוגיקה של `mark_downloaded()` (אידמפוטנטי, לא נוגע בדוחות). `orphan_review.py` - כלי **קבוע** לקריאה בלבד (`python -m scripts.orphan_review`): מאתר מאמרים שנותחו ולא נכללו באף דוח + סימולציה של הכלל על הארכיון; רץ כבדיקה אחרי Synthesize.
- `requirements.txt` - רשימת תלויות (telethon, python-dotenv, pdfplumber, anthropic, weasyprint) - מותקנות ב-venv מקומי.

## כללי עבודה קבועים

- **מקורות MVP פעילים בפועל: 10** - הרשימה המקורית של 10 (ראו PROJECT_LOG), בתוספת The Washington Post, בניכוי The New York Times שמושעה-לצמיתות (הסבר בהמשך; המספר "9" שהופיע כאן ובקבצי-ההמשכיות אחרי הוספת WaPo היה טעות-ספירה, תוקן ב-2026-09-19 מול 10 הערכים שמחזיר `guess_newspaper()` בפועל): The Guardian, The Daily Telegraph (בריטניה); The Wall Street Journal, Los Angeles Times, USA Today, The Washington Post (ארה"ב); The Economist (ארה"ב/בינלאומי); Süddeutsche Zeitung, Die Welt, Der Spiegel (גרמניה). ראו PROJECT_LOG לנימוק האיזון הפוליטי המכוון, בפרט הזירה האמריקאית (מספר גוונים, לא זוג פשוט).
- **The New York Times מושעה-לצמיתות בכל מהדורה** (לא רק הבינלאומית, ולא נספר ברשימת ה-10 הפעילים למעלה) - נבדק-תוכן-בפועל גם במהדורה הביתית/Magazine ב-2026-09-12 ונמצא פגום באותה מידה בדיוק כמו הבינלאומית (ראו `src/ingestion/fetch.py` למעלה ו-PROJECT_LOG 4.34). The Washington Post נוסף כתחליף - נבדק-תוכן ואומת תקין לפני הוספה.
- כל התקשורת וההסברים עם המשתמש בעברית. כל הקוד, שמות המשתנים, הפונקציות וההערות בקוד באנגלית.
- ניהול תלויות באמצעות `venv`.
- בתחילת סשן חדש או לאחר מעבר למחשב אחר, יש להפעיל את `/resume-project` (סקיל ידני בלבד, ללא הפעלה אוטומטית) - מבצע `git pull` תחילה (עוצר אם יש קונפליקט/שינויים לא-committed), ואז משחזר מצב מ-CLAUDE.md, HANDOFF.md, ומ-PROJECT_LOG.md (אם קיים).
- בסיום סשן יש להפעיל את `/handoff` (סקיל ידני בלבד) - מעדכן CLAUDE.md/HANDOFF.md ולעיתים PROJECT_LOG.md, ואז מבצע רצף git חצי-אוטומטי: `git add .` אוטומטי, בדיקת קבצים חשודים, הצעת הודעת commit, ו-commit+push **רק** אחרי אישור מפורש מהמשתמש.
- סודות (`TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `ANTHROPIC_API_KEY`) נשמרים ב-`.env` בשורש הפרויקט (לא נכנס ל-git) ונטענים באמצעות `python-dotenv`. `.env.example` מתעד את שמות המשתנים הנדרשים בלי ערכים.
- WeasyPrint (הפקת PDF) דורש גם GTK3 Runtime ברמת מערכת ההפעלה, לא רק `pip install weasyprint` - ב-Windows: `winget install tschoonj.GTKForWindows`. בלעדיו `import weasyprint` נכשל; `render.py` מזהה זאת אוטומטית ומדלג רק על שלב ה-PDF (קובצי ה-HTML עדיין נוצרים כרגיל).
- הגדרות GitHub Pages/דומיין (כמו `docs/CNAME`) עלולות להיערך ישירות בממשק ה-Web של GitHub, לא רק דרך git מקומי - לפני `push` אחרי עבודה על `docs/`/הגדרות פרסום, שווה `git pull` מקדים גם באמצע סשן, לא רק בתחילתו (ראו PROJECT_LOG 4.13 למקרה קונפליקט אמיתי שקרה כך).
- **לפני הרצת Screening על קבצים חדשים שירדו, יש לסרוק אותם לחתימת-שחיתות "tradingref.com"** (עמוד 0 עם ~14 תווי-טקסט בלבד + תמונה יחידה - PDF סרוק-בלבד, image-only) - דפוס שחוזר על עצמו בפועל (NYT International, Guardian, USA Today Sports Weekly, ומהדורות-Economist-אזוריות, ראו PROJECT_LOG 4.20/4.26). בדיקה יזומה לפני Screening חוסכת תשלום מיותר על סינון/ניתוח של קבצים חסרי-תוכן - עדיין מבוצעת כסקריפט אד-הוק בכל הרצה, לא כשלב-קוד קבוע בפייפליין (ראו action item פתוח ב-HANDOFF.md). **אותה בדיקה חייבת גם להשוות SHA-256 של כל קובץ חדש מול הקבצים שכבר ב-DB, לא רק בתוך הקבוצה החדשה** - כפילות-Economist (זהה-בייטים לקובץ שהורד יום קודם) נצפתה 3 פעמים, ובדיקה בתוך-הקבוצה-בלבד לא הייתה תופסת אותה (PROJECT_LOG 4.43, action items 24/38/39). כשקובץ כפול נשאר ב-DB, מריצים `screen.py --file <תת-מחרוזת ייחודית>` לכל קובץ לגיטימי בנפרד במקום ריצה כללית - כדי לא לשלם על סינון הכפול.
- **אחרי Synthesize יש לוודא שאף מאמר מהקליטה החדשה לא נשאר מחוץ לדוחות**: לספור מאמרים ב-`articles` שאינם ב-`report_section_articles` (נבחרים לפי `REPORT_DATE_SQL`, לא לפי `date(published_at)`); הכלי: `python -m scripts.orphan_review` (קריאה בלבד, שורת VERDICT בסוף). התוצאה אמורה להיות 0 - ונכון ל-2026-09-20 היא 0 בכל הארכיון: הפער ההיסטורי של 308 מאמרים (9.7%) נסגר (PROJECT_LOG 4.44). סטייה - מדווחים, לא מתקנים בשקט. מקרה-ידוע שהכלל החדש לא מכסה: עמוד שנכשל ב-Analysis ומשוחזר ביום מאוחר (ראו "החלטות ארכיטקטוניות קבועות").
- **כל סקריפט ב-`src/` שמריץ `print()` על תוכן שמקורו בטקסט חיצוני** (כותרות מאמרים, שמות אנשים, וכו') **חייב `sys.stdout.reconfigure(encoding="utf-8")` בתחילת הקובץ** - Windows קובע כברירת מחדל את קידוד ה-console (cp1252) גם כש-stdout מנותב לקובץ, מה שגורם ל-`UnicodeEncodeError` בלתי-נתפס על כל תו מחוץ ל-cp1252 (למשל שמות בלקניים/סלאביים). כל 5 סקריפטי ה-Pipeline (`fetch.py`, `extract.py`, `screen.py`, `analyze.py`, `synthesize.py`) נתקלו בזה בפועל ותוקנו; `render.py` תוקן מראש (`publish.py` מכוסה עקיפין דרך ה-import ממנו). כל סקריפט חדש עתידי חייב לכלול את זה מההתחלה.
- **כל שינוי במבנה/נתיבי-פלט של `render.py` או `publish.py`** (שמות קבצים, קישורים פנימיים, מבנה HTML) **מחייב regeneration מלא של כל התאריכים הקיימים בארכיון** (לא רק תאריך בדיקה בודד) **+ `git status` מפורש לפני commit** - כדי לוודא שאין קבצים ישנים שנשארו "תקועים" עם המבנה הקודם. הדפוס הזה נתפס שוב ושוב רק ברגע האחרון: תיוג גיאוגרפי (backfill נדרש למאמרים קיימים), עוגני `section-id` (נכתבו בקוד אך לא נאפו לקבצים קיימים), והעברת הארכיון ל-`archive.html` (קישורי "חזרה" בדוחות קיימים המשיכו להצביע על `index.html` הישן). לפני שמכריזים על שינוי כזה "גמור" - להריץ מחדש את `render.py` על כל תאריך קיים ואת `publish.py`, ורק אז לבדוק `git status`.

## מגבלות והעדפות

- דו-לשוניות (עברית + אנגלית) היא דרישת ליבה שחייבת להיות מובנית בשלב ה-Reporting מההתחלה - לא תוספת מאוחרת.
- PDF בעברית דורש CSS עם `direction: rtl` וגופן תומך עברית ב-WeasyPrint - יש לקחת זאת בחשבון בכל עבודה על `src/reporting/` ותיקיית `reports/`.

## פקודות הפעלה ובדיקה

אין build/lint מוגדרים - הפעלה היא הרצת סקריפטים ישירות (כולם דורשים venv פעיל); בדיקות אוטומטיות - ראו שתי הפקודות האחרונות ברשימה:

- `python -m src.ingestion.fetch` - משיכת PDF-ים חדשים מטלגרם.
- `python -m src.extraction.extract` - חילוץ טקסט מקבצים שהורדו וטרם חולצו.
- `python -m src.analysis.screen` - שלב א' של Analysis (סינון רחב, Haiku). אופציונלי: `--file <מחרוזת בשם הקובץ>` להגבלה לקובץ יחיד לצורך בדיקה.
- `python -m src.analysis.analyze` - שלב ב' של Analysis (ניתוח מעמיק, Sonnet, כולל תיוג גיאוגרפי). אופציונלי: `--file-id <int>` להגבלה לקובץ יחיד לצורך בדיקה.
- `python -m src.analysis.geo_tag_backfill` - מתייג בדיעבד מאמרים קיימים שחסר להם תיוג גיאוגרפי (בקבוצות, Haiku זול). אופציונלי: `--date <YYYY-MM-DD>` להגבלה לתאריך יחיד לצורך בדיקה. בטוח להרצה חוזרת - מאמר שכבר תויג לא מתעבד שוב.
- `python -m src.reporting.synthesize --date <YYYY-MM-DD>` - שלב א' של Reporting. אופציונלי: `--force` למחיקה ובנייה מחדש של דוח קיים לאותו תאריך.
- `python -m src.reporting.render --date <YYYY-MM-DD>` - שלב ב' של Reporting, מפיק 4 קבצים (HTML+PDF × עברית+אנגלית) מתוך דוח שכבר נבנה ע"י `synthesize.py`.
- `python -m src.publishing.publish` - שלב Publishing, בונה/מעדכן את `docs/` מתוך כל התאריכים הקיימים ב-`reports/`. בלי דגלים, תמיד דורס.
- `python -m unittest discover -s tests -v` - הטסטים (מהשורש; שניות ספורות, בלי API ובלי `tracker.db`).
- `python -m scripts.orphan_review` - קריאה בלבד: כמה מאמרים נותחו ולא נכללו באף דוח (אמור להיות 0), וסימולציית הכלל על הארכיון.

## החלטות ארכיטקטוניות קבועות

Pipeline בן 5 שלבים עוקבים, כל שלב בתת-תיקייה נפרדת תחת `src/`:

Ingestion -> Extraction -> Analysis -> Reporting -> Publishing

שלב ה-Reporting מייצר משתי גרסאות שפה (עברית ואנגלית) לכל דוח, ובשני פורמטים (PDF + HTML) - זהו חלק קבוע מהארכיטקטורה ולא הרחבה עתידית. Python 3 + SQLite הם הבסיס הטכנולוגי הקבוע.

שלב ה-Analysis בנוי כשני תתי-שלבים עצמאיים ונפרדים (כל אחד ניתן להרצה ולבדיקה בלי השני):
1. **סינון רחב** (`screen.py`, Claude Haiku) - שאלה בינארית זולה על כל עמוד: "האם ייתכן שיש כאן תוכן גאופוליטי?". מכוון להיות רשת רחבה מתוך כוונה - במקרה ספק מסמן רלוונטי, כדי לא לפספס תוכן. התוצאה נשמרת ב-`page_screening`.
2. **ניתוח מעמיק** (`analyze.py`, Claude Sonnet) - רק על עמודים שסומנו רלוונטיים בשלב א', מזהה מאמרים בודדים ומחלץ מהם שדות מובנים (כותרת/מחבר/נושא/עמדה/ציטוט) לטבלת `articles`.

`src/common/db.py` הוא מקור האמת היחיד לסכמת ה-DB: הוספת עמודה/טבלה חדשה נעשית **רק** ע"י עריכת `TABLE_COLUMNS`/`TABLE_CONSTRAINTS` שם - `init_db()` מזהה ומוסיף את מה שחסר אוטומטית בכל הרצה, גם על `tracker.db` קיים. אין לכתוב הצהרות `CREATE TABLE`/`ALTER TABLE` ידניות בקבצים אחרים.

**שיוך מאמר לדוח: לפי יום ריצת הפייפליין (היום המקומי שבו הקובץ הורד), לא לפי `published_at` ולא לפי תאריך שנגזר משם-הקובץ** (הוחלט 2026-09-20, ראו PROJECT_LOG 4.43/4.44). `downloaded_files.report_date` נקבע פעם אחת בקליטה: `mark_downloaded()` גוזר אותו מ-`downloaded_at` לפי אזור-הזמן של מערכת ההפעלה (`report_date_for_download(downloaded_at, tz=None)` - ה-`tz` קיים כדי שהטסטים יקבעו אזור-זמן במפורש), בלי שינוי ב-`fetch.py`. הכלל נשמר בטסט קבוע, `tests/test_report_date_rule.py`, שכולל גם שמירה מפני שאילתה חדשה שמסננת ישירות לפי `date(df.published_at)`. `REPORT_DATE_SQL` (`COALESCE(df.report_date, date(df.published_at))`, ב-`db.py`) הוא הביטוי היחיד שמותר להשתמש בו כדי לבחור "מאמרי דוח X" - `get_articles_for_date`/`get_sources_for_date`/`get_articles_without_geo_tags` משתמשים בו, ושאילתה חדשה מסוג זה חייבת להשתמש בו ולא ב-`date(published_at)` ישירות.
- **למה**: `published_at` הוא זמן ההעלאה לטלגרם, והוא נסחף מיום-הכיסוי (שבועונים, מהדורות-סופ"ש, מוספים, קבצים שמועלים בשעה מאוחרת ב-UTC). הסקירה של 2026-09-20 מצאה 308 מאמרים מתוך 3,178 (9.7%) שנותחו ומעולם לא נכללו באף דוח, כולל 163 מאמרי Economist. כולם מאותו מנגנון בדיוק: הדוח של "יום ה-`published_at`" שלהם כבר נבנה לפני שהקובץ נותח. הכלל הוחל למפרע על 20 הקבצים שהחזיקו אותם (`scripts/backfill_report_date.py`), 4 הדוחות המושפעים נבנו מחדש (28.8, 13.9, 18.9, 19.9), וכל 308 המאמרים שוקפו - אומת מאמר-מאמר ב-DB וב-HTML המרונדר, בשתי השפות.
- **נשקל ונדחה**: (א) גזירת תאריך משם-הקובץ (action item 29) - נדחה במכוון: שמות הקבצים לא אחידים בין מקורות וכל וריאנט חדש דורש תיקון-קוד, בעוד יום-הריצה תמיד ידוע. (ב) יום-הניתוח במקום יום-ההורדה - נדחה: בריצות רגילות הם זהים (0 סטיות מאז 10.9), ונבדלים רק בריצות שמתפצלות על פני ימים (24.8, 30.8, 9.9); יום-ההורדה נקבע פעם אחת ולא זז כש-retry של עמוד שנכשל קורה יום אחרי. (ג) UTC במקום שעון-מקומי - נדחה: "היום" בפרויקט הוא תאריך-המערכת המקומי; אף הורדה עד כה לא נפלה בין 00:00 ל-03:00 מקומי (0 מתוך 168), כך שההבדל תיאורטי כרגע, אך נכון להגן עליו.
- **כללי מעבר**: שורות שנקלטו לפני הכלל נשארות `report_date=NULL` ומתנהגות בדיוק כמקודם (נבדק על עותק של ה-DB: 30 תאריכים, תוצאה זהה לחלוטין), ולכן אין צורך ב-backfill גורף ואין לעשות אחד: **הכלל לא הוחל למפרע על ה-backlog של 3.9–9.9**, שם 813 המאמרים היו הופכים 7 דוחות יומיים תקינים (380/102/125/27/70/90/19) לדוח אחד. ה-backfill היחיד (2026-09-20) כיסה רק את 20 הקבצים שהחזיקו מאמרים יתומים: 12 קבצי 24–27.8 ← 28.8, 5 קבצי Washington Post של 6–11.9 ← 13.9, Economist ← 18.9, Guardian file 187 ← 18.9, WSJ Magazine ← 19.9. `compute_scan_limit()` נשאר על `MAX(published_at)` בכוונה (הוא מודד את חלון-הסריקה בערוץ, לא שיוך-דוחות), וכך גם תאריך-הציטוט ב"מראי מקום" (`get_section_citations`).
- **השלכות ידועות**: (1) **ריצת-backlog אחרי פער רב-יומי מייצרת דוח אחד "עבה"** במקום כמה דוחות יומיים. בסימולציה על הארכיון: 9.9 היה 813 מאמרים במקום 19, ו-24.8/28.8/13.9 היו 308/358/177. שלב 1 של `synthesize.py` הוא קריאה בודדת עם `max_tokens=16000`; נמדדו עליו 246 מאמרים ב-8,535 טוקני-פלט ו-380 מאמרים עברו בהצלחה, והתקרה המשוערת (לא נמדדה) היא כ-450–480 מאמרים. מדידות נוספות מ-2026-09-20: 358 מאמרים ב-11,849 טוקני-פלט (74% מהתקרה) בלי retry, ואילו ב-321 מאמרים (18.9) הניסיון הראשון החזיר 100% מאמרים לא-משויכים ורק ה-retry היחיד תיקן זאת (סיבה לא נבדקה) - כלומר "מתחת לתקרה המשוערת" אינו ערובה. לפני ריצת-backlog גדולה יש לבדוק את מספר-המאמרים הצפוי ולהחליט מראש על פיצול. (2) **עמוד שנכשל ב-Analysis ומשוחזר ביום מאוחר** מצטרף לדוח של יום-ההורדה המקורי שכבר פורסם, ונשאר פער (נצפה: 2 מאמרי Guardian מ-18.9); הכלל הזה לא פותר אותו. (3) **מראי-המקום מציגים את תאריך ההעלאה של כל מאמר**, לא את תאריך הדוח (Economist בדוח 18.9 מצוטט כ-"17.9.2026", מאמרי 24–27.8 בדוח 28.8 מצוטטים בתאריכיהם) - נשאר כך בכוונה; החלטה פתוחה, PROJECT_LOG action item 41.

שמות עיתונים בכל תוצר (דוחות, פרומפטים ל-Claude) משתמשים תמיד במיפוי קבוע יחיד `NEWSPAPER_DISPLAY_NAMES` (בלי הפרדה ל-HE/EN) בכתיב הלטיני המקורי - 10 מקורות ה-MVP הפעילים בפועל, פלוס ערך-מת עבור NYT (המושעה-לצמיתות, נשאר במיפוי אך `guess_newspaper()` לעולם לא מחזיר אותו - ראו "כללי עבודה קבועים") - לעולם לא תעתיק/תרגום עברי, גם בתוך טקסט עברי וגם בתוך הנחיות לפרומפט עצמו. המפתח הפנימי הנשמר ב-DB (`downloaded_files.newspaper`, גם מה שמחזיר `guess_newspaper()`) הוא הצורה הקצרה-בלי-"The" (כמו `NEWSPAPER_DISPLAY_NAMES` הקיים כבר עשה ל-Guardian/Daily Telegraph); `NEWSPAPER_DISPLAY_NAMES` מוסיף "The"/"Der" רק כשזה חלק אמיתי מהשם הרשמי (למשל "The Wall Street Journal", "The Economist"), לא כשזה סתם תווית-גנרית (Los Angeles Times/USA Today נשארים בלי "The").

עיקרון מנחה שחזר פעמיים בפרויקט: כשמתגלה כשל חוזר ולא-תלוי-הקשר בהתנהגות מודל (למשל שגיאות רשת חולפות ב-Analysis, השמטת article_id-ים ב-Synthesis) - הפתרון הוא רשת-ביטחון גנרית ברמת הקוד (טיפול שגיאות ברמת-יחידה + retry, fallback דטרמיניסטי), לא רדיפה אחרי כל מקרה בנפרד דרך שינויי פרומפט נקודתיים.

`report_sections.category` הוא מיפוי קבוע של 8 קטגוריות (security_conflict/diplomacy_international/trade_economics/domestic_politics/migration_society/society_culture/technology_media/energy_environment) שהמודל בוחר בעצמו באותה קריאת `synthesize.py`, פלוס ערך שמור `additional_coverage` שלעולם לא מוצע למודל - הוא מסמן section שנוצר ע"י מנגנון ה-fallback ולא ע"י המודל. שינוי ברשימת הקטגוריות דורש עדכון גם ב-`synthesize.py` (ה-enum בסכמת הכלי) וגם ב-`render.py` (`CATEGORY_LABELS`/`CATEGORY_STYLES`).

תיוג גיאוגרפי (`article_countries`/`article_conflict_zones`, many-to-many כי מאמר יכול לגעת ביותר ממדינה/קונפליקט אחד) נכתב ע"י `analyze.py` באותה קריאת API הקיימת שכבר מפיקה headline/region_topic/וכו' - לא קריאה נפרדת. `articles.region_topic` (טקסט חופשי) נשאר כמו שהוא לצדו, לא מוחלף. **חשוב לפרשנות עתידית של הנתונים**: מאמר בלי אף שורה לא ב-`article_countries` ולא ב-`article_conflict_zones` יכול להיות גם מאמר שטרם עבר תיוג וגם מאמר שתויג ונמצא נכון שאין לו מדינה/קונפליקט רלוונטיים (המקרה השני קורה בפועל, לא נדיר) - שני המצבים נראים זהים ב-DB. אין להניח "0 שורות = טרם עובד".

הרחבת רשימת המדינות ב-`geo_taxonomy.py` (`COUNTRY_LIST`/`COUNTRY_TO_REGION`) בעתיד היא ציפייה מובנית, לא חריגה - הרשימה ההתחלתית (94 מדינות) נבנתה מכיסוי אמיתי שכבר נצפה, לא מכסה את כל 195 מדינות העולם בכוונה.

`manifest.json` (נבנה ע"י `build_manifest()` ב-`publish.py`) הוא קובץ סטטי בלבד - נוצר בזמן build, לא API/שרת בזמן ריצה, כמו כל שאר האתר. `sections` הוא מקור האמת המלא (נושא, קטגוריה, מקורות, מדינות/קונפליקטים, קישורים); `countries`/`conflict_zones`/`dates`/`categories`/`regions` הם **רק אינדקסים/תרגומי-תצוגה** מעל `sections` (`countries`/`conflict_zones`/`dates` כוללים אך ורק ערכים עם לפחות section אחד, לעולם לא כל 94 המדינות; `categories` משוכפל מ-`CATEGORY_LABELS` הקיים ב-`render.py` כדי שה-JS לא יצטרך רשימה קשיחה) - לעולם לא כולל את `comparison_text` המלא (כדי שהקובץ יישאר קטן ככל שהארכיון גדל). `regions` נגזר מ-`COUNTRY_TO_REGION` (אותו עיקרון בדיוק כמו `categories`) - `regions[key] = {name_he, name_en, country_codes, section_ids}`, כשה-`section_ids` הוא איחוד (union) של כל המדינות באזור, ורק אזורים עם `section_ids` לא-ריק נכללים (בפועל גדל עם הזמן - `oceania` כבר לא ריק נכון ל-30.8, בזכות כיסוי הקשור לאוסטרליה). `newspaper_display_names` (מראה ל-`NEWSPAPER_DISPLAY_NAMES`) נוסף באותה רוח בדיוק - תצוגה בלבד, לא לוגיקת-סינון. קישורי `href_he`/`href_en` הם נתיבים יחסיים מתוך `docs/` עצמו (`he/report_<date>_he.html#section-<id>`), **ללא** קידומת `../` מוטמעת מראש - כל צרכן (למשל דף הבית) מוסיף את קידומת-העומק שלו (`asset_prefix`) בזמן ריצה, כי אותו manifest משרת צרכנים בעומקים שונים תחת `docs/` (שורש/`he/`/`en/`).

**דף הבית** (`build_homepage_html()` ב-`publish.py`) הוא הצרכן הראשון של `manifest.json`, ונבנה בשלושה מיקומים בדיוק כמו הארכיון הישן (`docs/index.html` שורש-עברית-ברירת-מחדל, `docs/he/index.html`, `docs/en/index.html`) - כל עותק מקבל קבוע JS `asset_prefix` (`""`/`"../"`) שנקבע בזמן build לפי מיקומו, ומשמש הן לנתיבי-נכס סטטיים (גופן/מפה/`manifest.json` עצמו) והן לתיקון קישורי ה-`href_he`/`href_en` שמגיעים מה-manifest בזמן ריצה. שלושת המודולים (מפה/ציר-זמן/דוח-אחרון) קוראים את `manifest.json` דרך `fetch()` **בזמן טעינת הדף**, לא embedded בזמן build - עיקרון קבוע: שום נתון מה-manifest לא "נאפה" ישירות ל-HTML הסטטי, גם כשזה טכנית אפשרי (כמו כרטיס הדוח האחרון), כדי ששינוי ב-`manifest.json` תמיד ישתקף בלי לבנות מחדש את דף הבית עצמו. מפת העולם (`assets/map/world.svg`, Public Domain, Wikimedia Commons) מוטמעת **inline** בגוף העמוד (לא `<img>`) כדי לאפשר ל-JS לצבוע/להוסיף click handler לכל `<g id="xx">` (קוד ISO 3166-1 alpha-2). הפילטור (קליק על מדינה/תאריך/אזור → פאנל תוצאות משותף) הוא **single-select** - קליק מחליף את הפילטר הפעיל, לא AND משולב בתוך דף הבית עצמו. סינון-AND-משולב אמיתי (קטגוריה+אזור+קונפליקט+טווח-תאריכים) קיים דרך `topic.html` (ראו למטה) - נגיש מדף הבית (צ'יפ-קונפליקט, קישור "פתח כדוח מלא" מבחירת-אזור), לא כ-UI-אינטראקטיבי על העמוד עצמו.

**`topic.html`** (`build_topic_html()` ב-`publish.py`) הוא עמוד-תצוגה נושאי חוצה-דוחות, בכל שפה, **בלי עותק-שורש** (מוסכמת `about.html`, לא `archive.html` - `topic.html` הוא יעד-קישור-עומק, לא יעד-ניווט-ראשי). קורא `?category=&region=&conflict=&from=&to=` (הכל אופציונלי, משתלב) וממיין את `manifest.json` **כולו בצד-לקוח, בלי אינדקס-חדש**: כל section כבר נושא `date`/`category`/`countries`/`conflict_zones` משלו, כך שסינון-AND-משולב הוא predicate ישיר על כל section, לא intersection של רשימות `section_ids` - עיקרון-מפתח שחל על כל הרחבה עתידית דומה למניפסט: לפני שמוסיפים אינדקס חדש, לבדוק אם השדות הקיימים על כל רשומה כבר מספיקים לסינון-הישיר. כל תוצאה מקושרת ל-`href_he/en#section-{id}` האמיתי (כולל תאריך-הדוח שממנו היא מגיעה) - לעולם לא משכפלת/כותבת-מחדש `comparison_text`.

**פריסת דף הבית (v2)**: שתי שורות, לא גריד תלת-טורי אחיד - שורה עליונה = ציר-זמן וכרטיס-דוח-אחרון זה-לצד-זה; שורה תחתונה = מפה ברוחב מלא של העמוד (במקום שליש-טור כמו ב-v1) כדי שקליק על מדינה בודדת מתוך 94 יהיה שימושי בפועל, לא רק אסתטי. אין אף כותרת-מודול מילולית ("מפה"/"ציר זמן" וכו') - עיקרון מכוון: העיצוב עצמו מתקשר את התפקיד של כל מודול, לא טקסט. צ'יפים לסינון לפי אזור (מתוך `regions` שבמניפסט, עד 8) מתחת למפה - קליק על צ'יפ מסנן את פאנל התוצאות **וגם** מדגיש (`is-selected`, אותה מחלקת CSS בדיוק כמו בחירת מדינה בודדת - לא מחלקה נפרדת) את כל מדינות האזור על המפה. כרטיס הדוח האחרון מציג מספר-תאריך גדול ומודגש (באותו פורמט `DD.MM` כמו תאי ציר-הזמן) ותקציר-נושא של ה-section הראשון, בלי מילות "היום"/"דוח" - עקבי עם עיקרון-בלי-כותרות.

**מוסכמה: לוגו מקושר לדף הבית בכל top-nav** - `build_nav_html()` (`render.py`, משמש גם ל-`about.html`) וה-nav הנפרד של `build_index_html()` (`archive.html`) כוללים תמיד תמונת לוגו (56px) עטופה ב-`<a>` שמצביעה על `index.html` (מותאם לעומק-הנתיב של כל עמוד); ה-nav היחיד היוצא מן הכלל הוא top-bar של דף הבית עצמו - הלוגו שם (56px, הוגדל מ-32px) **אינו** מקושר לשום מקום, כי דף הבית כבר "בית". לוגו נוסף וגדול יותר (96px) מופיע בכרטיס "אודות הכותב" בתחתית `about.html` - תפקיד שונה (זיהוי אישי, לא ניווט), מוסכמת גודל נפרדת.

לאימות ויזואלי של עמודים שתלויים ב-JavaScript בזמן ריצה (כמו דף הבית) - WeasyPrint לא מריץ JS בכלל, אז השיטה הרגילה (רינדור ל-PNG) לא מספיקה. אפשר להריץ **Playwright headless Chromium דרך `npx --yes playwright@latest`** (לא תלות קבועה בפרויקט, לא ב-`requirements.txt`) מול שרת HTTP מקומי (`python -m http.server` בתוך `docs/`) - `fetch()` על `file://` נחסם ע"י דפדפנים. אותה התקנה משמשת גם להזרקת `axe-core` (דרך `page.evaluate`) לבדיקת-נגישות אוטומטית לפני/אחרי שינוי - עדיף על ה-CLI העצמאי של axe-core, שדורש chromedriver בגרסה תואמת למחשב הספציפי.

**סדר-נושאים בדוח לפי רוחב-כיסוי, לא סדר-קיבוץ-שרירותי** - `section_coverage(newspapers)` (`render.py`, פומבי - `publish.py` מייבא אותו) מחשב `(source_count, language_count)` מנתונים שכבר קיימים בכל section (בלי קריאת-API נוספת, בלי שינוי-סכמה); שפת-מקור נגזרת ממיפוי קבוע נפרד, `NEWSPAPER_LANGUAGES` (לא `NEWSPAPER_DISPLAY_NAMES` - זה למיון, לא לתצוגה). נושאים ממוינים לפי `(language_count desc, source_count desc)` - "זום-אאוט לזום-אין": כיסוי-חוצה-שפות/מקורות אמיתי מוביל, נושא-מקומי-חד-מקורי בסוף. נושאי-fallback (`additional_coverage`) מתחרים **על אותו ציון בדיוק** ולא נדחקים לסוף באופן מובנה - מבחינה בפועל הם כמעט תמיד מגיעים ל-source_count=language_count=1 (מאמר בודד מטבעם) ומתמקמים נמוך על-סמך התוכן שלהם, לא מכוח כלל קשיח. אותו `section_coverage()` גם קובע אילו נושאים מקבלים הדגשה-ויזואלית עדינה (כותרת מוגדלת + רוחב-כרטיס מוגדל בכ-1.5rem דרך margin-inline שלילי, עם padding-inline מוגדל באותו סכום כדי שהטקסט עצמו יישאר מיושר מול כרטיסים שכנים) - סף: `source_count>=3 or language_count>=2`. `publish.py`'s `build_manifest()` משתמש באותו `section_coverage()` בדיוק כדי שסדר `dates[date].section_ids` (וממנו: כרטיס "הדוח האחרון" בדף הבית) יתאים לסדר האמיתי בדוח עצמו, לא לסדר-הכנסה גולמי.

**חריגה מודעת לעיקרון "הכל נקרא בזמן-ריצה" בדף הבית: תיוג-נגישות של המפה הוא תמונת-מצב בזמן-build** - כל שאר דף-הבית (צביעת-מדינות, פאנל-תוצאות, צ'יפים) נשאר Fetch-בזמן-ריצה מ-`manifest.json` כרגיל; אבל `aria-hidden="true"` (למדינה בלי כיסוי היום) מול `role="img" aria-label="..."` (למדינה עם כיסוי) על כל `<g id="xx">`/`<path id="xx">` במפת ה-SVG המוטמעת נקבעים **פעם אחת, ב-`publish.py`**, לפי `manifest["countries"]` הקיים אז - כי תוכן-נגישות-סטטי לא צריך "לחיות", בניגוד להתנהגות-אינטראקטיבית. שני ממצאים אמפיריים שקדמו להחלטה הזו (לא הונחו): (1) ה-SVG מערבב שתי צורות שונות למדינה בודדת - מדינה רב-חלקית (איים/מובלעות) היא `<g id="xx">` שעוטף כמה `<path>` (שכל אחד מהם נושא id עם סיומת שונה, למשל `"il-"`, לעולם לא הקוד החשוף עצמו), מדינה חד-חלקית היא `<path id="xx">` בודד בלי `<g>` עוטף בכלל - אף תבנית לבדה לא מכסה את כל כ-90 המדינות המכוסות, רק האיחוד של שתיהן; (2) `aria-label` על אלמנט-SVG בלי role תקין כלל לא מובטח כנגיש (נתפס ע"י axe-core כ-`aria-prohibited-attr`) - `role="img"` נבחר במפורש על-פני `role="button"` כי המדינות עדיין לא ניתנות-למיקוד-מקלדת (קליק-עכבר בלבד, בלי `tabindex`) - "button" בלי הפעלה-מקלדת אמיתית היה טענת-נגישות-שקרית.

**עיקרון: לפני תיקון-קוד לבאג-זיהוי/תבנית-שם, לסרוק רטרואקטיבית את מלוא-ההיקף הרלוונטי כדי לדעת אם זה חד-פעמי או שיטתי** - יושם פעמיים בסשן זה (הפעלת/פתרון-מקרה ב-PROJECT_LOG 4.32: וריאנט-כתיב חסר-ניקוד ב-`guess_newspaper()`; וגם סריקת כל report_sections לחיפוש תווי `<`/`>` לפני שנקבע שבאג-דליפת-תג היה חד-פעמי) - כדי שההחלטה "לתקן רק את המופע הבודד" מול "צריך backfill לימים ישנים" תהיה מבוססת-מדידה, לא הנחה. אם הסריקה חושפת מופעים נוספים בימים כבר-מפורסמים - זה עדיין **לא** אישור אוטומטי ל"פתיחה מחדש" (ראו הפסקה הבאה) - זו החלטה נפרדת שדורשת אישור מפורש.

**"תיקון-בזמן-אמת של דוח שפורסם מוקדם מדי" שונה מהותית מ"פתיחה מחדש של ארכיון-יציב"** - עיקרון-הבחנה מפורש (לא רק ניסוח): דוח שפורסם באותו יום, לפני שהערוץ סיים להעלות את כל המקורות שלו, ותוקן **באותו יום** (למשל בעקבות תיקון-קוד ל-`guess_newspaper()` שהתגלה כחוסם מקור שכבר-קיים בערוץ) - זה תפעול-שוטף-רגיל, לא תקדים. פתיחה מחדש של יום **ישן ויציב** (שכבר עבר "היום שלו" ולא צפוי להתעדכן עוד) היא סוג-החלטה שונה לגמרי ודורשת שיקול נפרד בכל מקרה - ראו PROJECT_LOG 4.32 לדוגמה מלאה שבה ההבחנה הזו נדרשה במפורש.

**חבילת-ציות רגולטורי בסיסית** (`accessibility.html`/`terms.html` + סעיף-גילוי-נאות ב-`about_content.py`, ראו PROJECT_LOG 4.38) - מכוונת ל-ת"י 5568 חלק 1 (מבוסס WCAG 2.0 AA), **בלי** זכויות-יוצרים ו**בלי** אנליטיקס/מעקב חדש (אושר-בפועל לפני תחילת העבודה שהאתר נקי מקוד-מעקב, לא הונח). כולל **פוטר-משותף חדש** (`build_footer_html()`/`shared_chrome_css()` ב-`render.py`) שקושר לשני העמודים החדשים מכל 5 סוגי-העמוד, ותיקוני-ניגודיות **שנמדדו בפועל** (נוסחת-WCAG המלאה על ערכי-הצבע הריאליים, לא הערכה) - `--chip-selected-text` (token תלוי-ערכת-נושא חדש, כי `--masthead-accent` הופך לוורוד-בהיר במצב-כהה כך שטקסט-לבן-קבוע על הצ'יפ-הנבחר נכשל שם), הגבלת עצימות-המיזוג של תאי-ציר-הזמן ל-55% (במקום 80%, שנכשל בשתי הערכות דווקא בימים-בעלי-הכיסוי-הגבוה ביותר), ותיקון-צבע לתג-קטגוריית "כלכלה וסחר". ניווט-מקלדת למפת-העולם **נשאר פער-ידוע-ומוצהר בכוונה** בהצהרת-הנגישות עצמה (לא הוסתר) - עקבי עם ה-`role="img"`-לא-`"button"` שכבר תועד למעלה.
