# Project Handoff

## הערות פתיחה להרצה הבאה (לקרוא לפני שמתחילים)

- **תעדוף גבוה, לא בוצע**: הפיכת בדיקת-שחיתות-PDF (tradingref.com) + השוואת-SHA-256-מול-הארכיון מסקריפט-אד-הוק לקוד-קבוע בפייפליין (action item 54) - המשתמש ביקש עדיפות גבוהה יותר. הערכת-זמן קיימת (PROJECT_LOG 4.53): כ-2-2.5 שעות. לא בוצע.
- **WSJ פגום (`tradingref.com`) יומיים רצופים** (23.9 id=225, 24.9 id=235) - **לא הושעה** (המשתמש: יומיים לא מספיק, מקורות אחרים התאוששו בעבר). לעקוב: מופע שלישי = דפוס מאושר, שווה לדון בהשעיה.
- **שני זוגות-כפילות-Economist נמצאו בסריקה מקיפה (25.9) ולא טופלו**: id=41/70 (29.8) ו-105/107 (5.9) - **שניהם** בכל זוג תרמו מאמרים **שכבר פורסמו** (לא יתומים כמו 228/229 שכן נוקו) - סימן לכיסוי-כפול-בפועל בשני דוחות ישנים. Resynthesis של דוח-ישן-ויציב הוא סוג-החלטה נפרד (action item 55) - לא לגעת בלי דיון מפורש.
- **`analyze.py` עדיין חסר מנגנון-הגנה-כללי לפי-קובץ** (action item 52) - הרצה כללית (בלי `--file-id`) תעבד כל עמוד `pending`, כולל קבצים-כפולים-ישנים שנשארו ב-DB. אם מופיע קובץ-כפול-חדש: יש להריץ `analyze.py --file-id <id>` נפרד לכל קובץ לגיטימי, לא ריצה גורפת - עד שהמנגנון הכללי ייבנה.
- **`analyze.py` קיבל שני תיקונים חדשים (25.9, חלים רק על הרצות-חדשות מכאן ואילך)**: אכיפת-תיוג-עצמי (regex safety net על `stance_summary`) וזיהוי-עמוד-מכתבים (WSJ+Süddeutsche בלבד - **Die Welt לא מכוסה**, לא ניחוש). **אין resynthesis רטרואקטיבי** לדוחות ישנים בעקבותיהם.
- **לפני כל הרצה**: `python -m unittest discover -s tests` (15 טסטים). **אחרי Synthesize**: `python -m scripts.orphan_review` (VERDICT אמור להיות 0 - ואכן 0 נכון ל-25.9).
- **גיבוי-DB** (`backup_database()` ב-`publish.py`) **דורס תמיד את קובץ-היום** מכאן ואילך (תוקן 25.9) - לא מדלג יותר בהרצה שנייה באותו יום.

## מצב הפרויקט הנוכחי

**חמשת שלבי ה-Pipeline יציבים, כולל הרצות אוטונומיות-לגמרי חוזרות ונשנות** (האחרונה: 24.9, שנייה ברציפות ללא סקירה-ויזואלית-אנושית, ניווטה בהצלחה שלוש אנומליות בו-זמנית ללא עצירה). הארכיון כולל **33 תאריכים** (2026-08-20 עד 2026-09-24) - **בשלוש שפות מלאות** (עברית/אנגלית/גרמנית), שני פורמטים (HTML+PDF). **האתר חי**: https://geopolitics.meirshemesh.com.

**מקורות MVP: 10** (כרשימה הקבועה ב-CLAUDE.md), אך **WSJ בסיכון-מעקב** (שחיתות יומיים רצופים, לא הושעה עדיין).

**הסשן הנוכחי (24-25.9) עסק בעיקר בתגובה לשתי ביקורות-איכות חיצוניות** על תוצרי הפרויקט (מומחה-חוץ ראשון: סקירה כללית; מומחה-חוץ שני: פוקוס על דוח 22.9 ספציפית) - כל טענה נבדקה מול ה-DB/המקור בפועל, לא התקבלה כמות שהיא. כמה ממצאים הובילו לתיקוני-קוד בפועל (ראו "מה הושלם"), אחרים הוכרעו במפורש כלא-לפעולה (scope-decision, ראו CLAUDE.md).

## מה הושלם (יכולות בשלות + תיקונים מהסשן הנוכחי)

- **ציטוטים גלויים ב-PDF** (`render.py`) - `@media print` שכופה את תיבת-מראי-המקום גלויה; התיקון הראשון נכשל בשקט (`.section-actions` אבי-האב היה `display:none` גורף) - נתפס רק ע"י בדיקת-טקסט-אמיתי ב-PDF.
- **שיוך-לכותב-בשם בשלב 2 של Synthesize** (`synthesize.py`+`db.py`) - מאמר עם מחבר-בשם משויך לאדם, לא למוסד; אומת מול 2 דוגמאות-בעייתיות-אמיתיות ויושם בפועל לדוח 23.9 החי (`scripts/fix_author_attribution_2026-09-23.py`).
- **מנגנון-גיבוי-DB** (`backup_database()`) - דורס-תמיד-קובץ-של-היום (במקום דילוג), מתועד ב-CLAUDE.md ובתיקיית-הגיבויים.
- **ניקוי-כפילויות בטוח** - 4 שורות נמחקו (`scripts/cleanup_duplicate_files_2026-09-25.py`) אחרי אימות-orphan-status לכל שורה; orphan_review ירד מ-25 ל-**0**.
- **`analyze.py` - שני תיקוני-scope חדשים, כל אחד עם רשת-ביטחון-קוד**: אכיפת-תיוג-עצמי (`filter_self_tagged_articles`) וזיהוי-עמוד-מכתבים (`is_letters_page`, WSJ+Süddeutsche). Backtest על 15 עמודים אמיתיים אימת ששבעה sections גאופוליטיים-אמיתיים שרדו (אחד אף גדל 8→10) בעוד שני מכתבי-רעש-ספציפיים נוקו לחלוטין.
- **תגובה מאומתת לביקורת-חוץ שנייה** - artifact פרטי פורסם (URL אצל המשתמש), כל טענה-ניתנת-לאימות נבדקה מול ה-DB/מקור בפועל; מספר תיקונים-מדויקים לביקורת (למשל ספירת-sections האמיתית - 63/51 לא 37/25).
- **הרצה אוטונומית מלאה לדוח 24.9** - WSJ פגום יומיים-רצופים (מופע-שני, לא-בודד), guard `_looks_suspicious` הופעל פעמיים והתאושש, analyze.py עיבד-בטעות קבצי-כפילות-ישנים (25 מאמרים כפולים נוצרו, לא פגעו בדוח 24.9 עצמו).

## קבצים שנוצרו או שונו (24-25.9)

- **`src/reporting/render.py`** - `.citations-popup[hidden]`/`.section-actions` תחת `@media print`.
- **`src/common/db.py`** - `get_articles_for_date()` שולף גם `a.author`.
- **`src/reporting/synthesize.py`** - `STAGE2_SYSTEM_PROMPT` (הנחיית-שיוך-לכותב).
- **`src/publishing/publish.py`** - `backup_database()` (דריסה-תמיד).
- **`src/analysis/analyze.py`** - `is_letters_page()`, `filter_self_tagged_articles()`, `_SELF_EXCLUSION_RE`, `LETTERS_PAGE_MARKERS`, הרחבת `SYSTEM_PROMPT`+`analyze_page()`+`run()`.
- **חדשים**: `scripts/fix_author_attribution_2026-09-23.py`, `scripts/cleanup_duplicate_files_2026-09-25.py`.
- **`PROJECT_LOG.md`** - סעיפים 4.52 עד 4.57 (כל התיקונים+ניסויים+ממצאים של הסשן הנוכחי), action items 49-55 מעודכנים.
- **`CLAUDE.md`** - תיאורי `analyze.py`/`synthesize.py`/`render.py` עודכנו, נוסף פסקת-scope-decision קבועה.
- כל 33 תאריכי הארכיון נבנו-מחדש (`render.py`+`publish.py`) בעקבות שינוי-המבנה ב-`render.py` (ציטוטים-ב-print), לפי הכלל הקבוע.

## החלטות שהתקבלו

ראו "החלטות ארכיטקטוניות קבועות" + "כללי עבודה קבועים" ב-CLAUDE.md - לא משוכפל כאן.

**החלטות ספציפיות-לסשן שלא שייכות ל-CLAUDE.md (זמניות/הקשריות)**:
- **scope-decision (`analyze.py`) אושר במפורש כמכוון** - לא לשנות בלי דיון נפרד (עבר ל-CLAUDE.md, ראו שם).
- **תיקוני 25.9 חלים קדימה-בלבד** - אין resynthesis רטרואקטיבי לדוחות-ישנים-ומפורסמים בעקבות אף אחד מהם (אכיפת-תיוג-עצמי, זיהוי-מכתבים, שיוך-מחברים) - עיקרון-אחיד שחוזר בכל תיקוני-הסשן.
- **WSJ לא הושעה** למרות שחיתות-יומיים-רצופים - החלטת-המשתמש המפורשת, ממתין למופע שלישי.
- **29.8/5.9 (כפילות-Economist-מפורסמת)** - לא נוגעים, ממתין להחלטה נפרדת (action item 55).
- **Die Welt לא מכוסה בזיהוי-עמוד-מכתבים** - הוחלט במפורש לתעד כפער-ידוע ולא לנחש חתימה-חלופית.

## עבודה שטרם הושלמה

1. **action item 54 (עדיפות גבוהה)** - זיהוי-שחיתות-כקוד-קבוע. הערכה: 2-2.5 שעות. לא בוצע.
2. **action item 52** - `analyze.py` זקוק למנגנון-הגנה-כללי-לפי-קובץ (כמו `screen.py --file`).
3. **action item 55** - החלטה על 29.8/5.9 (כפילות-כבר-מפורסמת).
4. **מנגנון-דילוג ב-`fetch_channel()`** (message_id/hash ידועים) - עדיין לא נבנה (פותר גם 52 חלקית).
5. **כלל-קבוע להרצות-בדיעבד שחוצות חצות** (action item 43) - עדיין לא הוחלט.
6. **שם הסרט התיעודי "NAZA"** (action item 40) - עדיין לא תוקן.
7. **תאריך המראי-מקום** (action item 41) - מציג תאריך-העלאה, לא תאריך-דוח - החלטת-בעלים פתוחה.
8. **תקרת שלב 1 של Synthesize** (action item 42) - לפני backlog-גדול לספור מאמרים ולהחליט על פיצול.
9. **2 עמודי WSJ Magazine (file 199, עמודים 39/63)** - עדיין `pending`, נכשלים בכל הרצת Analysis (שגיאת `'articles'`), לא נחקר שורש-הבעיה.
10. **גיבוי-חיצוני-אמיתי** (מעבר ל-OneDrive+המנגנון-החדש) - action item 48, לא דחוף.
11. **רעיון עתידי, לא לביצוע כרגע**: מדור-משני-קליל לחדשות-עובדתיות-מהותיות שנופלות מחוץ ל-scope (action item 51).

## בעיות ידועות (עדיין רלוונטיות - ראו PROJECT_LOG להיסטוריה המלאה)

- **"0 שורות" דו-משמעי** ב-`article_countries`/`article_conflict_zones` - מגבלה מודעת, לא באג.
- **אי-דטרמיניזם אמיתי בעמודים-צפופים-רב-פריטים ב-`analyze.py`** (נצפה 25.9): אותו עמוד החזיר 3 תת-קבוצות שונות ב-3 קריאות נפרדות, כולל תוצאה פגומה אחת (`stance_summary: "placeholder"`). רשת-הביטחון-הקוד (25.9) היא ביטוח חלקי לזה, לא פתרון-שורש.
- **Leserbriefe גרמניים-מקומיים-בלבד נשארים בדוח** (למשל בחירות/AfD) - המודל לא מתייג אותם כלא-רלוונטיים, כך שלא נתפסים ע"י אף אחד משני התיקונים החדשים. בעיה רחבה-יותר, לא בהיקף הסשן הנוכחי.
- **`published_at` נסחף מיום-הכיסוי** - לא משפיע על שיוך-דוחות, אך קובע `compute_scan_limit()` ותאריך-מראי-המקום.
- **תקלת "רוב-לא-משויך" בשלב 1** נצפתה 2 פעמים (18.9, 21.9) - **לא נצפתה ב-24.9** (98% כיסוי, ללא `--force`) - עדיין לא מספיק-דגימות לדפוס קבוע.

## הצעד הבא המומלץ

1. **להחליט אם לבנות את action item 54** (זיהוי-שחיתות-כקוד-קבוע, ~2-2.5 שעות) - עדיפות-גבוהה לפי המשתמש.
2. **לעקוב אחרי WSJ** - מופע שלישי של שחיתות ידרוש החלטת-השעיה.
3. **להחליט על action item 55** (29.8/5.9) - כרוך ב-resynthesis של דוח-ישן, סוג-החלטה נפרד.
4. שאר ה-action items הפתוחים הישנים (NAZA, תאריך-ציטוט, תקרת-Stage-1, חצות) - החלטות-בעלים קצרות, לא דחופות.

**תזכורת קבועה מ-CLAUDE.md**: כל שינוי עתידי במבנה/נתיבי-פלט של `render.py`/`publish.py` מחייב regeneration מלא של הארכיון בשלוש השפות + `git status` מפורש לפני commit.

## סשן אחרון

**תאריך:** 2026-09-24 עד 2026-09-25 (המשך ישיר מהרצת 24.9)

**תיאור:** סשן ארוך שהתחיל בתגובה לביקורת-איכות-חיצונית ראשונה (תוקנו: ציטוטים-ב-PDF, שיוך-מחברים - הוחל גם רטרואקטיבית ל-2 sections ב-23.9 באישור-diff מפורש), המשיך בהרצה-אוטונומית-מלאה-שנייה (24.9, ללא סקירה-אנושית, ניווטה 3 אנומליות בו-זמנית), ואז שלוש משימות-תשתית (גיבוי-דורס-תמיד, ניקוי-4-כפילויות-בטוח, תעדוף זיהוי-שחיתות). לאחר מכן התקבלה **ביקורת-חוץ שנייה, ממוקדת בדוח 22.9** - נבדקה לעומק (כולל קריאת-מקור-גולמי מלאה שחשפה שהתחקיר "Deportation Project" של ה-Washington Post קיים במקור אך הוחמץ ע"י Analysis, ולא בגלל scope-אחר אלא **אותה תופעה שכבר אופיינה יום קודם**) - תגובה מאומתת פורסמה כ-artifact. בעקבות הביקורת השנייה נבנתה תוכנית-משולבת דו-חלקית: ניסוי-מבוקר (הנחיית-"למה" בשלב 2 - **לא הצליח**, תיקוני-טרמינולוגיה-עברית - **הצליחו**) ובדיקה-זולה (תיוג-עצמי "non-geopolitical" - נמצא כתופעה חוזרת על-פני חודש שלם, לא מקרה-בודד). לבסוף נבנו-בפועל שני תיקוני-scope ל-`analyze.py` (אכיפת-תיוג-עצמי + זיהוי-עמוד-מכתבים), כל אחד עם רשת-ביטחון-קוד, ואומתו ב-backtest-15-עמודים לפני commit.

**דפוס חוזר בסשן**: כל תיקון נבדק תחילה כניסוי-לא-פוגע-בפייפליין-החי (ללא כתיבה ל-DB), הוצגו דוגמאות-לפני/אחרי קונקרטיות, ורק אחרי אישור מפורש בוצע בפועל - גם כשהתוצאה חיובית (backtest של 15/25.9) וגם כשהתוצאה שלילית (ניסוי-שכבת-ה"למה" שלא הראה אפקט). "commit אחרי אישור, לא לפני" נשמר לכל אורך הסשן, כולל תיקונים טכניים-לחלוטין (למשל דריסת-גיבוי) שהמשתמש הצהיר-מראש שלא דורשים סקירה.
