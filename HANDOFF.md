# Project Handoff

## מצב הפרויקט הנוכחי

**כל חמשת שלבי ה-Pipeline עובדים מקצה לקצה על נתונים אמיתיים, על עשרת מקורות ה-MVP המלאים**: Ingestion → Extraction → Analysis → Reporting (Synthesis+Render) → Publishing. הארכיון כולל **10 תאריכים** (2026-08-20 עד 2026-08-29) ב-`reports/{he,en}/` ובאותו מבנה תחת `docs/{he,en}/`. שלב ה-Publishing (`src/publishing/publish.py`) בונה `docs/` מתוך `reports/`, והאתר חי בפועל: https://meir-shemesh.github.io/geopolitics-tracker/ (כולל `docs/CNAME` לדומיין מותאם עתידי, `geopolitics.meirshemesh.com` - צד ה-GitHub Pages מוכן, רשומת ה-DNS ב-GoDaddy טרם אומתה). venv מוגדר ומותקן (`telethon`, `python-dotenv`, `pdfplumber`, `anthropic`, `weasyprint` + GTK3 Runtime).

**`synthesize.py` עבר תכנון-מחדש ארכיטקטוני מלא (2 שלבים: קיבוץ-לנושאים → השוואה-מקבילית-לכל-נושא, עם Prompt Caching)** - פתר בפועל את מגבלת-קנה-המידה שנחשפה ב-9+ מקורות (ראו PROJECT_LOG 4.23-4.24, 4.26). נבדק בשלוש הרצות אמיתיות ברצף: 27.8 (69 מאמרים, ~$0.50), 28.8 (257 מאמרים, 9 מקורות, ~$1.21 - פי 7-8 זול מהמנגנון הישן), ו-29.8 (185 מאמרים, 5 מקורות בפועל אחרי סינון-קבצים-פגומים, ~$0.71) - כל שלוש עם **0 כשלים, 0 הפעלות retry, 100% כיסוי**. יחס מאמרים/נושא ממשיך לעלות (1.41 → 2.45 → 3.25) - נתון שממשיכים לעקוב אחריו לאורך זמן, לא רק כבדיקה חד-פעמית.

**שלב ה-Analysis כולל תיוג גיאוגרפי/קונפליקטים מובנה** (`src/common/geo_taxonomy.py` + טבלאות `article_countries`/`article_conflict_zones`). **שלב ה-Publishing מייצר `manifest.json` סטטי** (`docs/assets/data/manifest.json` + עותק ב-`reports/`) עם תיוג גיאוגרפי/תאריך/אזור/קישור/קטגוריה ברמת section (426 sections, 84 מדינות, 3 קונפליקטים, 10 תאריכים). **דף בית אינטראקטיבי (v2)** ב-`docs/index.html` (+ `docs/{he,en}/index.html`) - מפת עולם SVG אמיתית ברוחב-מלא, ציר זמן וכרטיס-דוח-אחרון זה-לצד-זה, צ'יפי-סינון-לפי-אזור, פאנל תוצאות מסונן, כולם קוראים מ-`manifest.json` בזמן ריצה. הארכיון הכרונולוגי הישן ב-`archive.html` (5 מיקומים). `about.html` דו-לשוני **נכתב מחדש לגמרי** (2026-08-29 - לא תוספת, מסמך אחיד המשקף את מצב הפרויקט הנוכחי, כולל פרק חדש על היסטוריית-הרחבת-המקורות) - גופן אחיד (**Assistant**) בכל האתר כולל מסמך הרקע העצמאי (`scripts/build_background_doc.py`).

**`fetch.py`'s `guess_newspaper()` מזהה כעת רק מהדורת Economist Web Edition** (לא עוד המהדורות-האזוריות - EU/UK/Asia Pacific/Middle East and Africa - שהתבררו כפגומות/כפולות-תוכן, ראו PROJECT_LOG 4.26). **בדיקת-בריאות יזומה** (סריקת חתימת-שחיתות "tradingref.com") רצה עכשיו כשלב סטנדרטי לפני Screening, לא רק גילוי-בדיעבד. **הכל ב-git ופורסם** - עד ל-commit `37c4185` (ראו "סשן אחרון" לפירוט המלא של הסשן הנוכחי).

## מה הושלם

- **`src/common/db.py`** - מודול DB משותף לכל השלבים. סכמה מוצהרת פעם אחת ב-`TABLE_COLUMNS`/`TABLE_CONSTRAINTS`; `init_db()` יוצר טבלאות חסרות **וגם** מוסיף עמודות חסרות לטבלה קיימת אוטומטית (נבדק בפועל: DB ישן + עמודות חדשות בקוד → מיגרציה תקינה בלי איבוד נתונים).
- **`src/ingestion/fetch.py`** - מתחבר לערוץ טלגרם `demagazinesharing`, מוריד רק PDF-ים שמזוהים כאחד מ-4 עיתוני ה-MVP (`guess_newspaper`, כולל טיפול מיוחד שלא לתפוס "Welt am Sonntag" בטעות תחת "Die Welt"), עוקב אחרי קבצים שכבר ירדו ב-`downloaded_files`.
- **`src/extraction/extract.py`** - מחלץ טקסט גולמי לעמוד עם pdfplumber ל-`extracted_pages` + עותק `.txt` תחת `data/processed/extracted/`. כשל בפתיחת קובץ שלם = `extraction_status='failed'`; כשל בעמוד בודד בתוך קובץ שנפתח בהצלחה נרשם כטקסט `[EXTRACTION FAILED]` לאותו עמוד בלבד, לא מפיל את הקובץ.
- **`src/analysis/screen.py`** (שלב א', Claude Haiku) - סינון רחב מכוון: בספק מסמן רלוונטי. מדלג על עמודים ריקים (בלי לבזבז קריאת API) אחרי שהתגלה שזה גורם ל-400 מה-API.
- **`src/analysis/analyze.py`** (שלב ב', Claude Sonnet + adaptive thinking) - מזהה מאמרים בעמודים שסומנו רלוונטיים, מחלץ כותרת/מחבר/נושא/עמדה/ציטוט. הפרומפט מכיל הנחיה מפורשת נגד "דליפת" טקסט לשדה `author` (להשאיר ריק בספק), ונגד כלילת תוכן פנים-בלבד ללא זווית בינלאומית.
- הרצה מלאה על כל 17 הקבצים - ראו טבלת תוצאות למטה.

**תוצאות ריצה מלאה (795 עמודים, 4 עיתונים):**

| עיתון | עמודים | רלוונטים | % | מאמרים |
|---|---|---|---|---|
| Süddeutsche Zeitung | 159 | 69 | 43% | 99 |
| Die Welt | 48 | 32 | 67% | 46 |
| Daily Telegraph | 152 | 76 | 50% | 83 |
| Guardian | 436 | 131 | 30% | 80 |
| **סה"כ** | **795** | **308** | **39%** | **308** |

- **`src/reporting/synthesize.py`** (שלב א' של Reporting, Claude Sonnet) - קריאה בודדת ליום שלם: מקבצת מאמרים לפי נושא אמיתי (לא `region_topic` הגולמי), ממזגת מאמרי טיזר/כתבה-מלאה מאותו עיתון לקול אחד בהשוואה (תוך שמירת כל ה-`article_ids` המקוריים לצורך קרדיט - זו הפתרון בפועל לדה-דופליקציה שתוכננה לשלב הזה), כותבת השוואה בין-מקורית דו-לשונית בבת אחת, ומקצה קטגוריה (8 ערכים קבועים) לכל סקשן. כולל שתי שכבות רשת-ביטחון גנריות: fallback section דטרמיניסטי לכל מאמר שלא שובץ (בלי קריאת API נוספת), ו-retry אוטומטי חד-פעמי אם יחס ה-fallback עולה על 20% מאמרי היום. הפרומפט אוסר במפורש דליפת מספרי `article_id` לפרוזה, ומחייב שמות עיתונים בכתיב הלטיני המקורי בלבד (`NEWSPAPER_DISPLAY_NAMES`) + עברית תקנית מבחינת תיאום מגדרי - שני באגים אמיתיים שהתגלו ותוקנו בפרומפט (ראו "בעיות ידועות").
- **`src/reporting/render.py`** (שלב ב' של Reporting) - הופך שורות `report_sections` ל-4 קבצים (HTML+PDF × עברית+אנגלית). מסמך HTML אחד לכל שפה משמש גם כקובץ ה-`.html` (דפדפן, `@media screen`) וגם כקלט ל-WeasyPrint ל-`.pdf` (`@media print`) - בלי כפל תבניות. קידוד צבעוני לפי `category` (9 ערכים: 8 קטגוריות + `additional_coverage` שמור ל-fallback), עם fallback sections מוצגים במדור נפרד בתחתית הדוח ("כיסוי נוסף"). גופן Heebo (variable font, קובץ יחיד לכל המשקלים) מוטמע מקומית - לא Google Fonts חי - כדי שגם ה-PDF (build-time) וגם האתר המתפרסם לא יהיו תלויים ברשת חיצונית.
- **אומת בפועל**: WeasyPrint דורש GTK3 Runtime ברמת מערכת הפעלה (לא רק pip) - חסר במחשב זה, הותקן ידנית (`winget install tschoonj.GTKForWindows`), ואומת שעובד (v69.0). לפני ההתקנה, `render.py` זיהה את החוסר אוטומטית ודילג רק על שלב ה-PDF, בלי לקרוס.
- **הרצה מלאה ראשונה על תאריך אמיתי (2026-08-24, 81 מאמרים)**: `synthesize.py` → 34 sections, 0 fallback; `render.py` → 4 קבצים (14 עמודים ב-PDF העברי). נבדק בקפידה מול ה-PDF בפועל (לא רק ה-DB): נמצאו ותוקנו שני באגי איכות תוכן (שמות עיתונים משובשים/מעורבבי-כתב, תיאום מגדרי שגוי בעברית) - ראו "בעיות ידועות".
- **הורחב לכל 5 התאריכים הקיימים ב-DB (2026-08-26)**: הורץ `synthesize.py`+`render.py` על כל תאריך עם לפחות מאמר מנותח אחד - 20 קבצים בסה"כ (5 תאריכים × 4). 0 כשלים, 0 הפעלות retry (יחס ה-fallback נשאר תמיד מתחת לסף 20%). תוצאות: 2026-08-20 (4 מקורות, 29+1), 2026-08-21 (4 מקורות, 39+0), 2026-08-22 (3 מקורות - Die Welt חסר ליום זה, 38+1), 2026-08-23 (מקור בודד - Daily Telegraph, 14+2), 2026-08-24 (4 מקורות, 34+0).
- **פס ניווט ב-`render.py`** - "לכל הדוחות" (קישור לארכיון) + החלפת שפה, בראש כל דוח. מוצג ב-HTML, מוסתר במפורש ב-PDF (`@media print { .top-nav { display: none; } }`) אחרי הכרעה דו-צדדית: קישורי-ניווט יחסיים עובדים באתר חי אבל נשברים באופן בלתי-נמנע כש-PDF "זז" ממקומו (הורדה/מייל/הדפסה) - ראו "בעיות ידועות"/PROJECT_LOG 4.12. חיבור עתידי ל-PDF (footer עם קישור מוחלט) ממתין להחלטת כתובת אתר סופית.
- **`src/publishing/publish.py`** (שלב Publishing, חדש) - בונה `docs/` (מיקום GitHub Pages): מעתיק קבצי `report_*.html/pdf` וגופן Heebo מ-`reports/` ל-`docs/`, בונה עמוד ארכיון כרונולוגי (`index.html`) לכל שפה (גם עותק ב-`reports/{he,en}/` לנוחות מקומית), ובונה עותק-שורש נפרד (`docs/index.html`, לא העתק בייטים - נתיבים מותאמים) כדי שהכתובת הבסיסית תיפתח כברירת מחדל לעברית. בלי `--force` (כמו `render.py` - זול, read-only, תמיד דורס).
- **הריפו הוגדר כציבורי** - נדרש כדי לאפשר GitHub Pages בחשבון חינמי; ללא סיכון, כל סוד תמיד הוזן ידנית ל-`.env` בלבד ומעולם לא נכנס ל-git.
- **האתר חי ואומת בפועל בדפדפן**: https://meir-shemesh.github.io/geopolitics-tracker/ - כתובת בסיסית פותחת לעברית כברירת מחדל, עמודי ארכיון בשתי השפות, ניווט דוח↔ארכיון ובחזרה, החלפת שפה, HTTPS תקין (אוטומטי מ-GitHub Pages).
- **הורחב לתאריכים 2026-08-25/26 (2026-08-26)**: הורצו כל 5 שלבי ה-Pipeline (Ingestion→Extraction→Analysis→Reporting→Publishing) על שני תאריכים חדשים. 8 קבצי PDF נמשכו, 244 עמודים חולצו, 118 סומנו רלוונטיים (48%), 129 מאמרים זוהו, 78 sections נוצרו (45 ל-25.8 כולל 2 fallback, 33 ל-26.8 בלי fallback). 0 כשלים ב-Reporting/Publishing, 0 הפעלות retry. שני עמודים נוספים נכשלו ב-Analysis עם `KeyError: 'articles'` - אובחנו ישירות מול ה-API (`stop_reason=refusal`, `category=general_harms`), תואמים בדיוק לדפוס המתועד (עמוד "Court & Social" עם נקרולוגים ב-Telegraph, עמוד ביקורת-תקשורת ב-SZ שדן בפרטי רדיקליזציה של מבצע פיגוע טרור אמיתי) - תוקנו ידנית ל-`is_relevant=0` כמו במקרים הקודמים. הארכיון גדל מ-5 ל-**7 תאריכים** (28 קבצים).
- **`docs/CNAME`** נוסף (`geopolitics.meirshemesh.com`) - צד GitHub Pages מוכן לדומיין מותאם; DNS ב-GoDaddy טרם הוגדר/אומת.
- **מסמך רקע/מותג עצמאי (2026-08-27) - חדש, מחוץ ל-Pipeline:** `scripts/build_background_doc.py` מפיק PDF+HTML חד-פעמי (geopolitics-tracker-background-doc) על בסיס תוכן מאושר מ-PDF רפרנס חיצוני, באותה שיטת רינדור כמו `render.py` (WeasyPrint, פונט variable מוטמע מקומית, פלטת הצבעים הבסיסית מ-`render.py`: `--bg`/`--text`/`--masthead-accent`) כדי שהמסמך ייראה כחלק מאותה משפחת מותג כמו האתר. כותרת עם לוגו (`scripts/assets/MS_Logo.png`, 76px) + שורת זיהוי אחת ("**מאיר שמש** - ייעוץ וניהול טכנולוגיות", שם מודגש/כהה מול תפקיד מעומעם, `direction:rtl` מפורש כדי שסדר הקריאה יהיה נכון). תוקנו כמה ביטויים לטיניים/מעורבי-שפה שהתגלו הפוכים בטקסט שחולץ מה-PDF המקורי (למשל "Claude API", "PDF + HTML אינטראקטיבי", הדומיין) - זוהה כאותו כשל bidi/extraction ידוע בפרויקט (ראו "בעיות ידועות" למטה), תוקן לפי הניסוח הקנוני הקיים כבר ב-CLAUDE.md/PROJECT_LOG.md, לא ניסוח מחדש של התוכן. **הפונט עבר שתי איטרציות אחרי משוב ויזואלי**: Heebo (זהה לאתר) → Frank Ruhl Libre (אלגנטי יותר, אך "מיושן" לדעת המשתמש) → **Assistant** (הבחירה הסופית - מודרני, נקי, variable font, מתאים למסך). כל הגופנים הורדו מ-Google Fonts (OFL) ל-`scripts/assets/`; קבצי הפונט שנדחו הוסרו שוב לפני commit. אומת ויזואלית (רינדור ל-PNG) אחרי כל שינוי לפני אישור סופי - 3 עמודים בפועל מול 2 ברפרנס (הבדל ריווח-שורות בין הפונטים בלבד, לא תוקן בכוונה). תוצרי ה-build (`scripts/output/*.{html,pdf}`) לא נכנסים ל-git - נבנים מחדש מהסקריפט.
- **`manifest.json` - מקור נתונים סטטי לדף בית עתידי (2026-08-27) - חדש:** שלב Publishing מייצר כעת קובץ JSON קומפקטי (`docs/assets/data/manifest.json` + עותק ב-`reports/assets/data/`, אותה מוסכמת עותק-כפול כמו גופן Heebo) עם תיוג גיאוגרפי/תאריך/קישור-עומק **ברמת section** (לא ברמת article) - הכנה לדף בית עם 3 מודולים (מפה, ציר זמן, כניסה לדוח אחרון) עם סינון inline. שלושה שינויים: (1) `render.py` - כל `<section class="topic-section">` מקבל `id="section-{id}"` (גם main וגם fallback), מאפשר קישור-עומק לנושא ספציפי בתוך דוח; (2) `db.py` - פונקציה חדשה `get_geo_tags_for_section(conn, section_id)` שמחזירה איחוד (union) מדינות/קונפליקטים של כל המאמרים המקושרים ל-section דרך `report_section_articles`; (3) `publish.py` - `build_manifest(conn, entries)` חדש, קורא ל-`run()` הקיים (לא סקריפט נפרד), `sections` הוא מקור האמת המלא (נושא/קטגוריה/מקורות/גיאו/קישורים), `countries`/`conflict_zones`/`dates` הם **רק אינדקסים** של `section_ids` (רק ערכים לא-ריקים - לא כל 94 המדינות תמיד), בלי `comparison_text` בכלל (לשמור על גודל קובץ קטן). נבדק בפועל: `python -m src.publishing.publish` על כל 7 התאריכים הקיימים → **236 sections, 66 מדינות, 3 קונפליקטים, 7 תאריכים, 107KB**. `href_he`/`href_en` הם נתיבים יחסיים מתוך `docs/` עצמו בלבד (`he/report_<date>_he.html#section-<id>`) - **בלי הנחה על מיקום דף הבית העתידי** (זו החלטה שטרם התקבלה, לפי דרישת המשתמש במפורש), התאמת היחסיות תיעשה ב-JS כשהמיקום ייקבע. בניית ה-UI עצמו (HTML/JS של דף הבית) היא שלב נפרד עתידי, טרם התחיל.
- **`about.html` דו-לשוני + גרסה אנגלית למסמך הרקע (2026-08-27) - חדש:** `docs/{he,en}/about.html` (+ עותק ב-`reports/`) - עמוד אתר רגיל (Heebo, top-nav/masthead משותפים עם כל שאר האתר, לא letterhead עצמאי כמו מסמך הרקע), עם כרטיס "אודות הכותב" (לוגו + שם + תפקיד) בתחתית העמוד. תוכן משותף (עברית מתוקנת + אנגלית) עבר למודול חדש `src/common/about_content.py` - מקור אמת יחיד שגם `about.html` וגם `scripts/build_background_doc.py` (שקיבל עכשיו גרסה אנגלית מלאה, `geopolitics-tracker-background-doc-en.{html,pdf}`, זהה עיצובית ל-100% לעברית - `dir`/`direction`/`text-align` בלבד משתנים) קוראים ממנו, כדי שתיקון ניסוח (כמו "צינור"→"תהליך עבודה" שקרה כאן) לא יצטרך להתבצע בשני מקומות. `render.py`'s `build_nav_html` עבר רפקטור קטן (מקבל `back_href`/`other_lang_href` מפורשים במקום לגזור אותם מ-`report_date`) כדי ש-`about.html` יוכל לעשות בו שימוש חוזר.
- **דף בית אינטראקטיבי + מעבר ארכיון ל-`archive.html` (2026-08-27) - חדש, התוסף הגדול ביותר של הסשן:** `build_homepage_html()` חדש ב-`publish.py`, נכתב ל-`index.html` בשלושה מיקומים (`docs/` שורש, `docs/he/`, `docs/en/`). שלושה מודולים בגריד שווה, כולם קוראים מ-`manifest.json` **בזמן ריצה** (fetch דרך JS, לא embedded ב-build - כדי שאותו manifest ישרת גם עמודים עתידיים): (1) **מפה** - קובץ SVG אמיתי של מפת עולם (`src/publishing/assets/map/world.svg`, Public Domain מ-Wikimedia Commons, כל מדינה `<g id="xx">` בקוד ISO 3166-1 alpha-2, אומת מול `COUNTRY_LIST` - 0 קודים חסרים) מוטמע **inline** בתוך העמוד (לא `<img>`, כדי לאפשר גישת JS לצביעה/קליק על מדינה בודדת), עם עותק גם כקובץ נכס עצמאי ב-`assets/map/`; (2) **ציר זמן** - שורה גוללת אופקית, תא לכל תאריך ב-manifest, עוצמת צבע לפי מספר sections, קישור "לארכיון המלא" צמוד; (3) **לפי טקסט** - כרטיס לדוח האחרון. קליק על מדינה או תאריך מסנן פאנל תוצאות משותף (v1: single-select, לא AND משולב - ראו PROJECT_LOG action item). `category_css()` (שונה משם מ-`_category_css` הפרטי ב-`render.py`) משוכפל לשימוש בדף הבית כדי שתגיות הקטגוריה בפאנל התוצאות יזהו בדיוק כמו בדוח; `manifest.json` קיבל מפתח חדש `categories` (שמות תצוגה) כדי שה-JS לא יצטרך לשכפל את הרשימה. **פתרון בפועל לשאלת יחסיות ה-href שנדחתה קודם**: כל אחד מ-3 עותקי דף הבית "יודע" את העומק שלו (`asset_prefix`: `""`/`"../"`), מוזרק כקבוע JS זעיר. **אומת בדפדפן אמיתי** (Playwright headless Chromium דרך `npx`, כי WeasyPrint לא מריץ JS) בעברית, אנגלית, ומסך צר (390px) - תפס ותיקן שני באגים אמיתיים: CSS Grid blowout (ה-SVG המוטמע דחף את העמוד לרוחב מעבר ל-viewport במסך צר, תוקן עם `minmax(0, 1fr)`), ותא ציר-הזמן הנבחר שלא נגלל לתצוגה במסך צר (תוקן עם `scrollIntoView`). **תופעה חוזרת שהתגלתה בשלישית**: הארכיון הישן (`report_*.html`) המשיך להצביע על `index.html` הישן עד שהורץ מחדש `render.py` על כל 7 התאריכים - הביא להוספת כלל קבוע חדש ב-CLAUDE.md (ראו "החלטות שהתקבלו").
- **תיוג גיאוגרפי/קונפליקטים מובנה ב-Analysis (2026-08-27) - חדש:** `src/common/geo_taxonomy.py` מגדיר רשימה סגורה של 94 מדינות (`COUNTRY_LIST`, קוד ISO + שם עברי/אנגלי), מיפוי כל מדינה ל-1 מתוך 8 אזורים קבועים (`COUNTRY_TO_REGION`: north_america/south_america/europe/middle_east/eurasia/far_east/oceania/africa), ו-3 תגיות קונפליקט סגורות (`CONFLICT_ZONE_LABELS`: israel_palestine_conflict/iran_west_conflict/russia_ukraine_conflict). שתי טבלאות many-to-many חדשות ב-`db.py` (`article_countries`, `article_conflict_zones`, עם `UNIQUE(article_id, code)`). `analyze.py` מתייג `country_codes`/`conflict_zones` **באותה קריאת API הקיימת** ל-Sonnet (בלי קריאה נוספת) - `articles.region_topic` נשאר ללא שינוי, זו תוספת בלבד. `insert_article()` שונה מ-`-> None` ל-`-> int` (מחזיר `lastrowid`) כדי לאפשר קישור תגיות המדינה למאמר החדש.
- **`src/analysis/geo_tag_backfill.py` (חדש) - backfill למאמרים קיימים:** סקריפט עצמאי ורב-שימוש (לא חד-פעמי) שמתייג מאמרים שנותחו *לפני* שהגיאו-תיוג נוסף, בקריאות Haiku זולות בבאצ'ים של 25 (קורא רק `headline`/`region_topic`/`stance_summary` כבר-קיימים - לא טקסט עמוד גולמי, לא קריאה חוזרת ל-Sonnet). Idempotent מטבעו דרך `get_articles_without_geo_tags` (LEFT JOIN + WHERE NULL, כמו תבניות ה"תור ממתין" הקיימות בפרויקט) - הרצה חלקית/נכשלת בטוחה לגמרי להרצה חוזרת. נתמך `--date` להרצת בדיקה על תאריך יחיד לפני ריצה מלאה. **תוצאות בפועל**: בדיקת תאריך יחיד (2026-08-23, 19 מאמרים) - 19/19 תויגו, נבדקו ידנית לאיכות (תיוג רב-מדינתי נכון, שמרנות נכונה בתיוג קונפליקט). הרצה מלאה (שאר 6 התאריכים, 419 מאמרים שהיו "ממתינים") - 17 באצ'ים, 0 כשלים, 418/419 תויגו בריצה אחת (מאמר `id=67` הוחמץ פעם אחת, עצמו-מרפא בהרצה הבאה). **מצב סופי ב-DB**: 437 מאמרים סה"כ, 418 עם תיוג גיאוגרפי אמיתי, 19 ללא תיוג כלל - כל ה-19 נבדקו ידנית ואומתו כ**חסרי תיוג כדין** (נושאים גלובליים/על-לאומיים ללא מדינה בודדת מהרשימה הסגורה, למשל מדיניות הגירה של ה-EU, שוקי אג"ח גלובליים, ממשל FIFA - לא באג). ראו "בעיות ידועות" למגבלת העיצוב שגילוי זה חושף.
- `.env`/`.env.example` (`TELEGRAM_API_ID`, `TELEGRAM_API_HASH`, `ANTHROPIC_API_KEY`) - `.env` מאומת כלא-נכנס ל-git.
- תוקן באג בשם ה-frontmatter של סקיל `resume-project` (היה `name: Resume Project` עם רווח, לא תואם לשם התיקייה - מנע הפעלה כ-`/resume-project`; תוקן ל-`resume-project`).
- **הורחבו סקילי ההמשכיות:** `resume-project` מבצע כעת `git pull` כצעד ראשון (עוצר אם יש קונפליקט/שינויים לא-committed) וקורא גם את `PROJECT_LOG.md` אם קיים. `handoff` מעדכן כעת גם `PROJECT_LOG.md` (מותנה - רק כשיש החלטה ארכיטקטונית/ממצא מדיד/שינוי אסטרטגי ב-action items), ובסיומו מבצע רצף git חצי-אוטומטי: `git add .` אוטומטי, בדיקת קבצים חשודים (PDF/session/.env), הצעת הודעת commit, ו-commit+push רק אחרי אישור מפורש.
- **הרחבת מקורות ה-MVP מ-4 ל-10 - שלב א' (זיהוי-קבצים) + שלב ב' (Extraction), הושלמו ונבדקו (2026-08-28):** `guess_newspaper()` ב-`fetch.py` מזהה כעת 6 מקורות חדשים (NYT International, WSJ, LA Times, USA Today, Economist, Der Spiegel), כולל הבחנה מפורשת בין מהדורות דומות-שם (NYT International מול NYT הביתי; LA Times מול Times UK) ותיקון-בדרך: נורמליזציה של `_`/`-` לרווח לפני השוואה (אותה מלכודת שכבר הייתה קיימת סמויה בזיהוי SZ הישן). **אומת בפועל מול הערוץ החי** (לא רק דוגמאות סינתטיות) - 21 קבצים חדשים ירדו נכון, כולל כל התבניות שהמשתמש ציין מראש (NYT International/Economist בכל התבניות/LA Times באורך-מלא ובקיצור). Extraction רץ על כל 26 הקבצים הממתינים (1265 עמוד), 0 כשלים - השווה מבנה-פריסה בין המקורות: ערבוב-סדר-קריאה הידוע חוזר ב-NYT International (עמוד שער בלבד), אך התגלה גם **ממצא נפרד**: הדבקת-מילים-בלי-רווחים ב-WSJ/USA Today (קופסאות-תקציר צפופות בלבד, לא הטקסט הרגיל) - תועד כשני ממצאים נבדלים, לא תופעה אחת.
- **סריקה שיטתית חשפה 6 קבצים פגומים (image-only, "tradingref.com" בלבד) מתוך 55 - 11% (2026-08-28):** לא 2 מקרים בודדים כפי שנראה תחילה, אלא דפוס חוזר: NYT International נפגע ב-3 מתוך 4 קבצים (75%!), Guardian UK נפגע ב-2 קבצים, USA Today Sports Weekly בקובץ אחד. כל הקבצים "ירדו בהצלחה" ו"חולצו בהצלחה" טכנית (0 חריגות קוד) - רק Screening תפס נכון שאין תוכן. ראו PROJECT_LOG action item מודגש.
- **Screening (2026-08-28) - הושלם על כל 1265 העמודים, 45% רלוונטי:** דגימה ייעודית מהמקורות החדשים (בדגש Economist/WSJ/USA-Today/LA-Times כפי שהמשתמש ביקש) אושרה כתקינה - הרשת-הרחבה עובדת כמתוכנן, כולל מעבר תוכן-פנים-אמריקאי לשלב הבא (זה בכוונה - הסינון האמיתי קורה ב-Analysis).
- **Analysis (2026-08-28) - הושלם על 530 עמודים רלוונטיים, 358 מאמרים חדשים:** נתפס ותוקן כשל אמיתי חדש (`TypeError` על תגובת-API מעורערת - `articles` הוחזר כמחרוזת ולא רשימה; לא השחזר בבדיקה חוזרת, כנראה תקלה חד-פעמית) - נבנתה הגנה גנרית (בדיקת-תקינות לפני כתיבה ל-DB, לא רק try/except סביב קריאת ה-API) כדי שכשל-מבנה לא ישאיר רשומות-כפולות בהרצה חוזרת. **יחס מאמרים/עמודים-רלוונטיים לפי מקור אושר** (בדיוק כמו שהמשתמש ביקש לבדוק): WSJ 1.02 (תואם מקורות ותיקים), USA Today 0.50 ו-LA Times 0.60 (נמוך יותר, תואם את הציפייה לדחיית-תוכן-פנים-אמריקאי - אך גם Guardian/SZ באותו יום הראו יחסים דומים, כך שזו לא חריגה דרמטית).
- **⚠️ synthesize.py על 9 מקורות (2026-08-28) - נמצאה מגבלת-קנה-מידה אמיתית, לא "עוד באג":** ראו PROJECT_LOG 4.23 לפירוט המלא. תוקנו 3 בעיות אמיתיות (בדיקת-תקינות לפני `tool_use.input["sections"]`, `max_tokens` 16000→32000 + מעבר ל-streaming שנדרש ע"י ה-SDK, ותיקון-קידוד-קונסולה שהתגלה כרביעי מתוך 5 סקריפטים חשופים). למרות זאת, **5/5 ניסיונות דרשו retry**, עם תוצאות בין 21% fallback (מצוין) ל-91-100% (גרוע) על **אותו קלט בדיוק** + שתי קריסות-רשת נפרדות. **תוצאה: 2026-08-28 הסתיים בלי דוח כלל** - הוחלט במפורש לא לנסות שוב הלילה, ולטפל בזה כמגבלה ארכיטקטונית שדורשת סשן-תכנון ייעודי (chunking), לא תיקון-מהיר נוסף.
- **✅ `synthesize.py` נבנה מחדש כארכיטקטורת-שני-שלבים + Prompt Caching (סשן נפרד, 2026-08-28) - פותר את המגבלה הנ"ל בפועל:** ראו PROJECT_LOG 4.24 לפירוט המלא (עובדות שנמדדו לפני כתיבת קוד: rate limits, סף-streaming המדויק של ה-SDK, סף-cache מינימלי). `synthesize_day()` הישן נמחק לגמרי (נשאר נגיש בהיסטוריית git). נבדק על 27.8 (69 מאמרים, ~$0.50) ואז על קנה-המידה האמיתי, 28.8 (257 מאמרים, 9 מקורות) - **הצלחה מלאה בניסיון ראשון: 0 כשלים, 0 fallback, 0 retry, ~$1.21** (פי 7-8 זול מהערכה הקודמת). דוח 2026-08-28 קיים ופורסם. נשמר ב-git (`f7a1b30`, אחרי `8a9268d`/`945d300`/`c564af8` - חיזוקי-אמינות ל-Analysis/Reporting + תיקון-קידוד-קונסולה + הרחבת-זיהוי-מקורות).
- **✅ הרצה מלאה שלישית וחדשה על 2026-08-29 - כולל בדיקת-בריאות יזומה חדשה, שני באגים חדשים שנמצאו ותוקנו, ופתרון שאלת ריבוי-המהדורות של Economist:** ראו PROJECT_LOG 4.26 לפירוט המלא. תמצית: (1) בדיקת-בריאות **יזומה** (סריקת "tradingref.com") לפני Screening תפסה 5/13 קבצים פגומים לפני תשלום עליהם - 4 מהדורות-Economist-אזוריות + NYT International (הפעם ה-4/4, 100%). (2) חקירת-תוכן (לא רק hash) פתרה את שאלת ריבוי-המהדורות של Economist - `guess_newspaper()` צומצם לזהות רק את ה-Web Edition. (3) עלות Screening+Analysis נמדדה בדיעבד באמצעות שחזור-מדויק של `count_tokens` (טכניקה חדשה - `screen.py`/`analyze.py` לא שומרים `response.usage`): Screening $1.83 (מדויק), Analysis $3.17 (**גבול תחתון** - טוקני adaptive-thinking אבודים לצמיתות). (4) שני באגים חדשים: `published_at` שגוי (הפעם הרביעית, ראו action item 29 ב-PROJECT_LOG) ו-`article_id` כפול בתוך נושא בודד ב-`synthesize.py` (קרס עם UNIQUE constraint, תוקן עם דה-דופ). תוצאה סופית: 57 נושאים/185 מאמרים, 100% כיסוי, 0 fallback, יחס 3.25 מאמרים/נושא (ממשיך לעלות: 1.41→2.45→3.25). נשמר ב-git (`6fa16d1`).
- **✅ `src/common/about_content.py` נכתב מחדש לגמרי (2026-08-29) - לא תוספת, מסמך אחיד ומעודכן:** תוכן עברית+אנגלית מאושר במלואו מראש, כולל פרק חדש ("המקורות: מארבעה לעשרה") שמתעד את היסטוריית-הרחבת-המקורות, רענון נתוני "מה קיים היום" (7→9+ ימים, 4→10 מקורות), החלפת התיאור המיושן של Synthesize (קריאה-בודדת) בתיאור הארכיטקטורה החדשה, והסרת ניסוחי "חדש"/"הבא" שכבר לא רלוונטיים (המפה/דף-הבית כבר חיים). נבנה מחדש (`build_background_doc.py` + `publish.py`) ואומת ויזואלית (רינדור-לתמונה) בשתי השפות, גם המסמך העצמאי וגם `about.html`. נשמר ב-git (`37c4185`).

## קבצים שנוצרו או שונו

- `src/common/__init__.py`, `src/common/db.py` (חדשים - db.py הועבר מ-`src/ingestion/`; הורחב שוב בהמשך עם טבלאות/פונקציות ה-Reporting - ראו למטה)
- `src/ingestion/__init__.py`, `src/ingestion/fetch.py` (import עודכן אחרי ה-refactor)
- `src/extraction/__init__.py`, `src/extraction/extract.py`
- `src/analysis/__init__.py`, `src/analysis/screen.py`, `src/analysis/analyze.py`
- `.env`, `.env.example`, `.gitignore` (תוספות: `*.session`, `data/processed/tracker.db`, `data/processed/extracted/`), `requirements.txt` (נוסף `python-dotenv`)
- `.claude/skills/resume-project/SKILL.md` (תיקון `name:` ב-frontmatter; בהמשך גם git pull + קריאת PROJECT_LOG.md)
- `.claude/skills/handoff/SKILL.md` (עדכון PROJECT_LOG.md מותנה + רצף git חצי-אוטומטי)
- `CLAUDE.md`, `HANDOFF.md`, `PROJECT_LOG.md` (עדכון סגירת סשן)
- `venv/` (נוצר, לא נכנס ל-git); `data/raw/demagazinesharing/*.pdf` (17 קבצים, לא נכנס ל-git); `data/processed/tracker.db` ו-`data/processed/extracted/*.txt` (לא נכנס ל-git)
- `src/reporting/__init__.py`, `src/reporting/synthesize.py`, `src/reporting/render.py` (חדשים) - הרחבת `db.py` עבורם: טבלאות `reports`/`report_sections`/`report_section_articles`, עמודת `category` על `report_sections`, ופונקציות גישה חדשות (`get_articles_for_date`, `get_sources_for_date`, `report_exists`, `delete_report`, `insert_report`, `insert_report_section`, `link_section_article`, `get_report`, `get_report_sections_for_date`, `get_section_articles`)
- `src/reporting/assets/fonts/Heebo-Variable.ttf` (חדש - גופן Heebo, קובץ variable font יחיד, רישיון OFL, ~120KB)
- `reports/assets/fonts/Heebo-Variable.ttf` (חדש - עותק מועתק אוטומטית ע"י `render.py` לשימוש הדפים המתפרסמים)
- `reports/he/report_<date>_he.{html,pdf}`, `reports/en/report_<date>_en.{html,pdf}` עבור `<date>` ∈ {2026-08-20, 2026-08-21, 2026-08-22, 2026-08-23, 2026-08-24} (חדשים - 20 קבצים בסה"כ, כל 5 התאריכים הקיימים ב-DB)
- `src/publishing/__init__.py`, `src/publishing/publish.py` (חדשים) - הרחבת `db.py`: פונקציה `get_all_reports`
- `reports/he/index.html`, `reports/en/index.html` (חדשים - עמוד ארכיון לכל שפה, לנוחות מקומית)
- `docs/` (חדש, כולו) - `docs/index.html` (שורש, עברית ברירת מחדל), `docs/he/`, `docs/en/` (כל קבצי הדוחות + `index.html`), `docs/assets/fonts/Heebo-Variable.ttf`, `docs/CNAME` (דומיין מותאם) - זהו מיקום ה-GitHub Pages בפועל
- `reports/he/report_2026-08-25_he.{html,pdf}`, `reports/en/report_2026-08-25_en.{html,pdf}`, ואותם קבצים ל-2026-08-26 (חדשים), + עותקיהם המתאימים תחת `docs/{he,en}/`
- `scripts/build_background_doc.py`, `scripts/assets/MS_Logo.png` (חדשים) - מסמך רקע/מותג עצמאי, מחוץ ל-Pipeline; `scripts/output/` נבנה ע"י הסקריפט, לא נכנס ל-git (`.gitignore` עודכן: `scripts/output/*.pdf`, `*.html`)
- `scripts/assets/Assistant-Variable.ttf`, `scripts/assets/Assistant-OFL.txt` (חדשים) - הגופן הסופי של מסמך הרקע (הוחלף פעמיים - ראו "מה הושלם"); קבצי Frank Ruhl Libre שהורדו ונדחו הוסרו לפני commit, לא נכנסו לגיט
- `src/common/geo_taxonomy.py` (חדש) - `COUNTRY_LIST`, `COUNTRY_TO_REGION`, `CONFLICT_ZONE_LABELS`, `country_codes()`, `country_list_prompt_text()`
- `src/analysis/geo_tag_backfill.py` (חדש) - סקריפט backfill עצמאי לתיוג מאמרים קיימים
- `src/common/db.py` (שונה) - טבלאות חדשות `article_countries`/`article_conflict_zones` + `TABLE_CONSTRAINTS` תואם; `insert_article()` משתנה מ-`-> None` ל-`-> int`; פונקציות חדשות `insert_article_countries`, `insert_article_conflict_zones`, `get_countries_for_article`, `get_conflict_zones_for_article`, `get_articles_without_geo_tags`, `get_geo_tags_for_section` (חדש - union גיאו-תיוג לכל מאמרי section)
- `src/analysis/analyze.py` (שונה) - `SYSTEM_PROMPT`/`ANALYZE_TOOL`/`run()` מורחבים לתייג `country_codes`/`conflict_zones` באותה קריאת API הקיימת
- `src/reporting/render.py` (שונה) - `render_report()`/`_render_section()`: כל section מקבל `id="section-{id}"` (עוגן לקישורי-עומק מהמניפסט)
- `src/publishing/publish.py` (שונה) - `build_manifest()`/`_write_manifest()` חדשים, `MANIFEST_RELATIVE_PATH`; `run()` בונה גם `manifest.json` בסוף (ראו "מה הושלם")
- `docs/assets/data/manifest.json`, `reports/assets/data/manifest.json` (חדשים, נבנים ע"י `publish.py`) - נכנסים ל-git כמו שאר `docs/`/`reports/` (תוצר-נגזר-אך-מפורסם, לא כמו `scripts/output/`)
- `src/common/about_content.py` (חדש) - `CONTENT` (he/en: doc_meta/identity_name/identity_role/title/subtitle/sections) + `render_sections_html()` משותפים ל-`build_background_doc.py` ול-`build_about_html()`
- `scripts/build_background_doc.py` (שונה) - `build_html(lang)` תומך בשתי השפות (במקום עברית בלבד), קורא מ-`about_content.py` במקום תוכן מוטבע; פלט חדש: `geopolitics-tracker-background-doc-en.{html,pdf}`
- `docs/{he,en}/about.html`, `reports/{he,en}/about.html` (חדשים) - `build_about_html()` ב-`publish.py`
- `docs/assets/images/MS_Logo.png`, `reports/assets/images/MS_Logo.png` (חדשים) - `_copy_logo()` ב-`publish.py`
- `docs/archive.html`, `docs/{he,en}/archive.html`, `reports/{he,en}/archive.html` (חדשים) - `build_index_html` (הארכיון הכרונולוגי הישן, שם בלבד השתנה) עבר לכתוב ל-`archive.html` במקום `index.html`
- `docs/index.html`, `docs/{he,en}/index.html` (שונו - תוכן הוחלף מארכיון לדף בית) - `build_homepage_html()` חדש ב-`publish.py`
- `src/publishing/assets/map/world.svg`, `src/publishing/assets/map/NOTICE.txt` (חדשים) - מפת עולם Public Domain (Wikimedia Commons) + תיעוד מקור/רישיון; מועתקים ל-`docs/assets/map/`, `reports/assets/map/` ע"י `_copy_map()` חדש
- `src/reporting/render.py` (שונה שוב) - `_category_css` → `category_css` (הוסר המקף התחתי, נהיה פומבי לשימוש חוזר ב-`publish.py`); `build_nav_html(report_date, lang)` → `build_nav_html(back_href, other_lang_href, lang)` (מקבל hrefs מפורשים); ה-`back_href` בקריאה הפנימית עודכן מ-`"index.html"` ל-`"archive.html"`
- `src/publishing/publish.py` (שונה שוב) - `build_homepage_html()`, `build_about_html()`, `_copy_map()` חדשים; `build_manifest()` קיבל מפתח `categories` חדש; `run()` בונה עכשיו גם `archive.html` (5 מיקומים) וגם `index.html`/דף-בית (3 מיקומים)
- כל 28 קבצי `report_*.html/pdf` תחת `reports/`/`docs/` נבנו מחדש (בשני סבבים נפרדים באותו סשן) כדי לאפות בהם את עוגני `id="section-{id}"` ואת קישור `archive.html` המתוקן
- `src/reporting/assets/fonts/Assistant-Variable.ttf`, `OFL.txt` (חדשים, מחליפים את `Heebo-Variable.ttf` שהוסר - גם מ-`reports/assets/fonts/` וגם מ-`docs/assets/fonts/`)
- `src/reporting/render.py` (שונה שוב) - `FONT_FAMILY`/`FONT_WEIGHT_RANGE`/`FONT_FILENAME`/`FONT_RELATIVE_PATH` חדשים; `font_face_css(relative_path)` חדש (מאחד 4 בלוקי `@font-face` משוכפלים); `build_nav_html()` מקבל לוגו (56px, מקושר ל-`../index.html`)
- `src/publishing/publish.py` (שונה שוב, השינוי המשמעותי ביותר בסשן) - `build_manifest()` מקבל מפתח `regions` חדש (וייבוא `REGION_LABELS`); `build_index_html()` מקבל פרמטר `asset_prefix` חדש + לוגו-מקושר בנאב; `build_about_html()` - לוגו ה-nav מ-56px, לוגו הכרטיס מ-56px ל-96px; `build_homepage_html()` - פריסה דו-שורתית (`.home-top-row`+`.map-module` נפרד), הסרת `.module-header`/`.module-title`, צ'יפי-אזור (`.region-chips`/`.region-chip`), כרטיס-דוח-אחרון מחודש (`.latest-card-number`/`.latest-card-preview`), לוגו מ-32px ל-56px (בלי קישור); `_HOMEPAGE_JS_TEMPLATE` - `renderRegionChips()`/`selectRegion()` חדשים, `renderLatestCard()` נכתב מחדש
- `src/common/geo_taxonomy.py` (שונה) - `REGION_LABELS` חדש (8 אזורים, name_he/name_en)
- כל 28 קבצי `report_*.html/pdf` הקיימים (7 תאריכים) נבנו מחדש שוב (סבב שלישי) לאיחוד הגופן; `archive.html`/`about.html`/`index.html` (8 מיקומים בסה"כ) ו-`manifest.json` נבנו מחדש פעמיים נוספות (איחוד גופן, ואז שיפור דף הבית v2)
- `reports/{he,en}/report_2026-08-27_{he,en}.{html,pdf}` + עותקיהם ב-`docs/` (חדשים - התאריך ה-8 בארכיון, מההרצה המלאה)
- `scripts/fix_sources_included.py` (חדש) - סקריפט חד-פעמי, מחוץ ל-5 שלבי ה-Pipeline, מתקן-למפרע את `reports.sources_included` הקיים בלי קריאת API
- `src/common/db.py` (שונה) - `get_sources_for_date()` עבר מ-`downloaded_files`-בלבד ל-JOIN דרך `articles`
- `src/reporting/render.py` (שונה שוב) - תיקון ניסוח קטן ("שסיקרו"→"שנסקרו")
- כל 28 קבצי `report_*.html/pdf` הקיימים (7 תאריכים הישנים) נבנו מחדש פעם רביעית (regeneration לתיקון-התגית + הניסוח) + `archive.html`/`manifest.json` (7 מיקומים)
- **איחוד גופן האתר סביב Assistant (2026-08-27) - חדש:** כל האתר (דוחות, `about.html`, `archive.html`, דף הבית) עבר מ-Heebo ל-**Assistant** - אותו גופן שכבר שימש את מסמך הרקע העצמאי, אך זה קובץ/מנגנון נפרד לגמרי (`scripts/` לא תלוי ב-`src/`). ארבעת בלוקי ה-`@font-face` שהיו משוכפלים אוחדו לפונקציה פומבית אחת, `font_face_css(relative_path)` ב-`render.py` (יחד עם `FONT_FAMILY`/`FONT_WEIGHT_RANGE`/`FONT_FILENAME`), באותה רוח כמו `category_css()` הקיים. קובץ הגופן (`Assistant-Variable.ttf`+`OFL.txt`) הוחלף באותה מוסכמת-מיקום כמו Heebo (`src/reporting/assets/fonts/`, מועתק אוטומטית); קובצי Heebo הוסרו משלושת המיקומים (`src/reporting/`, `reports/`, `docs/`). **regeneration מלא בוצע** (כל 7 התאריכים הקיימים + `publish.py`) לפי הכלל הקבוע ב-CLAUDE.md, ואומת ויזואלית (רינדור-לתמונה) שני דוחות בשתי השפות (בדיקת שבירת-שורה לא השתנתה) + `about.html`/`archive.html`/דף הבית, כולל בדיקת-נקודה מפורשת שבאג מספור-העמודים ההפוך ב-PDF ("14 / 1") לא החמיר עם הגופן החדש (זהה, לא תוקן - עדיין "בעיה ידועה"). נשמר ב-git (`a97adee`).
- **שיפור דף הבית v2 (2026-08-27) - חדש:** שישה שינויים על דף הבית שנבנה בסשן הקודם: (1) פריסה דו-שורתית - ציר-זמן+כרטיס-דוח-אחרון בשורה עליונה, מפה **ברוחב מלא** בשורה נפרדת מתחת (כ-3x רוחב לעומת v1, כדי שקליק על מדינה בודדת מתוך 94 יהיה שימושי בפועל); (2) מפתח `regions` חדש ב-`manifest.json` (נגזר מ-`COUNTRY_TO_REGION`, אותו עיקרון בדיוק כמו `categories` - 7/8 אזורים בפועל, `oceania` ריק) + צ'יפי-סינון-לפי-אזור מתחת למפה שמסננים את פאנל התוצאות **וגם** מדגישים את כל מדינות האזור על המפה (שימוש חוזר במחלקת `is-selected` הקיימת, לא מחלקה נפרדת); (3) הוסרו כל כותרות-המודול המילוליות ("מפה"/"ציר זמן"/"לפי טקסט") - העיצוב עצמו מתקשר את התפקיד; (4) כרטיס-דוח-אחרון מחודש - מספר-תאריך גדול ומודגש (`DD.MM`, כמו תאי ציר-הזמן) + תקציר-נושא של ה-section הראשון, בלי מילות "היום"/"דוח"; (5)+(6) לוגו הוגדל בכל מקום (56px ב-top-nav של דוחות/`about`/`archive`/דף-הבית, 96px בכרטיס "אודות הכותב") **וגם** מקושר ל-`index.html` בכל top-nav - מלבד דף הבית עצמו, ששם הלוגו נשאר בלי קישור ("כבר בית"). התגלה ותוקן במהלך אימות ויזואלי: הדגשת-אזור על המפה הייתה **בלתי-נראית** בהתחלה כי צבע ה-stroke היה זהה לצבע ה-fill של `has-coverage` - תוקן לזהב/ענבר (`#f4b942`) שמנוגד לשני הצבעים הקיימים. אומת ב-Playwright (דסקטופ+מובייל, שתי השפות) כולל לחיצה בפועל על צ'יפ אזור והדגשת חבריו על המפה. נשמר ב-git כ-2 commits נפרדים (המשך המוסכמה הקיימת - כל שכבה ב-commit משלה): איחוד הגופן (`a97adee`) ואז שיפור דף הבית (`3d6afe7`) - למרות ששני השינויים "נחתו" יחד על אותם קבצים מיוצרים (regeneration חופפת), כי שינויי הגופן היו כבר staged אך לא-committed מסשן קודם.
- `src/ingestion/fetch.py` (שונה) - `guess_newspaper()` מזהה 10 מקורות (הוספת 6 חדשים), נורמליזציית `_`/`-` לרווח לפני השוואה
- `src/reporting/render.py` (שונה שוב) - `NEWSPAPER_DISPLAY_NAMES` הורחב ל-10 מקורות; תיקון-קידוד-קונסולה (`sys.stdout.reconfigure`)
- `CLAUDE.md`, `PROJECT_LOG.md` (שונו) - רשימת 10 מקורות + נימוק-איזון-פוליטי (הזירה האמריקאית ב-4 גוונים), תיעוד שני ממצאי-פריסה נבדלים (ערבוב-סדר מול הדבקת-מילים), ותיעוד מגבלת-הקנה-מידה של `synthesize.py` (4.23 + action item 25)
- `scripts/fix_sources_included.py`-style DB fix חד-פעמי (לא סקריפט, ידני): `published_at` עבור file_id 41 (Economist) ו-42 (Der Spiegel) תוקן מ-2026-08-27 ל-2026-08-28 (התאריך האמיתי של הגיליון, לא זמן-ההעלאה-לטלגרם) - אומת מראש שלא "עוקר" תוכן מדוח 27.8 שכבר פורסם
- `src/analysis/analyze.py` (שונה שוב) - בדיקת-תקינות (isinstance/required-keys) לפני עיבוד `articles` שחזרו מה-API, לפני כל כתיבה ל-DB - מונע כשל-מבנה מלהשאיר רשומות חלקיות בהרצה חוזרת
- `src/reporting/synthesize.py` (שונה) - `max_tokens` 16000→32000 + מעבר מ-`create()` ל-`stream()`+`get_final_message()` (נדרש ע"י ה-SDK מעל סף-גודל); בדיקת-תקינות לפני `tool_use.input["sections"]` (מחזיר `[]` על תגובה פגומה, לא קורס - `run()` הקיים כבר מטפל ב-`[]` כ-100% fallback ומפעיל retry אוטומטי); תיקון-קידוד-קונסולה
- `src/reporting/synthesize.py` (נכתב מחדש לגמרי, סשן נפרד) - `synthesize_day()` הישן (קריאה-בודדת) הוסר לגמרי; שלב א' חדש (`STAGE1_SYSTEM_PROMPT`+`record_topic_groups`, קיבוץ-לנושאים בלבד, שדות קומפקטיים) + שלב ב' חדש (`record_topic_comparison`, per-topic, `ThreadPoolExecutor(max_workers=6)`, `cache_control: ephemeral` על ה-system prompt, קריאת-חימום סינכרונית ראשונה לפני fan-out מקבילי) - ראו PROJECT_LOG 4.24. `GROUPING_RATIO_THRESHOLD=0.20` (סף ישן, מיושם עכשיו רק על שלב א'); retry קצר לכל נושא בשלב ב' (`write_topic_comparison_with_retry`); ה-fallback-section-per-article הקיים (ללא שינוי) סופג את שתי הרמות.
- `reports/{he,en}/report_2026-08-28_{he,en}.{html,pdf}` + עותקיהם ב-`docs/` (חדשים - התאריך ה-9 בארכיון, מהריצה הראשונה המוצלחת של הארכיטקטורה החדשה)
- `src/ingestion/fetch.py` (שונה שוב, 2026-08-29) - `guess_newspaper()` מזהה Economist רק דרך דפוס-שם ה-Web Edition (`"web"` בשם הקובץ + "economist"/`te-\d{4}-\d{2}-\d{2}`) - שאר מהדורות-Economist-האזוריות לא יורדו יותר מעתה
- `src/reporting/synthesize.py` (שונה שוב) - `dict.fromkeys(section["article_ids"])` לפני הלולאה שמקשרת מאמרים ל-section - דה-דופ מקומי, מונע `sqlite3.IntegrityError` על `article_id` כפול בתוך נושא בודד
- `data/processed/tracker.db` (לא נכנס ל-git, שונה ידנית) - `downloaded_files.published_at` עבור Economist Web Edition (file_id=70) תוקן מ-2026-08-28 ל-2026-08-29 אחרי אישור מפורש; 6 רשומות נמחקו (4 מהדורות-Economist-אזוריות פגומות, NYT International פגום, `TE-2026-08-29-PDF.pdf`+`The Economist_2908.pdf` - כפילות-תוכן עם ה-Web Edition)
- `reports/{he,en}/report_2026-08-29_{he,en}.{html,pdf}` + עותקיהם ב-`docs/` (חדשים - התאריך ה-10 בארכיון)
- `PROJECT_LOG.md` (שונה) - סעיף חדש 4.26 (הרצה מלאה שלישית, שני באגים חדשים, פתרון-Economist), עדכון action items 23/27-30, עדכון סטטוס-שלבים (10 תאריכים)
- `src/common/about_content.py` (נכתב מחדש לגמרי) - `CONTENT["he"]`/`CONTENT["en"]` הוחלפו במלואם (6 sections כל אחד, כולל section חדש "המקורות: מארבעה לעשרה") - לא תוספת על הגרסה הקודמת; `render_sections_html()`/`_render_blocks()` לא שונו
- `scripts/output/*.{html,pdf}` (לא נכנס ל-git, נבנה מחדש פעמיים באותו סשן - פעם ראשונה עם התוכן המאושר, פעם שנייה אחרי תיקון-הקלדה "התבררה"→"התברר"), `docs/{he,en}/about.html`+`reports/{he,en}/about.html` (נבנו מחדש, אותו תיקון)

## החלטות שהתקבלו

- דו-לשוניות (עברית + אנגלית) היא דרישת ליבה מובנית בשלב ה-Reporting מההתחלה, לא תוספת מאוחרת.
- PDF בעברית ידרוש CSS עם `direction: rtl` וגופן תומך עברית ב-WeasyPrint.
- מקורות ל-MVP: Guardian, Daily Telegraph (אנגלית); Süddeutsche Zeitung, Die Welt (גרמנית).
- מוסכמות טכניות קבועות: Python 3, SQLite, `venv`. תקשורת בעברית; קוד/משתנים/הערות באנגלית.
- שחזור מצב בתחילת סשן באמצעות `/resume-project`.
- **Analysis כשני שלבי-LLM עצמאיים** (Haiku סינון רחב → Sonnet ניתוח מעמיק) - כל שלב ניתן להרצה ולבדיקה בנפרד, כדי לאפשר ביקורת איכות לפני המשך. מכוון להיות over-inclusive בשלב א'.
- **`src/common/db.py` כמקור אמת יחיד לסכמה** - שינויי סכמה עתידיים דרך `TABLE_COLUMNS` בלבד, לא `ALTER TABLE` ידני בקבצים אחרים.
- false-positive בסינון מתוקן ידנית ב-`page_screening.is_relevant` (עם עדכון `screening_reasoning` להסבר), לא נמחק ולא מתעלמים ממנו - שומר מסלול ביקורת.
- כפילות טיזר/כתבה-מלאה בין עמודים לא נפתרת ברמת עמוד ב-Analysis - **נפתר בפועל בשלב ה-Reporting**: `synthesize.py` ממזג מאמרים מאותו עיתון על אותו אירוע לקול אחד בהשוואה, תוך שמירת כל ה-`article_ids` המקוריים לקרדיט.
- **המשכיות דו-שכבתית:** HANDOFF.md נשאר מסמך טכני מדויק (מצב קוד); PROJECT_LOG.md חדש הוא יומן נרטיבי ברמה גבוהה (למה החלטנו ככה, ממצאים מדידים, action items) - לא כפילות של אותו תוכן בשני מקומות.
- **סגירת סשן כוללת רצף git חצי-אוטומטי** (`/handoff`): add אוטומטי, אך commit+push רק אחרי אישור מפורש בכל הרצה - לא פרה-אישור גורף.
- **`NEWSPAPER_DISPLAY_NAMES` כמיפוי קבוע יחיד** (בלי הפרדה HE/EN) - שמות עיתונים תמיד בכתיב הלטיני המקורי ("The Guardian" וכו'), גם בתוך טקסט עברי, גם בפרומפט ל-Claude עצמו - קונבנציה עיתונאית מקובלת שגם מסייעת בזיהוי מקור.
- **`report_sections.category`** - 8 קטגוריות קבועות (security_conflict/diplomacy_international/trade_economics/domestic_politics/migration_society/society_culture/technology_media/energy_environment) שהמודל בוחר באותה קריאה של `synthesize.py` (בלי עלות API נוספת) - במקום חוקי מילות-מפתח שבירים. הערך השמור `additional_coverage` (לעולם לא מוצע למודל) משמש כפילות תפקיד: גם צביעה וגם סימון "זה fallback, לא סקשן אמיתי" - בלי צורך בעמודת `is_fallback` נפרדת.
- **רשת ביטחון כפולה ב-`synthesize.py`** (fallback section דטרמיניסטי + retry מבוסס-יחס-20%) - ראו "בעיות ידועות" לפרטים המלאים. עיקרון שחוזר בפרויקט (ראו גם טיפול השגיאות ברמת-עמוד ב-Analysis) - העדפת רשת-ביטחון גנרית בקוד על פני תיקוני-פרומפט ממוקדי-מקרה.
- **פונט Heebo מוטמע מקומית** (לא Google Fonts חי) - קובץ variable font יחיד תחת `src/reporting/assets/fonts/`, מועתק אוטומטית ל-`reports/assets/fonts/` - כדי שגם ה-PDF (build-time) וגם האתר המתפרסם לא יהיו תלויי-רשת, קריטי לקראת הרצה עתידית ב-GitHub Actions.
- **`render.py` בלי `--force`/idempotency-guard** - בניגוד ל-`synthesize.py` (שעולה כסף לכל הרצה), render הוא read-only מה-DB וזול לגמרי - תמיד דורס קבצים קיימים, כמו build step רגיל.
- **רשימה סגורה (closed list) לתיוג גיאוגרפי, לא free-form** - `COUNTRY_LIST`/`COUNTRY_TO_REGION`/`CONFLICT_ZONE_LABELS` מוזרקות לפרומפט כ-`enum` בסכמת ה-tool (כמו `NEWSPAPER_DISPLAY_NAMES`/`CATEGORY_LABELS` הקיימים) - עקביות מבנית לצורך UI עתידי (מפה/טיימליין) עדיפה על גמישות חופשית. שלוש הכרעות סיווג גבוליות מפורשות: מצרים (EG) → `middle_east`; הודו/פקיסטן/אפגניסטן/בנגלדש/סרי לנקה → `far_east` (מכיוון שאין ל-8 האזורים סלוט "דרום-אסיה" טבעי); הונג קונג (HK) נשמר כקוד נפרד מסין (CN), גם הוא תחת `far_east`. הרשימה צפויה לגדול עם הזמן ("closed but growing") - זה לא יעד-שלמות חד-פעמי.
- **גיאו-תיוג נכתב באותה קריאת API הקיימת של `analyze.py`, לא קריאה נפרדת** - עיקרון מפורש שחזר מהפרויקט: להימנע מעלות/latency כפולה כשניתן להרחיב סכימת tool קיימת. ה-backfill למאמרים ישנים (`geo_tag_backfill.py`) כן משתמש בקריאת API נפרדת אך זולה (Haiku, בלי extended thinking, קורא רק שדות כבר-מחולצים) - כי לאותם מאמרים כבר אין עמוד גולמי זמין מול קריאת Sonnet חדשה.
- **`manifest.json`: `sections` מקור אמת, `countries`/`conflict_zones`/`dates` רק אינדקסים** - כל השלושה מכילים אך ורק ערכים עם `section_ids` לא-ריק (לא כל 94 המדינות/3 הקונפליקטים כברירת מחדל) - נימוק: אינדקס לא נועד להחזיק רשומות ריקות; UI עתידי (מפת עולם) ממילא יזדקק למקור נפרד לגיאומטריית-מפה (כל מדינות העולם), ויעשה join לפי קוד ISO מול המניפסט - מדינה שלא ברשימה = 0 כיסוי בפועל.
- **`manifest.json` בלי `comparison_text` ובלי pretty-print** - קומפקטי (`json.dumps(..., ensure_ascii=False)` בלי `indent`) - מכוון לשמור על גודל קטן ככל שהארכיון גדל (הנחיה מפורשת מהמשתמש).
- **`manifest.json` href-ים בלי הנחה על מיקום דף הבית** - `href_he`/`href_en` הם נתיב יחסי-בלבד מתוך `docs/` עצמו (`he/report_<date>_he.html#section-<id>`), לא "../" ולא הנחה איפה בדיוק דף הבית יגור - זו החלטה נפרדת שטרם התקבלה; המרת היחסיות תיעשה ב-JS כשהמיקום ייקבע, לא מוטמעת מראש בקובץ.
- **תוכן משותף (`about_content.py`) ב-`src/common/`, לא ייבוא מ-`scripts/`** - כדי ש-`scripts/build_background_doc.py` (חד-פעמי, מחוץ ל-Pipeline) לא יהפוך לתלות-הרצה של שלב Publishing (חלק מה-5 שלבים המרכזיים) - התלות היחידה היא הפוכה: שניהם מייבאים מ-`common/`, אף אחד לא מייבא מהשני.
- **מפת העולם: SVG אמיתי מוטמע inline, לא `<img>`, לא GIS מדויק** - `BlankMap-World-Compact.svg` (Wikimedia Commons, Public Domain, Canuckguy et al.) נבחר כי כל מדינה היא `<g id="xx">` בקוד ISO 3166-1 alpha-2 בדיוק (אומת: 0 קודים חסרים מול `COUNTRY_LIST`) - מאפשר ל-JS לצבוע/להוסיף click handler למדינה בודדת, מה ש-`<img src="...">` חיצוני לא מאפשר בכלל (חוסם גישת DOM). הוטמע inline (לא referenced), עם עותק גם כקובץ נכס עצמאי תחת `assets/map/` לעקביות עם מוסכמת הגופנים.
- **`manifest.json` מקבל מפתח `categories`, לא JS מקודד-קשיח** - שמות תצוגה לקטגוריות (he/en) מגיעים מ-`CATEGORY_LABELS` הקיים ב-`render.py` דרך המניפסט, לא משוכפלים ידנית ב-JS - אם רשימת הקטגוריות תשתנה, מספיק לעדכן מקום אחד. הצבעים עצמם משתמשים ב-`category_css()` (משותף עם הדוחות) כדי שהתגיות בדף הבית יזהו זהה בדיוק לדוח.
- **דף הבית: single-select v1, לא AND משולב** - קליק על מדינה או תאריך מחליף את הפילטר הפעיל; שילוב מדינה+תאריך (AND) נדחה במפורש כהרחבה עתידית (v2), לא v1 - ראו action item ב-PROJECT_LOG.
- **Playwright (headless Chromium דרך `npx`, לא תלות קבועה בפרויקט) לאימות ויזואלי של עמודים עם JS** - WeasyPrint (הכלי הקיים לאימות PDF/HTML) לא מריץ JavaScript בכלל, אז לא יכול לאמת דף שתלוי ב-`fetch()`+DOM manipulation. הורד ל-Session זה בלבד (לא נוסף ל-`requirements.txt` או ל-venv) - נדרש שרת HTTP מקומי (`python -m http.server`) כי `fetch()` על `file://` נחסם ע"י דפדפנים.
- **כלל חדש: כל שינוי במבנה/נתיבי-פלט של `render.py`/`publish.py` מחייב regeneration מלא של כל הארכיון** - נוסף ל-CLAUDE.md אחרי שהדפוס נתפס בשלישית (ראו "בעיות ידועות" למטה).
- **איחוד גופן: פונקציה משותפת אחת (`font_face_css()`), לא כפילות מתמשכת** - עקבי עם עקרון `category_css()`/`build_nav_html()` הקיימים: כל CSS/HTML משוכפל שנוצר פעם שלישית (4 מקומות במקרה הזה) מאוחד לפונקציה פומבית אחת ב-`render.py`, לא מושאר כפול "כי זה עובד".
- **הדגשת-אזור על המפה: שימוש חוזר במחלקת `is-selected` הקיימת, לא מחלקה חדשה** - נשקל להוסיף מחלקה נפרדת (`region-highlighted`) כדי להפריד את מנגנון-הניקוי בין בחירת-מדינה-בודדת לבחירת-אזור, אך **נדחה**: `clearSelection()` הקיימת כבר מנקה כל `.is-selected` גלובלית בלי תלות במקור, כך ששימוש באותה מחלקה בדיוק גם לצ'יפ הנבחר וגם למדינות-האזור עובד נכון בלי קוד נוסף - פשטות עדיפה כשהניקוי הגנרי כבר קיים ומכסה את המקרה.
- **פיצול ל-2 commits נפרדים כשעבודה משתי משימות "נוחתת" על אותם קבצים מיוצרים** - כשהתגלה שאיחוד-הגופן (משימה קודמת) נשאר staged-אך-לא-committed ושיפור דף-הבית (משימה נוכחית) נכתב מעליו, **הוחלט** לפצל בכל זאת ל-2 commits נפרדים תואמי-ה"שכבה" (לא commit מאוחד אחד) - עקבי עם איך שכל שכבה אחרת בפרויקט טופלה (geo-tagging, `about.html`, דף-הבית המקורי כל אחד ב-commit נפרד משלו) - גם במחיר regeneration כפול (זול וזניח, אין עלות API בשלבים אלה). בוצע בפועל בלי `git stash`: מכיוון שהגופן כבר היה staged מסשן קודם והשינויים החדשים רק התווספו על גביו ב-working tree, `git commit` (בלי `git add` נוסף) על ה-index הקיים כמו-שהוא הפיק ישירות commit "גופן-בלבד" נקי; רק אז `git add -A` + commit שני לשאר.
- **תיקון-למפרע לעמודת-מטא-דאטה שגויה, לא הרצה חוזרת של שלב יקר** - כשהתגלה ש-`reports.sources_included` שגוי, **הוחלט במפורש לא** להריץ `synthesize.py --force` (שהיה עולה כסף Sonnet וחוזר על עבודה שכבר הייתה תקינה - הקיבוץ-לנושאים/ההשוואות עצמם) - במקום זאת סקריפט חד-פעמי שמתקן ישירות ב-DB, אותה מוסכמה בדיוק כמו `geo_tag_backfill.py`. עיקרון כללי: לתקן ברמת השכבה שבה הבאג קיים בפועל (כאן: עמודת מטא-דאטה מחושבת), לא ברמה גבוהה יותר שגם היא הייתה "עובדת" אבל בעלות מיותרת.
- **"0 שורות = דו-משמעי" - מגבלת עיצוב מודעת ולא-מתוקנת:** העדר שורות ב-`article_countries`/`article_conflict_zones` למאמר נתון לא מבחין בין "טרם תויג" ל"תויג, ואין לו תיוג אמיתי". הוחלט במפורש **לא** להוסיף עמודת סטטוס/sentinel לצורך זה - עלות ה-reprocessing המיותר (מאמרים שכבר תויגו ל"ריק" ייבדקו שוב בהרצה הבאה של ה-backfill) נמוכה מספיק (אומת בפועל: 19/437 מאמרים) לעומת המורכבות הנוספת. כל קוד עתידי שקורא מהטבלאות האלה חייב להביא בחשבון את הדו-משמעיות הזו.
- **תיקון-למפרע ידני חד-פעמי ל-`published_at`, לא שינוי-קוד כללי** - Economist ו-Der Spiegel (שבועונים) מתויגים ב-DB לפי זמן-ההעלאה-לטלגרם, שקדם ליום-הכיסוי הנומינלי שלהם (הועלו בערב הקודם). **הוחלט (במקור)**: לתקן רק את שתי הרשומות הספציפיות האלה ידנית (אחרי וידוא שלא "עוקר" תוכן מדוח שכבר פורסם) - **לא** לשנות את לוגיקת-הקיבוץ הכללית. **עדכון (2026-08-29)**: אותו דפוס נצפה בפעם הרביעית (Economist Web Edition, 29.8) - תוקן שוב נקודתית, אך ההחלטה "לא לשנות קוד כללי" **הועלתה בעדיפות** ל-action item גבוה (ראו PROJECT_LOG action item 29) - דפוס חוזר מספיק פעמים שהפתרון הכללי (לגזור תאריך-שער מתוך שם-הקובץ ב-`fetch.py`) כבר לא "מתוכנן לעתיד" אלא ממתין לביצוע.
- **⚠️→✅ מגבלת-קנה-מידה של `synthesize.py` ב-9+ מקורות - נפתרה בפועל (סשן נפרד, 2026-08-28), לא נותרה פתוחה**: הוחלט מלכתחילה שלא לתקן-מהר (max_tokens נוסף/retry נוסף) אלא לתכנן-מחדש ארכיטקטונית - ראו PROJECT_LOG 4.24. הארכיטקטורה החדשה (שני-שלבים + caching) נבדקה בשלוש הרצות אמיתיות נפרדות (27.8/28.8/29.8) עם 0 כשלים בכולן.
- **`guess_newspaper()` מצומצם לזהות Economist רק דרך ה-Web Edition** (2026-08-29) - אחרי שחקירת-תוכן (לא רק hash) הראתה שהמהדורות-האזוריות האחרות הן כפילות-תוכן או פגומות, לא מקור-מידע ייחודי אמיתי (ראו PROJECT_LOG 4.26). מונע הורדה מיותרת מראש, לא רק מחיקה-בדיעבד.
- **בדיקת-בריאות יזומה (סריקת "tradingref.com") הפכה לשלב סטנדרטי לפני Screening, לא רק גילוי-בדיעבד** - הרצה על 29.8 תפסה 5/13 קבצים פגומים לפני תשלום עליהם. עדיין לא מומש כקוד קבוע בפייפליין (בוצע כסקריפט אד-הוק על ידי בכל הרצה) - ראו action item פתוח.
- **דה-דופ מקומי (`dict.fromkeys()`) על `article_ids` בתוך section אחד ב-`synthesize.py`** - כשל חדש (`article_id` כפול בתוך רשימת נושא בודד, UNIQUE constraint) שנצפה לראשונה ב-185 מאמרים/57 נושאים. תוקן ברמת-קוד גנרית (לא תיקון-פרומפט), עקבי עם העיקרון החוזר בפרויקט.
- **מדידת-עלות בדיעבד (retroactive) דרך שחזור-מדויק ל-`count_tokens`** - כש-`screen.py`/`analyze.py` לא לוכדים `response.usage` בזמן-אמת, עלות בדיעבד נמדדת ע"י שחזור מדויק של אותו system+tools+content שנשלח בפועל (מ-DB) והרצת `count_tokens` האמיתי מולו. **מגבלה ידועה**: לא עובד ל-output tokens כש-adaptive thinking מעורב (הטוקנים האלה אבודים לצמיתות) - רק input + tool-output "רגיל" נמדד במדויק. לעתיד: אם נדרשת מדידת-עלות מדויקת בשלבים עם thinking, יש ללכוד `response.usage` בזמן-ריצה מלכתחילה.
- **בדיקת-תקינות לפני DB write, לא רק try/except סביב קריאת ה-API** - ב-`analyze.py`, תגובת-API "מוצלחת" מבחינה טכנית (`stop_reason=tool_use`) יכולה עדיין להחזיר מבנה שגוי (`articles` כמחרוזת ולא רשימה) שגורם קריסה **אחרי** שחלק מהמאמרים כבר נכתבו ל-DB - סיכון לרשומות-כפולות בהרצה חוזרת. **הוחלט**: לאמת את *כל* מבנה התגובה (isinstance + מפתחות-חובה) *לפני* תחילת לולאת ה-`insert_article()`, לא רק לתפוס exception סביב קריאת ה-API עצמה - כך שכשל-מבנה = 0 כתיבות, בטוח לגמרי להרצה חוזרת. באותה רוח: ב-`synthesize.py`, תגובה-פגומה (מפתח `sections` חסר לגמרי) מוחזרת כרשימה ריקה `[]` במקום קריסה - `run()` הקיים כבר מטפל ב"0% articles assigned" כ-100% fallback ומפעיל retry אוטומטי, כך שלא נדרשה לוגיקת-retry נוספת, רק מניעת-קריסה.

## עבודה שטרם הושלמה

- **סינון משולב מדינה+תאריך+אזור בדף הבית (AND, לא רק single-select)** - v2, טרם תוכנן. ראו PROJECT_LOG action item.
- **לבדוק שוב את גודל הלוגו (56px/96px) על האתר החי בדפדפן אמיתי** - אחרי שהכל התפרסם בפועל (לא רק Playwright headless מקומי) - ייתכן שיידרש עוד הגדלה. **חדש, ביקש המשתמש בפירוש ב-`/handoff`.**
- **לשקול מנגנון-זיהוי ל"מקור ירד בהצלחה אך תרם 0 מאמרים"** (PDF סרוק-בלבד וכדומה) - חדש, רלוונטי במיוחד לקראת הרצה אוטומטית (GitHub Actions) שבה לא יהיה משתמש אנושי שמבקש דיווח מפורש אחרי כל שלב. טרם תוכנן; ראו "בעיות ידועות" למקרה הקונקרטי שחשף את הפער.
- חיבור דומיין מותאם - `docs/CNAME` (`geopolitics.meirshemesh.com`) כבר נוצר וצד GitHub Pages מוכן, אך יש לוודא שגם רשומת ה-DNS המתאימה הוגדרה ב-GoDaddy (CNAME record מצביע ל-`meir-shemesh.github.io`) כדי שהכתובת בפועל תעבוד - לא אומת עדיין.
- footer עם קישור מוחלט לאתר בתוך ה-PDF - ממתין להחלטת כתובת אתר סופית (ראו "בעיות ידועות"/החלטה 4.12 ב-PROJECT_LOG).
- `tests/` - עדיין ריק, אין טסטים.
- כיול פרומפט `screen.py` נגד false positive על עמודי נוסטלגיה/היסטוריה (ראו "בעיות ידועות") - שינוי מוצע, טרם בוצע.
- אין עדיין אוטומציה/תזמון (cron) להרצת השלבים - כרגע רק סקריפטים חד-פעמיים ידניים, בכוונה, לא נבחן עדיין.
- מאמר גבולי אחד (`file_id=3`, "Weil es sich lohnt", `region_topic: Medienrecht/Boulevardpresse`) עלה כחשוד ל-false positive בבדיקת מדגם אקראית - לא אומת/תוקן ידנית.
- מספור עמודים הפוך ב-PDF העברי ("14 / 1" במקום "1 / 14", ראו "בעיות ידועות") - זוהה, לא תוקן בכוונה.
- **בעדיפות גבוהה: פתרון כללי (לא נקודתי) לגזירת תאריך-שער לשבועונים** (Economist/Der Spiegel/כל שבועון עתידי) ב-`fetch.py`, במקום להסתמך על זמן-העלאה-לטלגרם - נצפה **4 פעמים נפרדות** עד כה (תוקן נקודתית בכל פעם, ראו PROJECT_LOG action item 29). לא "מתוכנן לעתיד" יותר - ממתין לביצוע.
- לשקול סריקה שיטתית **קבועה** (לא אד-הוק כמו היום) לזיהוי קבצי-PDF פגומים (image-only, חתימת "tradingref.com") - נצפה שוב ב-29.8 (5/13 קבצים), מנגנון-הבדיקה עצמו עדיין לא ממומש כקוד קבוע בפייפליין.
- לשקול אם להמשיך להוריד NYT International כברירת מחדל - פגום ב-4/4 הגיליונות שנצפו עד כה (ראו PROJECT_LOG action item 27). המשתמש העלה את השאלה, לא ביקש פעולה מיידית.
- דה-דופ מבוסס-hash כללי ב-`fetch_channel()` (לפני `download_media()`) - עדיין לא ממומש; נדחה כל עוד המקרים הקיימים (Economist) נפתרים ברמת `guess_newspaper()`/תוכן, ראו PROJECT_LOG action item 24.

## בעיות ידועות

- **מגבלת סדר-קריאה בחילוץ (2026-08-24):** `pdfplumber` לא שומר סדר קריאה נכון בעמודי מגזין/גריד (כתבות מתערבבות). נבדק גם עם `extract_text(layout=True)` - לא פתר, כי אין טורים ברוחב אחיד. תיקון אמיתי דורש זיהוי גיאומטרי (rects/lines) - נדחה בכוונה; שלב ה-Analysis קיבל הנחיה מפורשת להתמודד עם זה במקום. **אושר בפועל גם על המקורות החדשים (2026-08-28):** עמוד השער של NYT International הראה בדיוק אותו דפוס - שלוש כותרות לא-קשורות התערבבו מילה-במילה. לא באג חדש, אותה מגבלה בדיוק שכבר תועדה.
- **הדבקת-מילים בלי רווחים בקופסאות-תקציר צפופות - ממצא חדש ונפרד מערבוב-הטורים (2026-08-28):** ב-Wall Street Journal (עמודת "What's News") וב-USA Today (קיצרים בעמוד הראשון) נמצא טקסט מחולץ ללא שום רווח בין מילים - `"TechfirmsfearGOP mightloseHouse"`, `"Twentyninestatessaidsocialmediacompany designedplatformstoaddictkids"`. זו **לא** אותה תופעה כמו ערבוב-סדר-קריאה למעלה - שם הטקסט של כל כתבה בפני עצמה תקין, רק סדר-ההתחלפות בין כתבות שגוי; כאן המילים בתוך אותה קופסת-טקסט עצמה נדבקות זו לזו לגמרי, כנראה בגלל טקסט מוצדק (justified) בגופן-צפוף-במיוחד שבו `pdfplumber` לא מזהה את המרווח בין מילים. **מוגבל לקופסאות-תקציר צפופות בלבד** - הטקסט הרגיל של הכתבות באותם עמודים (לא בקופסאות התקציר) חולץ תקין לגמרי. טרם תוקן; שלב ה-Analysis צריך לקבל בעתיד הנחיה נפרדת להתמודד עם קטעי-טקסט מחוברים ככה (בנוסף להנחיה הקיימת על ערבוב-סדר), אם זה יתברר כמשמעותי בפועל.
- **The Economist - הכי נקי מבין כל 10 המקורות, אך עם artifact-חיצוני משלו (2026-08-28):** עמודי "The world this week" נקראים כפרוזה רגילה ותקינה לגמרי, בלי אף ערבוב. נמצא artifact לא-קשור: כתובת ה-URL של ערוץ ההפצה (`https://t.me/tesharing`) מוטמעת ישירות בתוך הטקסט המחולץ (כנראה watermark/קרדיט של מי ששיתף את הקובץ) - רעש לא-מזיק לכאורה, לא נבדק אם משפיע על שלב ה-Analysis.
- **כפילות טיזר/כתבה-מלאה (2026-08-24) - נפתר בשלב ה-Reporting:** אותו אירוע מתועד לעיתים כשני מאמרים נפרדים באותו עיתון (עמוד שער + עמוד פנימי) - שיקוף אמיתי של מבנה עיתון, לא הזיה. `synthesize.py` ממזג אותם לקול אחד בהשוואה (ראו "החלטות שהתקבלו").
- **False positive בסינון על עמודי נקרולוגים/נוסטלגיה היסטורית (2026-08-25):** עמודים כמו "Court & Social" ב-Telegraph עם טור "ONE HUNDRED YEARS AGO" נתפסים לעיתים כרלוונטיים (אזכורי מדינות היסטוריים נשמעים כמו גאופוליטיקה עדכנית). תופעת לוואי: Sonnet מסרב לעבד אותם (`stop_reason=refusal`, `category=general_harms`) - ככל הנראה בגלל פרטים אישיים אמיתיים של נפטרים; סירוב לגיטימי, לא באג. שני המקרים הידועים (`file_id=2 page=20`, `file_id=14 page=24`) תוקנו ידנית ל-`is_relevant=0`. **לשקול:** הנחיה מפורשת ב-`SYSTEM_PROMPT` של `screen.py` להבחין תוכן היסטורי/נוסטלגי מתוכן גאופוליטי עדכני.
- **כשלי API חולפים בהרצות גדולות:** ברצף של מאות קריאות נצפו `Connection error` (קשור כנראה למחשב שנכנס למצב שינה) ו-`Internal server error` בודדים. הטיפול הקיים (עמוד שנכשל נשאר `pending`, לא נכתב חלקית) מאפשר Retry פשוט ובטוח - לא דורש שינוי קוד.
- **False positive מ-Analysis שהתגלה בהרצת הבדיקה הראשונה של Synthesis (2026-08-25):** `article_id=254` (Daily Telegraph, "Harry's service to his country deserves our respect and generosity", `region_topic: UK monarchy / Prince Harry's return to Britain`) עבר את שלב ה-Analysis למרות שאין לו זווית בינלאומית/חוצת-גבולות אמיתית - תוכן מקומי-בריטי גרידא. התקבל כסקשן יחיד-מאמר בדוח הראשון שנבנה (24.8.2026, 81 מאמרים קלט). הוחלט שלא לתקן כרגע - חריגה בודדת, מתועדת כרעש ידוע ברמת ה-Analysis; לא בוצע שינוי בפרומפט של `analyze.py`.
- **פער כיסוי בשלב ה-Synthesis - נפתר במנגנון קוד כפול-שכבתי (2026-08-25/26):** בשתי הרצות הבדיקה הראשונות (24.8.2026) המודל השמיט 1-2 מאמרים מתוך 81 מהוראת "כל article_id בדיוק בסקשן אחד" (`article_id=288` בהרצה 1, גם `54` בהרצה 2); בהרצת בדיקה נפרדת קפץ הפער חד-פעמית ל-70/81 בקריאה בודדת, אך אבחון ישיר הראה שזו תנודתיות רגילה של קריאה בודדת (קריאה חוזרת עם אותו קלט חזרה מיד ל-38 sections תקינים). נבנו שתי שכבות הגנה גנריות ב-`synthesize.py` (ולא עוד תיקוני פרומפט ממוקדי-מקרה): (1) fallback section דטרמיניסטי בלי קריאת API נוספת לכל מאמר שלא שובץ, בנוי מ-`region_topic`/`stance_summary`/`key_excerpt` שכבר קיימים; (2) אם יחס הנפילה ל-fallback עולה על 20% ממאמרי היום - retry אוטומטי חד-פעמי עם אותו קלט, עם "QUALITY WARNING" בולט בפלט אם גם הניסיון השני לא משתפר. נבדק בפועל: הרצה סופית על 24.8.2026 נתנה יחס 1/81 (מתחת לסף, retry לא הופעל), 37 model sections + 1 fallback = 38 total, כיסוי 100%.
- **שמות עיתונים משובשים בתוך comparison_text - נמצא רק בקריאה ידנית של ה-PDF בפועל, לא ב-DB (2026-08-26):** באותו מסמך, Süddeutsche Zeitung הופיע בשלוש צורות שונות - הכתיב הנכון, ותעתיקים עבריים משובשים כולל תערובת קירילית-עברית ("זюддойче צייטонг"); גם Guardian/Daily Telegraph/Die Welt הופיעו בתעתיקים עבריים לא-עקביים. תוקן בהוספת הנחיה קשיחה ל-`SYSTEM_PROMPT` של `synthesize.py`: שמות עיתונים בתוך הפרוזה חייבים תמיד להיות בכתיב הלטיני המדויק מ-`NEWSPAPER_DISPLAY_NAMES`, עם דוגמת משפט עברי מפורש. אומת אחרי תיקון+הרצה חוזרת: סריקה של כל 14 עמודי ה-PDF (לא רק מדגם) - אפס תווים קיריליים, אפס תעתיקים עבריים של שם עיתון. **לקח מתודולוגי**: קריאת ה-PDF/HTML הסופי בפועל חשפה באג שבדיקות ברמת ה-DB (טקסט גולמי) לא חשפו - ביקורת איכות על שלב ה-Reporting חייבת לכלול קריאה של התוצר הסופי, לא רק שאילתות SQL.
- **תיאום מגדרי שגוי בעברית ("בשני זוויות" במקום "בשתי זוויות") - תוקן באותו תיקון פרומפט**: נוספה הנחיה כללית ב-`SYSTEM_PROMPT` לכתיבת עברית תקנית עם תשומת לב לתיאום מגדרי בין מספרים לשמות עצם. אומת בדגימה ידנית מה-PDF החדש שדוגמאות כן תואמות נכון ("שתי העמדות", "שני מאמרים", "בשלושה עיתונים").
- **`PermissionError` בכתיבת PDF כשהקובץ פתוח בתוכנה אחרת (2026-08-26):** `render.py` נכשל עם `PermissionError` פעמיים ברציפות בכתיבת `report_2026-08-24_he.pdf` - קובץ ה-PDF הקודם היה כנראה פתוח בצפייה (viewer) בזמן ההרצה, מה שנועל אותו ב-Windows. נפתר בפשטות ע"י סגירת הקובץ ולפני ריצה חוזרת - אין צורך בשינוי קוד, אך שווה לזכור כתופעה צפויה בכל פעם שבודקים PDF ואז מריצים `render.py` מחדש על אותו תאריך בלי לסגור אותו קודם.
- **כלי חילוץ טקסט אוטומטיים אינם עדות אמינה לתקינות RTL/bidi ב-PDF (2026-08-26):** בזמן אימות ה-PDF העברי נעשה שימוש ב-`pdfplumber` לחילוץ הטקסט לבדיקה - זה יצר רושם מוטעה של בעיה (תווים והופעות מילים הפוכות) שבפועל שיקף רק את חוסר-היכולת של הכלי לטפל נכון ב-bidi/RTL בחילוץ, לא בעיה אמיתית בקובץ. נצפו לפחות שני מקרים כאלה: היפוך תווים בתוך מילה בודדת (בבדיקת "גאופוליטיקה יומי" מוקדם יותר בסשן), והיפוך סדר מילים בביטויים אנגליים מוטמעים בתוך משפט עברי. אומת ויזואלית ע"י המשתמש שה-PDF בפועל תקין לגמרי בשני המקרים. **לקח**: הבדיקה הקובעת לרינדור PDF/RTL היא תמיד עין אנושית שמסתכלת על הקובץ כפי שהוא נראה בפועל (או viewer אמיתי) - לא פלט של כלי חילוץ טקסט אוטומטי, שאינו מיישם bidi reordering. **מקרה נוסף (2026-08-27, `scripts/build_background_doc.py`)**: טקסט שחולץ מ-PDF רפרנס חיצוני (לא מיוצר ע"י הפרויקט) הראה כמה ביטויים לטיניים/מעורבי-שפה מוטמעים בתוך משפט עברי בסדר הפוך ("Claude API", "PDF+HTML", דומיין ה-אתר) - אותו כשל בדיוק, לא ספציפי ל-`pdfplumber` או לקבצים שהפרויקט עצמו מייצר. תוקן ע"י הסתמכות על הניסוח הקנוני הידוע כבר מ-CLAUDE.md/PROJECT_LOG.md, לא ניחוש.
- **שני fallback sections חשודים כ-false positive מ-Analysis, לא רק "מאמר שלא שובץ" (2026-08-26, הרצת batch על 5 תאריכים):** ב-2026-08-23 (יום עם מקור בודד בלבד, Daily Telegraph) שני מאמרים נפלו ל-fallback עם `region_topic` חריג: `article_id=296` קיבל `region_topic="Language and grammar (non-geopolitical)"` - כלומר `analyze.py` עצמו כבר זיהה נכון שהתוכן **לא** גאופוליטי, ובכל זאת יצר עבורו רשומת מאמר מלאה בטבלת `articles` - חוסר-עקביות פנימית בשלב ה-Analysis (המסקנה שלו סותרת את הפעולה שלו), לא בעיה בשלב ה-Reporting. `article_id=297` קיבל `region_topic="And Sino-Ukraine relations / broader"` - תגית שמתחילה במילה "And" נראית כמו קטע חתוך מטקסט ארוך יותר, אולי בעיית פרסור/טרנקציה בתשובת המודל בשלב ה-Analysis. בשני המקרים מנגנון ה-fallback עבד בדיוק כמתוכנן - לכד את המקרים החריגים והציג אותם בגלוי במקום להסתיר אותם, זו לא תקלה של Reporting. **הוחלט**: לא לתקן כרגע - לצבור מקרים דומים נוספים לאורך זמן (הרצות עתידיות), ולשקול תיקון ב-`analyze.py` רק אם/כשמצטבר מספיק מדגם כדי לדעת אם זו תופעה שיטתית או רעש נדיר.
- **מספור עמודים הפוך ב-PDF העברי - "14 / 1" במקום "1 / 14" (2026-08-26):** נצפה ויזואלית (רינדור בפועל של עמוד ל-PNG, לא רק טקסט) ב-footer של כל עמוד ב-PDF העברי. הסיבה הסבירה: `content: counter(page) " / " counter(pages)` ב-`@bottom-center` יורש `direction: rtl` מהמסמך, וה-bidi reordering הופך את סדר שני המונים סביב ה-"/" (כל מספר בפני עצמו נשאר תקין, רק הסדר היחסי ביניהם מתהפך). לא תוקן - **הוחלט במפורש לחזור לזה בנפרד בהמשך**, לא כעת.
- **גיאו-תיוג: "0 שורות" דו-משמעי בין "טרם תויג" ל"תויג ואין לו כלום" (2026-08-27):** ראו "החלטות שהתקבלו" לפרטים המלאים - זו מגבלה מודעת ומקובלת, לא באג. אומתה בפועל: אחרי backfill מלא, 19/437 מאמרים חזרו כ"ממתינים" לפי `get_articles_without_geo_tags` אף שה-backfill עצמו דיווח שרק מאמר `id=67` הוחמץ - נבדקו ידנית כל ה-19 ואומת שכולם נושאים גלובליים/על-לאומיים ללא מדינה בודדת ברשימה הסגורה (לא באג בקוד). **לא לתקן** - התנהגות מתוכננת.
- **CSS Grid blowout במסך צר בדף הבית - נמצא ותוקן (2026-08-27):** מפת ה-SVG המוטמעת inline דחפה את `.home-grid` (ואיתו את כל העמוד) לרוחב גדול מה-viewport במסך נייד (390px) - הכותרת והמפה נחתכו, הלוגו נעלם מהתצוגה. נתפס רק דרך צילום מסך אמיתי (Playwright) במסך צר, לא היה נראה בבדיקה בדסקטופ. תוקן עם `grid-template-columns: repeat(3, minmax(0, 1fr))` + `min-width: 0` על `.home-module` - הגורם השורשי: implicit grid tracks משתמשים ב-`min-width:auto` כברירת מחדל, שמתבסס על תוכן ה-min-content (כאן: רוחב פנימי גדול של ה-SVG), לא על רוחב ה-container בפועל.
- **דפוס חוזר שלישי: קישורים ישנים "תקועים" אחרי שינוי מבנה - הפך לכלל קבוע ב-CLAUDE.md (2026-08-27):** מעבר הארכיון ל-`archive.html` שונה בקוד (`render.py`) אבל דוחות קיימים המשיכו להצביע על `index.html` הישן עד שהורץ מחדש `render.py` על כל 7 התאריכים - אותו דפוס בדיוק כמו ה-backfill הגיאוגרפי ועוגני ה-`section-id` קודם באותו סשן. ראו CLAUDE.md ("כללי עבודה קבועים") לכלל החדש שנוסף בעקבות זה.
- **הרצה מלאה ורצופה של כל 5 שלבי ה-Pipeline על תאריך חדש (2026-08-27) - חדש, פעם ראשונה:** זו הפעם הראשונה שכל 5 השלבים (Ingestion→Extraction→Analysis→Reporting→Publishing) רצו ברצף אחד על יום חדש, כולל תיוג גיאוגרפי **חי** ב-`analyze.py` (לא backfill). תוצאות: 5 קבצים ירדו (Ingestion), 155 עמודים חולצו (Extraction), 37/155 סומנו רלוונטיים (Screening), 43 מאמרים זוהו מ-36 עמודים (Analysis, 1 עמוד נכשל), 28 sections (Reporting), 264 sections/70 מדינות בסה"כ אחרי Publishing. בדיקת-איכות ייעודית על התיוג הגיאוגרפי (ביקש המשתמש מראש, כי זו הרצה חיה ראשונה ולא backfill) אישרה 43/43 מאמרים מתויגים, עם דגימה שהראתה תיוג רב-מדינתי נכון והבחנה נכונה בין conflict_zones רלוונטי לסתם-אזכור. **שני ממצאים אמיתיים עלו מההרצה** - ראו "בעיות ידועות" למטה (PDF סרוק-בלבד של הגארדיאן ותקלת-תגית-מקורות שנגרמה ממנו; עמוד-רפואי חוזר עם `stop_reason=refusal`).
- **תיקון `get_sources_for_date()` + backfill חד-פעמי לתיקון `reports.sources_included` הקיים (2026-08-27):** ראו "בעיות ידועות" לגילוי המקורי. `get_sources_for_date()` ב-`db.py` שונה מ-`SELECT DISTINCT newspaper FROM downloaded_files` (מבוסס-הורדה-בלבד) ל-JOIN דרך `articles` (מבוסס-תוכן-בפועל). מכיוון שהערך השגוי כבר היה **מאוחסן** ב-`reports.sources_included` (מחושב פעם אחת ב-`synthesize.py`, לא בזמן render), נדרש תיקון-למפרע לנתונים קיימים - **לא** הרצה חוזרת של `synthesize.py --force` (שהייתה עולה כסף Sonnet בלי סיבה, כי התוכן/הקיבוץ-לנושאים עצמו היה תקין, רק עמודת המטא-דאטה שגויה). נכתב `scripts/fix_sources_included.py` - סקריפט חד-פעמי (לא חלק מה-5 שלבים) שעובר על כל התאריכים הקיימים, מחשב מחדש עם הפונקציה המתוקנת, ומעדכן ב-DB ישירות (`UPDATE`, בלי קריאת API) - אותה מוסכמת "תיקון-למפרע זול" כמו `geo_tag_backfill.py`. נבדק בפועל: 1/8 תאריכים השתנה בפועל (2026-08-27 בלבד - Guardian הוסר; שאר 7 התאריכים כבר היו נכונים). תוקן גם ניסוח קטן ("שסיקרו"→"שנסקרו") באותו סבב `render.py`. **regeneration מלא בוצע** (8 תאריכים + `publish.py`) לפי הכלל הקבוע ב-CLAUDE.md, ואומת ויזואלית (רינדור-לתמונה, שתי השפות) שהתגית מציגה כעת רק 3 מקורות ל-27.8 (בלי Guardian). נשמר ב-git (`f38220d`), יחד עם תוצרי ההרצה המלאה (דוח 2026-08-27) ותיקון-הסיווג הידני לעמוד שסורב (ראו למטה).
- **תיקון ידני חוזר ל-`page_screening.is_relevant` (file_id=27, page=23, 2026-08-27):** אבחון ישיר מול ה-API אישר `stop_reason=refusal` על עמוד "Court & Social" (טור "100 שנה אחורה" + נקרולוגים אמיתיים) - אותו דפוס בדיוק שתועד ואושר בעבר (ראו "בעיות ידועות"). תוקן ל-`is_relevant=0` עם `screening_reasoning` מוסבר, אחרי אישור מפורש של המשתמש (ניסיון ראשון נחסם ע"י ה-classifier של auto mode כ-DB write ישיר; אושר ידנית בסבב שני).
- **PDF סרוק-בלבד (image-only) גורם לפער-כיסוי שקט לגמרי, בלי אף שגיאה - נמצא ב-2026-08-27:** קובץ ה-Guardian שירד ביום זה (29MB, גודל תקין למראה) התברר כ-scan מוטבע כתמונה בלבד ב-44/44 העמודים, עם רק 14 תווי טקסט-אמיתי ("tradingref.com" - כנראה watermark של שירות-שיתוף-קבצים חיצוני) על כל עמוד. `pdfplumber` לא נכשל (אין exception), Screening (Haiku) זיהה נכון שאין תוכן גאופוליטי בכל 88 העמודים (44 מקור + 44 כפילות-הורדה) - כך שדבר לא "נשבר" ברמת קוד, אבל **המקור נעדר לגמרי מהדוח בפועל**, בלי שום אינדיקציה. אומת ישירות: `page.chars` על עמוד 1 = 14 תווים, `page.images` = תמונה אחת. **חשוב להרצה אוטומטית עתידית**: אין כרגע שום מנגנון שמזהה "מקור ירד בהצלחה אך תרם 0 מאמרים" - התגלה כאן רק כי המשתמש ביקש דיווח מפורש אחרי כל שלב.
- **תוצאה ישירה של הממצא הקודם: `reports.sources_included` הציג מקור-שלא-תרם-כלום - נמצא ותוקן (2026-08-27):** `get_sources_for_date()` נגזר מ-`downloaded_files` בלבד (הורדה הצליחה = מקור "כלול"), לא מ-`articles` בפועל - כך שהתגית "עיתונים שנסקרו היום" הציגה Guardian למרות 0 מאמרים אמיתיים ממנו. תוקן (ראו "החלטות שהתקבלו" ו"מה הושלם") - זו הדגמה קונקרטית לכך שממצא "לא-קריטי, לא-שובר" יכול בכל זאת להדליף למידע-משתמש גלוי (לא רק ל-DB פנימי).
- **הדגשת-אזור על המפה בלתי-נראית - נמצא ותוקן באימות ויזואלי (2026-08-27):** כשנוסף סינון-לפי-אזור ל-v2 של דף הבית, ההדגשה (`is-selected`, stroke) הייתה בלתי-נראית לגמרי במדינות שכבר `has-coverage` - כי שני המצבים השתמשו באותו צבע `--masthead-accent` בדיוק (stroke זהה לצבע ה-fill, שקוף ויזואלית). נתפס רק דרך צילום-מסך אמיתי (Playwright) אחרי קליק על צ'יפ אזור, לא היה נראה בבדיקת קוד/CSS בלבד. תוקן ע"י שינוי צבע ה-stroke לזהב/ענבר (`#f4b942`) שמנוגד לשני מצבי ה-fill האפשריים (has-coverage/ברירת-מחדל, אור/כהה). לקח נוסף לרשימה: כל שינוי ויזואלי חדש חייב אימות בפועל מול מצב-קיים, לא רק מול רקע ריק.
- **6/55 קבצי PDF פגומים (image-only) - 11%, לא 2 מקרים בודדים כפי שנראה תחילה (2026-08-28):** סריקה שיטתית חד-פעמית (לא חוזרת עדיין) חשפה: NYT International (3/4 קבצים, 75%!), Guardian UK (2 קבצים), USA Today Sports Weekly (1 קובץ) - כולם עם אותה חתימה מדויקת ("tradingref.com" בלבד, 14 תווי-טקסט, תמונה אחת לעמוד). כנראה בעיה במקור-ההפצה (שירות חיצוני ששולח קבצים סרוקים-מחדש), לא בקוד הפרויקט. ראו PROJECT_LOG action item.
- **`published_at` לשבועונים לא תמיד תואם את יום-הכיסוי הנומינלי (2026-08-28, וגם 2026-08-29 - הפעם הרביעית):** Economist ו-Der Spiegel שניהם הועלו לערוץ הטלגרם **בערב שלפני** יום-הכיסוי שלהם (תבנית נפוצה לשבועונים אירופאיים) - `get_articles_for_date()`/`get_sources_for_date()` מקבצות לפי `date(published_at)` (זמן-הודעת-הטלגרם), לא לפי התאריך הנומינלי בשם-הקובץ. תוקן נקודתית שוב ב-29.8 (Economist Web Edition) - **פתרון כללי עדיין לא ממומש**, ראו "עבודה שטרם הושלמה" (הועלה בעדיפות אחרי 4 מקרים).
- **PDF סרוק-בלבד (תדרוגט tradingref.com) חזר שוב ב-29.8, הפעם ב-4/7 קבצי Economist בו-זמנית + NYT International (4/4 הגיליונות שנצפו עד כה):** ראו PROJECT_LOG 4.26. הבדיקה-היזומה (לא רק גילוי-בדיעבד) רצה כאן בפעם הראשונה כחלק סטנדרטי לפני Screening - תפסה את כל 5 המקרים לפני תשלום על סינון/ניתוח שלהם.
- **`synthesize.py` - `article_id` כפול בתוך רשימת נושא בודד (2026-08-29):** קריאת שלב-א' (קיבוץ-לנושאים) החזירה פעם ראשונה `article_id` שמופיע פעמיים באותו `article_ids` (לא בין-נושאים - בתוך אותו נושא ממש), גורם ל-`sqlite3.IntegrityError` (UNIQUE constraint) בזמן קישור-המאמרים ל-section. לא נצפה ב-27.8/28.8 (קנה-מידה קטן יותר). תוקן עם `dict.fromkeys()` לפני הלולאה - ראו "החלטות שהתקבלו".
- **שאלת ריבוי-המהדורות של Economist - נפתרה (2026-08-29), לא נותרה פתוחה:** ראו "החלטות שהתקבלו" + PROJECT_LOG 4.26 לפרטים המלאים. לא הייתה בעיה ארכיטקטונית אמיתית (מיזוג-שגוי-של-תוכן-שונה) כמו שנחשש - שילוב של כפילות-ייצוא + קבצים פגומים + מהדורה מקיפה אחת אמיתית.
- **קריסת-קידוד-קונסולה (cp1252) התפשטה לחמישה סקריפטים, לא רק ארבעה (2026-08-28):** התיקון הראשון היום (`sys.stdout.reconfigure(encoding="utf-8")`) הוחל על fetch.py/extract.py/screen.py/analyze.py לפי הבקשה המקורית - אך `synthesize.py` נתקל באותה קריסה בדיוק (`ć` בתוך שם פוליטיקאי הונגרי) בהמשך אותו יום, כי הוא לא נכלל ברשימה המקורית. תוקן גם ב-`synthesize.py` וב-`render.py` (`publish.py` מכוסה עקיפין דרך ה-import מ-`render.py` - אומת ישירות). **לקח**: זו לא הייתה קריסה "נדירה" - היא תחזור בכל סקריפט חדש/קיים שמדפיס תוכן-שמקורו-בשפה-זרה בלי הגנה, כל עוד stdout רץ עם קידוד-ברירת-מחדל של Windows.
- **אי-יציבות-רשת חוזרת בקריאות-streaming ארוכות (2026-08-28):** שלוש קריסות `ConnectionResetError`/`httpx2.ReadError` (WinError 10054) התרחשו באותו ערב במהלך קריאות-streaming ארוכות ל-Sonnet (synthesize.py על 9 מקורות) - קישוריות כללית ל-API נבדקה ותקינה (תגובה מהירה), כך שזו בעיה ספציפית לחיבורים-ארוכים, לא לרשת בכלל. לא ברור אם זו תופעה חולפת של אותו ערב או דפוס שיחזור - לצפות אם זה קורה שוב בהרצות עתידיות ארוכות.
- **"סקשן רפאים" ריק ב-Synthesis - כשל מסוג שונה מפער-כיסוי (2026-08-26, הרצת 2026-08-25):** `report_sections.id=432` ("Reclaiming the national flag from the AfD") נוצר עם 0 מאמרים מקושרים ב-`report_section_articles`, ו-`comparison_text` שלו הוא הפניה ריקה ("*See the AfD rise section.*") במקום השוואה אמיתית - נראה שהמודל התחיל סקשן נפרד, מיזג את תוכנו לסקשן ה-AfD בפועל, אבל השאיר את ה"קליפה" הריקה בפלט. המנגנון הקיים (רשת הביטחון הכפולה) לא תופס את זה כי הוא בודק כיסוי-מאמרים (שכל מאמר קלט משובץ לסקשן כלשהו), לא תקינות-סקשנים (שכל סקשן שנוצר מכיל לפחות מאמר אחד). **הוחלט**: לא לתקן כרגע - לצבור מקרים דומים נוספים; אם חוזר על עצמו, לשקול בדיקה נוספת ב-`synthesize.py` שפוסלת/מסירה סקשנים עם `article_ids` ריק לפני הכתיבה ל-DB.

## הצעד הבא המומלץ

**הפייפליין עצמו יציב ומוכח על 10 מקורות/3 הרצות מלאות רצופות** - אין כרגע חסימה טכנית להמשך הרצה יומית שוטפת. סדר-עדיפויות מומלץ לסשן הבא:

1. **בעדיפות גבוהה, חדש**: פתרון כללי לגזירת תאריך-שער לשבועונים ב-`fetch.py` (ראו "עבודה שטרם הושלמה") - 4 מקרים חוזרים מספיקים כדי לטפל בזה ברמת-קוד, לא עוד תיקון-DB נקודתי בכל הרצה.
2. לשקול אם להמשיך להוריד NYT International כברירת מחדל (4/4 גיליונות פגומים עד כה) - שאלה שהמשתמש העלה, טרם הוכרעה.
3. **בעדיפות גבוהה, ישן וטרם טופל**: להפעיל Prompt Caching ברמת-ארגון (לא רק בתוך `synthesize.py`'s stage 2, ששם זה כבר קורה) לפני שהריצה היומית הופכת קבועה לגמרי.
4. להקים GitHub Actions להרצה יומית מתוזמנת - עדיין לא התחיל; ראו PROJECT_LOG 4.7.2 לתלות שטרם נפתרה (אחסון/סנכרון בענן).
5. לוודא רשומת ה-DNS ב-GoDaddy עבור `geopolitics.meirshemesh.com`.
6. כיוונים נוספים פתוחים, לא-חוסמים: סינון AND משולב בדף הבית (v2); בדיקת גודל הלוגו על האתר החי בדפדפן אמיתי; מנגנון-זיהוי קבוע ל"מקור ירד אך תרם 0 מאמרים" (לקראת אוטומציה ללא-משתמש-אנושי).

**תזכורת קבועה מ-CLAUDE.md**: כל שינוי עתידי במבנה/נתיבי-פלט של `render.py`/`publish.py` מחייב regeneration מלא של הארכיון לפני commit.

## סשן אחרון

**תאריך:** 2026-08-29

**גשר על סשנים שקדמו (לא תועדו כאן קודם)**: בין ה-handoff הקודם (2026-08-27, "חלק ט'") לסשן הזה רצו לפחות שני סשנים שלא הריצו `/handoff` בסיומם: (1) תכנון-מחדש ארכיטקטוני מלא ל-`synthesize.py` (שני-שלבים + Prompt Caching, פותר את מגבלת-קנה-המידה שתועדה ב"חלק ט'" - ראו PROJECT_LOG 4.24, נשמר ב-`f7a1b30` אחרי חיזוקי-אמינות `8a9268d`/`945d300` והרחבת-זיהוי-מקורות `c564af8`); (2) הרצה מוצלחת ראשונה של הארכיטקטורה החדשה על 2026-08-28 (257 מאמרים/9 מקורות, 0 כשלים). PROJECT_LOG כן עודכן בזמן אמת בשני הסשנים האלה (4.24-4.25) - רק HANDOFF.md פיגר מאחור. תוקן עכשיו במלואו.

**תיאור הסשן הנוכחי:** הרצה מלאה שלישית ורצופה של כל 5 שלבי ה-Pipeline על תאריך חדש (2026-08-29) - הריצה השנייה בקנה-מידה אמיתי של ארכיטקטורת-Synthesize החדשה, ועוד מסמך אחד שנכתב מחדש. פירוט מלא בכל ממצא ב-PROJECT_LOG 4.26; כאן רק תמצית טכנית.

**Ingestion+Extraction**: 13 קבצים ירדו, 1195 עמודים חולצו, 0 כשלים. **בדיקת-בריאות יזומה** (לא רק גילוי-בדיעבד כמו קודם) - סריקת חתימת-שחיתות "tradingref.com" על כל הקבצים החדשים *לפני* Screening - זיהתה 5/13 קבצים פגומים: 4 מהדורות-Economist-אזוריות (Middle East and Africa, EU, Asia Pacific, UK) ו-NYT International (הפעם ה-4/4 שהוא נצפה פגום, 100%).

**חקירת ריבוי-המהדורות של Economist (עצירה יזומה של המשתמש לפני Screening/Analysis, כדי לא לשלם על ניתוח לפני שהוכרעה שאלה ארכיטקטונית)**: 7 קבצי-Economist שירדו עוררו חשש למיזוג-שגוי-של-תוכן-שונה תחת מקור אחד. השוואת-תוכן בפועל (לא רק SHA-256, ששלל כפילות-בייטים פשוטה) גילתה שילוב של שלושה דברים, לא בעיה ארכיטקטונית: מהדורה סטנדרטית שהופצה פעמיים (ייצוא/OCR שונה), 4 המהדורות-האזוריות הפגומות שכבר זוהו, ומהדורת-Web מקיפה אמיתית (347 עמודים, כל האזורים בקובץ אחד). **הוחלט**: `guess_newspaper()` צומצם לזהות רק את ה-Web Edition מעתה; קבצי-היום שירדו תחת המהדורות הישנות נמחקו (DB+דיסק).

**Screening+Analysis**: 678 קריאות Haiku אמיתיות (21 עמודים ריקים דולגו בחינם), 289 סומנו רלוונטיים; 289 קריאות Sonnet, 185 מאמרים, 0 כשלים. **עלות נמדדה בדיעבד** (לא נוחשה) - טכניקה חדשה: שחזור-מדויק של כל קריאה (system+tools+content+output מ-DB) והרצת `count_tokens` האמיתי מולם (967 קריאות בסה"כ). Screening $1.83 (מדויק לחלוטין), Analysis $3.17 (**גבול תחתון בלבד** - טוקני adaptive-thinking אבודים לצמיתות, לא נלכדו בזמן-ריצה).

**Synthesize - שני באגים חדשים נמצאו ותוקנו תוך כדי**: (1) `published_at` של Economist Web Edition הראה 2026-08-28 בעוד יום-הכיסוי האמיתי 2026-08-29 (הפעם הרביעית שהדפוס הזה נצפה) - תוקן ב-`UPDATE` ישיר אחרי אישור מפורש, ואז Synthesize רץ מחדש ותפס את כל 185 המאמרים/5 המקורות (במקום 61/4 בהרצה הראשונה, לפני התיקון). (2) קריאת שלב-א' החזירה `article_id` כפול בתוך רשימת נושא בודד - קרס עם `sqlite3.IntegrityError`; תוקן עם `dict.fromkeys()`. **תוצאה סופית**: 57 נושאים/185 מאמרים, 100% כיסוי, 0 fallback, יחס 3.25 מאמרים/נושא (ממשיך לעלות: 1.41→2.45→3.25 על פני 27.8/28.8/29.8). Render+Publish הצליחו; תגית-מקורות אומתה נכונה (5 מקורות, בלי USA Today - 0 מאמרים כדין). אומת ויזואלית בשתי השפות (Playwright) לפני commit. נשמר ב-git (`6fa16d1`).

**`about_content.py` נכתב מחדש לגמרי (משימה נפרדת, אותו סשן)**: תוכן מאושר מראש במלואו (עברית+אנגלית) - מבנה-6-sections הוצג ואושר *לפני* כתיבת קוד. מחליף לחלוטין את `CONTENT["he"]`/`CONTENT["en"]` (לא תוספת) - כולל section חדש "המקורות: מארבעה לעשרה", רענון נתוני "מה קיים היום", והחלפת תיאור-הארכיטקטורה המיושן של Synthesize. נבנה מחדש (`build_background_doc.py`+`publish.py`) ואומת ויזואלית בשתי השפות פעמיים (פעם שנייה אחרי תיקון-הקלדה קטן, "התבררה"→"התברר" - טעות-התאמה-מגדרית בטיוטה המקורית). נשמר ב-git (`37c4185`).

**קבצים ששונו:** ראו "קבצים שנוצרו או שונו" למעלה - רשימה מלאה ומעודכנת, לא כפולה כאן.
