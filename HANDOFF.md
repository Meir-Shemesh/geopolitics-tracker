# Project Handoff

## הערות פתיחה להרצה הבאה (לקרוא לפני שמתחילים)

- **הכפול של ה-Economist (הודעה 29698) כבר ב-DB כשורה id=205 וממתין להחלטה שלך.** ירד ב-20.9 בהרצה האוטונומית (SHA-256 `7874051f…` זהה ל-file 191, אומת מול כל ה-DB) ולפי הוראתך **לא נמחק**. **מחיקתו דורשת אישור מפורש ממך, לא אוטומטית, גם אם ה-hash תופס אותו נכון.** מצבו: 356 `extracted_pages`, 0 `page_screening`, `report_date=2026-09-20`, בלי מאמרים. **עד שתחליט: ריצה כללית של `screen.py` (בלי `--file`) תסרוק את 356 עמודיו (~$1.7 מבוזבז) - לכן לכל קובץ לגיטימי `screen.py --file <תת-מחרוזת ייחודית>` בנפרד (כך נעשה ב-20.9).** בהיותו ב-DB, ההודעה 29698 לא תרד שוב: רק מחיקה מפעילה מחדש את מחזור ההורדה-החוזרת.
- **Analysis: `analyze.py --file-id <id>` לכל קובץ חדש, לא ריצה כללית**, כל עוד 2 העמודים הישנים של file 199 (עמודים 39/63, WSJ Magazine) ממתינים. ריצה כללית תנתח אותם, והמאמרים שיצאו (אם יצאו) יצטרפו לדוח 19.9 שכבר פורסם ויהפכו ליתומים. כך נעשה ב-20.9.
- הכלל החדש אומת בקליטה אמיתית ב-20.9 (`report_date` מולא, `orphan_review` החזיר OK). בכל הרצה: `python -m unittest discover -s tests` לפני, ו-`python -m scripts.orphan_review` אחרי Synthesize.

## מצב הפרויקט הנוכחי

**חמשת שלבי ה-Pipeline (Ingestion→Extraction→Analysis→Reporting→Publishing) יציבים ומוכחים על עשרות הרצות אמיתיות**, כולל 9 הרצות אוטונומיות-לגמרי ברצף (11.9 עד 19.9, כולל commit+push ואימות-חי). הארכיון כולל **28 תאריכים** (2026-08-20 עד 2026-09-19, עם פער-לא-סגור מודע ב-31.8-2.9) בשתי שפות מלאות, שני פורמטים (HTML+PDF), תחת `reports/{he,en}/` ו-`docs/{he,en}/`. **האתר חי**: https://geopolitics.meirshemesh.com.

**מקורות MVP פעילים בפועל: 10** (הרשימה המקורית של 10, בניכוי NYT המושעה-לצמיתות, בתוספת Washington Post; אומת מול `guess_newspaper()`). `compute_scan_limit()` מחשב דינמית את חלון-הסריקה וחוזר לגודל-בסיס (200) בהרצות שוטפות.

**כלל שיוך מאמר לדוח (חדש, 20.9)**: מאמר שייך לדוח של **היום המקומי שבו הקובץ הורד** (`downloaded_files.report_date`), לא ל-`published_at`. פירוט, נימוק, חלופות שנדחו ותוצאות - CLAUDE.md ("החלטות ארכיטקטוניות קבועות") ו-PROJECT_LOG 4.44. **308 מאמרים (9.7% מהארכיון) שנותחו ולא נכללו באף דוח שוקפו**: `report_date` מולא למפרע ל-20 קבצים, ו-4 דוחות נבנו מחדש (28.8, 13.9, 18.9, 19.9); הארכיון כולו מראה כעת 0 יתומים. **קישורי-עומק (`#section-<id>`) ישנים לארבעת התאריכים האלה נשברו** - ה-`section-id` הוחלפו בבנייה-מחדש.

**האתר עומד בחבילת-ציות רגולטורי בסיסית** (נגישות/גילוי-נאות-AI/תנאי-שימוש, PROJECT_LOG 4.38) ובעל פאביקון בכל עמוד. **דף הבית (v2)**: מפה אינטראקטיבית, ציר-זמן, כרטיס-דוח-אחרון, צ'יפי-אזור+קונפליקט - הכל קורא מ-`manifest.json` בזמן-ריצה. `topic.html`/`archive.html`/`about.html`/`accessibility.html`/`terms.html` זמינים בכל שפה. פרטי הדוחות והאתר - ראו CLAUDE.md, לא משוכפל כאן.

## מה הושלם (יכולות בשלות, לא יומן-שינויים)

- **`src/common/db.py`** - מקור-אמת יחיד לסכמה, מיגרציה אוטומטית; כולל את כלל שיוך-הדוח (`report_date`, `REPORT_DATE_SQL`, `report_date_for_download()`).
- **`src/ingestion/fetch.py`** - `guess_newspaper()` (10 מקורות, NYT מושעה, Economist רק Web-Edition, SZ עם וריאנט ללא-ניקוד), `compute_scan_limit()`, heartbeat+flood-wait logging. לא נערך בסשן האחרון.
- **`src/extraction/extract.py`**, **`src/analysis/screen.py`+`analyze.py`** (Haiku רשת-רחבה → Sonnet ניתוח+תיוג-גיאוגרפי; `screen.py --file` / `analyze.py --file-id` להרצה לקובץ בודד), **`geo_tag_backfill.py`**.
- **`src/common/geo_taxonomy.py`** - 94 מדינות/8 אזורים/3 קונפליקטים.
- **`src/reporting/synthesize.py`** - ארכיטקטורת שני-שלבים, רשת-ביטחון כפולה. **`render.py`** - HTML+PDF דו-לשוני, מראי-מקום, accordion, sticky-nav, מיון-לפי-כיסוי, פוטר, favicon. **`src/publishing/publish.py`** - `docs/` שלם.
- **`src/common/about_content.py`** - תוכן About משותף, כולל גילוי-נאות-AI.
- **בדיקות**: `tests/test_report_date_rule.py` - 15 טסטים ב-`unittest` (בלי תלות חדשה, DB זמני), נבדקו במוטציה (5 נכשלים אם מחזירים את הכלל הישן). `python -m unittest discover -s tests -v`.
- **`scripts/orphan_review.py`** - כלי **קבוע** לקריאה בלבד: מאמרים שנותחו ולא נכללו באף דוח + סימולציית הכלל על הארכיון (`python -m scripts.orphan_review`, שורת VERDICT; אמור להראות 0). **`scripts/backfill_report_date.py`** - חד-פעמי, בוצע.
- **מדידת-עלות** - שחזור `count_tokens` ל-Screening/Analysis (Analysis כ"גבול-תחתון", בלי thinking), Synthesize מ-usage אמיתי. תמחור מאומת: Sonnet 5 = $2/$10, Haiku 4.5 = $1/$5 למיליון טוקן. עלות יום מדודה: $4.47 (17.9), $9.43 (18.9), $5.01 (19.9); בנייה-מחדש של 4 דוחות ב-20.9: $3.84.
- **הרצה אוטונומית מלאה** מוכחת 9 פעמים, עם guardrail-בטיחות מכני לפני push ובדיקת-בריאות יזומה לפני Screening (סקריפטים אד-הוק בסקראצ'פד - לא שמורים בריפו, ראו "עבודה שטרם הושלמה").

## קבצים שנוצרו או שונו בסשן האחרון (20.9)

- **קוד/בדיקות**: `src/common/db.py` (שונה); `scripts/backfill_report_date.py`, `scripts/orphan_review.py`, `tests/test_report_date_rule.py` (חדשים).
- **תיעוד**: `CLAUDE.md` (החלטה ארכיטקטונית חדשה, כלל-הבדיקה אחרי Synthesize, פקודות-בדיקה, תיאור `tests/`/`scripts/`), `PROJECT_LOG.md` (סעיף 4.44, action items 24/29/39/40 עודכנו ו-41/42 חדשים, 4 שורות בטבלת-המעקב, תיקון 4.42/4.43), `HANDOFF.md`.
- **דוחות שנבנו מחדש**: `reports/{he,en}/report_{2026-08-28,2026-09-13,2026-09-18,2026-09-19}_*.{html,pdf}` + עותקים ב-`docs/`, `archive.html` ×5, `manifest.json` ×2 (1,392 נושאים).
- **DB (לא ב-git)**: `report_date` מולא ל-20 שורות; id=200 (Economist כפול) נמחק. **גיבוי ה-DB שלפני השינוי נשמר ב-`..\geopolitics-tracker-backups\tracker_2026-09-20_pre-backfill.db`** (תיקייה-אחות של הריפו, מחוץ ל-git, עם README; 183 קבצים/3,178 מאמרים/28 דוחות/308 יתומים, SHA-256 אומת זהה, integrity ok) - זו המוסכמה החדשה לגיבויי DB, ואין לו מקבילה ב-git. סקריפטים חד-פעמיים, גיבוי-ביניים נוסף (אחרי ה-backfill, לפני הבנייה-מחדש) ותמונות האימות נשארו בסקראצ'פד הזמני ויימחקו - מיותרים.

## החלטות שהתקבלו

ראו "החלטות ארכיטקטוניות קבועות" + "כללי עבודה קבועים" ב-CLAUDE.md. לא משוכפל כאן.

**החלטות ספציפיות-לסשן שלא שייכות ל-CLAUDE.md** (זמניות/הקשריות):
- Washington Post נבחר כתחליף ל-NYT בעמוד About באותו מיקום פוליטי בערך (אישור מפורש). כתובת-יצירת-קשר בהצהרת-הנגישות (`meir@meirshemesh.com`) ניתנה במפורש ע"י בעל-האתר.
- 2026-08-31–2026-09-02 (3 ימים) **לא ייסגרו כרגע** - החלטה פתוחה, לא השתנתה.
- **20.9 - שיוך לפי יום-ההורדה, ולא תאריך משם-הקובץ** (נטישה מכוונת של הפתרון שהוצע ב-action item 29). **לא הוחל למפרע על ה-backlog של 3.9–9.9**: ה-813 מאמרים היו סימולציה, ולקבצים האלה יש 7 דוחות יומיים תקינים - לא נבנה שום דבר ל-9.9.
- **20.9 - בנייה-מחדש של 28.8/13.9/18.9/19.9 אושרה במפורש** (כולל 19.9 בגלל מאמר בודד, כי הוא אחד מ-308 שנדרש לאמת), וכך גם מחיקת id=200 (שנחסמה ב-19.9 ע"י ה-classifier ולא נעקפה).
- **מראי-המקום נשארים עם תאריך-ההעלאה של המאמר** (Economist בדוח 18.9 מצוטט כ-17.9) - בכוונה, החלטה פתוחה (action item 41).

## עבודה שטרם הושלמה

1. **מנגנון-דילוג ב-`fetch_channel()`** (message_id/hash ידועים-כפולים-או-פגומים) - פותר יחד את action items 24/38/39. **הכפול ירד שוב ב-20.9 (id=205) וממתין להחלטה** - ראו "הערות פתיחה": עד שהמנגנון קיים, בכל הרצה להשוות SHA-256 מול ה-DB לפני Screening ולדווח על כפול, **ולמחוק רק באישור מפורש שלך, בכל פעם, גם כשה-hash תפס נכון** (בנוסף ה-classifier חסם מחיקה כזו פעם).
2. **שם הסרט התיעודי** (action item 40) - "NAZA" בלטינית ב-19.9 העברי (פעם שלישית; נשאר גם אחרי הבנייה-מחדש). תיקון בשיטת `4d2bb8f`, או מיפוי-שמות קבוע בפרומפט.
3. **תאריך המראי-מקום** (action item 41) - להשאיר תאריך-העלאה, או להציג תאריך-דוח.
4. **תקרת שלב 1 של Synthesize** (action item 42) - נמדד: 358 מאמרים ב-74% מהתקרה, אבל 321 (18.9) נזקקו ל-retry. לפני ריצת-backlog לספור מאמרים צפויים ולהחליט על פיצול.
5. **2 עמודי WSJ Magazine (file 199, עמודים 39/63) נשארו `pending` ב-Analysis** (`KeyError 'articles'`) - יטופלו בהרצת Analysis הבאה, עלות זניחה. שים לב: עמוד שמשוחזר יום אחרי ההורדה מצטרף לדוח שכבר פורסם (הפער היחיד שהכלל החדש לא מכסה, כפי שקרה ל-2 מאמרי Guardian).
6. **פער 31.8-2.9 (3 ימים)** - לא מזוהה ע"י `compute_scan_limit()` (action item 35).
7. **`analyze.py`'s `NEWSPAPER_LANGUAGES`** מכסה רק 4/10 - אינרטי-מאומת (action item 36). **טביעת-אצבע-שחיתות שנייה** בלי tradingref.com (action item 37) - לא הוכרעה.
8. **סקריפטי בדיקת-הבריאות ושחזור-העלות** עדיין נכתבים מחדש בכל הרצה ונמצאים רק בסקראצ'פד; לשמור ב-`scripts/` (בדיקת-ה-hash שלהם הייתה בתוך-הקבוצה בלבד - חייבת להשוות מול ה-DB). ואז action item 33 (allowlist לוגים, timeout 10 דקות, בדיקת GTK3) כתנאי-סף להרצה מתוזמנת.
9. Prompt Caching ברמת-ארגון; GitHub Actions לתזמון יומי; **כיסוי-טסטים** מוגבל כרגע לכלל שיוך-הדוח בלבד. מפה נגישה-לקורא-מסך אך לא ניתנת-למיקוד-מקלדת (הוצהר בהצהרת-הנגישות); סינון-AND-משולב אינטראקטיבי בדף הבית (קיים דרך `topic.html`).

## בעיות ידועות (עדיין רלוונטיות בלבד - ראו PROJECT_LOG להיסטוריה המלאה)

- **"0 שורות" דו-משמעי** ב-`article_countries`/`article_conflict_zones` - מגבלה מודעת, לא באג.
- **`published_at` נשאר זמן-העלאה בלבד** ונסחף מיום-הכיסוי (שבועונים/סופ"ש/מוספים) - כבר לא משפיע על שיוך-דוחות, אך עדיין קובע את `compute_scan_limit()` ואת תאריך מראי-המקום.
- **דוחות 28.8/13.9/18.9/19.9 נכתבו מחדש במלואם**: נוסחים חדשים ו-`section-id` חדשים (קישורי-עומק ישנים נשברו). ב-18.9 שלב 1 של Synthesize החזיר בניסיון הראשון 100% מאמרים לא-משויכים ו-retry יחיד תיקן - סיבה לא נבדקה.
- **WSJ Magazine (מוסף-רבעוני-לייפסטייל) מזוהה כ-Wall Street Journal** (5.9 ו-19.9) ועובר Screening (5% רלוונטי) - אותה שאלת-מוסף כמו Guardian Weekly/USA Today Sports Weekly (PROJECT_LOG פריט 8).
- **מספור-עמודים הפוך ב-PDF העברי** - bidi, לא תוקן בכוונה. **סקשן-רפאים ריק אפשרי ב-Synthesis** (נצפה פעם אחת). **article יחיד חשוד כ-false-positive** (`article_id=254`, "Prince Harry").
- *(נפתר ב-20.9)* ה-163 מאמרי Economist ועוד 145 מאמרים בלי דוח; דוח 18.9 כולל כעת את כל 10 המקורות.

## הצעד הבא המומלץ

**הפייפליין יציב ואין חסימה טכנית.** סדר-עדיפויות:

1. ✅ **הכלל החדש אומת בקליטה אמיתית (20.9)**: `report_date` מולא ושווה ליום ההורדה בכל 5 הקבצים, ו-`orphan_review` החזיר VERDICT OK. **להמשיך בכל הרצה**: `python -m unittest discover -s tests` לפני, `python -m scripts.orphan_review` אחרי Synthesize.
2. **מנגנון-דילוג ב-`fetch.py`** (#1 למעלה) - הורדה-חוזרת של הכפול צפויה בימים הקרובים.
3. להחליט על #2 (שם הסרט) ו-#3 (תאריך ציטוט) - החלטות-בעלים קצרות.
4. לשמור את סקריפטי בדיקת-הבריאות ושחזור-העלות ב-`scripts/` (#8), ואז לגשת ל-action item 33 כתנאי-סף להרצה-מתוזמנת, ואחר-כך Prompt Caching ברמת-ארגון + GitHub Actions.

**תזכורת קבועה מ-CLAUDE.md**: כל שינוי עתידי במבנה/נתיבי-פלט של `render.py`/`publish.py` מחייב regeneration מלא של הארכיון + `git status` מפורש לפני commit.

## סשן אחרון

**תאריך:** 2026-09-20 (המשך ישיר להרצה האוטונומית של 19.9; העבודה נמשכה מערב 19.9 ועד 20.9)

**תיאור:** מסע שלם משאלה אחת - "למה 163 מאמרי Economist לא נכללו באף דוח?": (1) סקירת כל הארכיון - 308 מאמרים יתומים (9.7%), כולם מאותו מנגנון; (2) החלטת המשתמש על כלל-שיוך חדש (יום-הורדה) ונטישת הרעיון של תאריך משם-הקובץ; (3) מימוש ב-`db.py`, אומת על עותק של ה-DB (30 תאריכים זהים); (4) backfill של 20 קבצים, מחיקת הכפול id=200 (אחרי אימות hash), וטבלת-השפעה מלאה שאושרה לפני שנגעו בדוח כלשהו; (5) בנייה-מחדש של 28.8/13.9/18.9/19.9 ($3.84) ו-publish; (6) אימות מאמר-מאמר ב-DB וב-HTML המרונדר בשתי השפות (308/308), 24 התאריכים האחרים ללא שינוי, אימות ויזואלי שאושר ע"י המשתמש; (7) שמירת `orphan_review.py` והטסט הקבוע, ועדכון התיעוד. commit+push אחד בסוף, באישור מפורש. **בהמשך אותו יום - הרצה אוטונומית מלאה עשירית על 20.9** (ראו PROJECT_LOG 4.45): הקליטה הראשונה תחת הכלל החדש, 4 מקורות, 55 מאמרים, $2.68; הכפול של ה-Economist ירד שוב (id=205) ונשאר בלי מחיקה, בהמתנה להחלטתך.

**דפוס חוזר בפרויקט**: "בדוק את ההנחה מול מה שבאמת בדוח, לא מול ספירה". בסשן זה זה קרה שלוש פעמים: הטענה שה-Economist תרם לדוח 18.9 (4.42) נכתבה בלי בדיקה מול `report_section_articles`; עמודת "הכלל הישן" בסימולציה ספרה מאמרים לפי יום-העלאה כולל יתומים ויצרה מראית-עין של "17.9 מתדלל"; ו-"9.9 עם 813 מאמרים" נקרא כדוח שצריך לבנות בעוד שהיה סימולציה בלבד. בכל שלושת המקרים הבדיקה מול מה שיש בפועל בדוחות הפכה את המסקנה.
