# Project Handoff

## הערות פתיחה להרצה הבאה (לקרוא לפני שמתחילים)

- **הכפול של ה-Economist (הודעה 29698) עדיין ב-DB כשורה id=205, וממתין להחלטה שלך.** SHA-256 זהה ל-file 191, `report_date=2026-09-20`, בלי מאמרים. **מחיקתו דורשת אישור מפורש ממך בכל פעם, גם אם ה-hash תופס אותו נכון** - הוראה קבועה מהמשתמש. עד שיוחלט: כל הרצת `screen.py` חייבת `--file <תת-מחרוזת ייחודית>` לכל קובץ לגיטימי בנפרד (ריצה כללית תסרוק את 356 עמודיו לחינם - כ-$1.7 מבוזבז). בהיותו ב-DB, ההודעה לא תרד שוב - רק מחיקה מפעילה מחדש את מחזור ההורדה-החוזרת.
- **Analysis: `analyze.py --file-id <id>` לכל קובץ חדש, לא ריצה כללית**, כל עוד 2 העמודים הישנים של file 199 (עמודים 39/63, WSJ Magazine) נשארים `pending`. ריצה כללית תנתח אותם והמאמרים (אם יצאו) יצטרפו לדוח 18.9 שכבר פורסם ויהפכו ליתומים.
- **לפני כל הרצה**: `python -m unittest discover -s tests` (15 טסטים - כלל שיוך-הדוח). **אחרי Synthesize**: `python -m scripts.orphan_review` (אמור להחזיר VERDICT 0). **אחרי Ingestion, אם יש דוחות חדשים**: אם הריצה חוצה חצות (מתחילה אחרי 00:00 אך אמורה להיחשב ל"אתמול"), אין עדיין כלל-קבוע - ראו "עבודה שטרם הושלמה" #5.
- **האתר תלת-לשוני מעכשיו (עברית/אנגלית/גרמנית)** - כל שינוי עתידי ב-`render.py`/`publish.py` חייב לשמור על כך, ולפי הכלל הקבוע ב-CLAUDE.md מחייב regeneration מלא של הארכיון בכל 3 השפות.

## מצב הפרויקט הנוכחי

**חמשת שלבי ה-Pipeline יציבים ומוכחים על עשרות הרצות אמיתיות**, כולל 11 הרצות אוטונומיות-לגמרי ברצף (11.9 עד 21.9). הארכיון כולל **30 תאריכים** (2026-08-20 עד 2026-09-21, עם פער-לא-סגור מודע ב-31.8-2.9) - **בשלוש שפות מלאות** (עברית/אנגלית/גרמנית), שני פורמטים (HTML+PDF), תחת `reports/{he,en,de}/` ו-`docs/{he,en,de}/`. **האתר חי**: https://geopolitics.meirshemesh.com, אומת-חי ב-3 השפות אחרי כל פרסום.

**מקורות MVP פעילים בפועל: 10** (הרשימה המקורית של 10, בניכוי NYT המושעה-לצמיתות, בתוספת Washington Post; אומת מול `guess_newspaper()`).

**כלל שיוך מאמר לדוח**: מאמר שייך לדוח של **היום המקומי שבו הקובץ הורד** (`downloaded_files.report_date`), לא ל-`published_at`. פירוט ב-CLAUDE.md ו-PROJECT_LOG 4.44.

**תלת-לשוניות (חדש, 22.9) - ההרחבה הגדולה של הסשנים האחרונים**: האתר עבר מדו-לשוניות לתלת-לשוניות. **מהדוח הבא ואילך גרמנית נוצרת כג'ינרוט מקורי** מהמאמרים המקוריים, באותה קריאת Stage 2 של Synthesize בדיוק כמו עברית ואנגלית - **Stage 1 (הקיבוץ) נשאר בלתי-נגוע במכוון**, כדי לא להגביר את הסיכון לתקלת "רוב-לא-משויך" הידועה שם. הארכיון הקיים (30 דוחות) עבר rewrite-לגרמנית חד-פעמי (לא ג'ינרוט-למפרע). דף-הבית, `about.html`, `archive.html`, `topic.html`, `accessibility.html`, `terms.html` וכל דוח - כולם תלת-לשוניים במלואם. פירוט מלא, כולל נימוקים ואפשרויות-שנדחו: CLAUDE.md ("החלטות ארכיטקטוניות קבועות") ו-PROJECT_LOG 4.47.

**האתר עומד בחבילת-ציות רגולטורי בסיסית** (נגישות/גילוי-נאות-AI/תנאי-שימוש) ובעל פאביקון בכל עמוד. **דף הבית**: מפה אינטראקטיבית, ציר-זמן, כרטיס-דוח-אחרון, צ'יפי-אזור+קונפליקט - קורא מ-`manifest.json` בזמן-ריצה, כעת ב-3 שפות.

## מה הושלם (יכולות בשלות, לא יומן-שינויים)

- **`src/common/db.py`** - מקור-אמת יחיד לסכמה, מיגרציה אוטומטית; כלל שיוך-הדוח (`report_date`, `REPORT_DATE_SQL`); `report_sections.topic_label_de`/`comparison_text_de` (nullable - `NULL` = דוח שקדם לתלת-לשוניות ולא גובה עדיין).
- **`src/ingestion/fetch.py`** - `guess_newspaper()` (10 מקורות, NYT מושעה, Economist רק Web-Edition), `compute_scan_limit()`, heartbeat+flood-wait logging.
- **`src/extraction/extract.py`**, **`src/analysis/screen.py`+`analyze.py`** (Haiku רשת-רחבה → Sonnet ניתוח+תיוג-גיאוגרפי; `--file`/`--file-id` להרצה לקובץ בודד), **`geo_tag_backfill.py`**.
- **`src/common/geo_taxonomy.py`** - 94 מדינות/8 אזורים/3 קונפליקטים; `REGION_LABELS`/`CONFLICT_ZONE_LABELS` כוללים `name_de`; `COUNTRY_LIST` כולל `name_de` לכל 94 המדינות (נוסף ללא-API, נדרש למפת-דף-הבית הגרמני).
- **`src/reporting/synthesize.py`** - ארכיטקטורת שני-שלבים; **Stage 2 מפיק תלת-לשוניות** (`topic_label_de`/`comparison_text_de` בכל קריאת-נושא, `max_tokens=6000`); Stage 1 בלתי-נגוע.
- **`src/reporting/render.py`** - HTML+PDF **תלת-לשוני**, מראי-מקום, accordion, sticky-nav, מיון-לפי-כיסוי, פוטר, favicon; `build_nav_html`/`footer_hrefs_for`/`CATEGORY_LABELS`/כל תאריכי-ה-UI תלת-לשוניים.
- **`src/publishing/publish.py`** - `docs/` שלם כולל דף-הבית, `about.html`, `archive.html`, `topic.html`, `accessibility.html`, `terms.html` - כולם תלת-לשוניים, כולל ה-JS של דף-הבית (`_HOMEPAGE_JS_TEMPLATE`).
- **`src/common/about_content.py`** - תוכן About משותף ב-3 שפות, כולל גילוי-נאות-AI וסעיף "השפה השלישית: גרמנית".
- **בדיקות**: `tests/test_report_date_rule.py` - 15 טסטים (`python -m unittest discover -s tests -v`), עברו ללא שינוי אחרי כל עבודת-הגרמנית.
- **`scripts/orphan_review.py`** - כלי קבוע, קריאה-בלבד (VERDICT 0 = תקין). **`scripts/backfill_german_translation.py`** - חד-פעמי אך אידמפוטנטי/מתחדש-בבטחה (פיצול-רקורסיבי-אוטומטי אם קבוצת-נושאים נכשלת/חלקית, כולל טיפול בחריגות-API-אמיתיות לא רק תשובות-חלקיות). **`scripts/backfill_report_date.py`** - חד-פעמי, בוצע.
- **מדידת-עלות** - תמחור מאומת: Sonnet 5 = $2/$10, Haiku 4.5 = $1/$5 למיליון טוקן. עלויות מדודות אחרונות: $4.43 (21.9, כולל retry כפול ב-Stage 1), $9.1044 (גיבוי-הארכיון-לגרמנית, 1,475 נושאים).
- **הרצה אוטונומית מלאה** מוכחת 11 פעמים, עם guardrail-בטיחות מכני לפני push ובדיקת-בריאות יזומה לפני Screening.

## קבצים שנוצרו או שונו בשני הסשנים האחרונים (21.9 - 22.9)

- **דוח 21.9**: 8 קבצי-דוח + `archive.html`×5 + `manifest.json`×2 (commit `f574186`); תיעוד ההרצה (commit `39f3b2d`).
- **תלת-לשוניות (commit `d862a33`, 407 קבצים)**: `src/common/db.py` (עמודות `_de`), `src/common/about_content.py` (מפתח `"de"` + סעיף חדש בשלוש השפות), `src/common/geo_taxonomy.py` (`name_de` בכל המיפויים), `src/reporting/synthesize.py` (Stage 2 תלת-לשוני), `src/reporting/render.py` (nav/footer/תאריכים/badges תלת-לשוניים), `src/publishing/publish.py` (about/archive/topic/homepage/accessibility/terms תלת-לשוניים) - כל 30 התאריכים ב-`reports/`+`docs/` בשלוש השפות (HTML+PDF); `scripts/backfill_german_translation.py` חדש; `data/processed/german_backfill_cost_log.json` (לא ב-git - תיעוד-עלות מקומי). `CLAUDE.md` ו-`PROJECT_LOG.md` (סעיף 4.47) עודכנו כחלק מאותו commit.

## החלטות שהתקבלו

ראו "החלטות ארכיטקטוניות קבועות" + "כללי עבודה קבועים" ב-CLAUDE.md. לא משוכפל כאן.

**החלטות ספציפיות-לסשן שלא שייכות ל-CLAUDE.md** (זמניות/הקשריות):
- **21.9 - חריגת-חצות אושרה כחד-פעמית**: הרצה שהתבקשה ל-21.9 אך התחילה בפועל אחרי חצות (00:39, 22.9) - `report_date` עודכן ידנית רק ל-8 השורות הרלוונטיות, מוגן-בבדיקה. **המשתמש ציין במפורש שיידרש כלל-קבוע להרצות-בדיעבד עתידיות** - עדיין לא הוחלט (action item 43).
- **22.9 - תלת-לשוניות**: הארכיון הקיים מתורגם (לא ג'ינרוט-למפרע), מהדוח הבא ואילך ג'ינרוט-מקורי. `Stage 1` בלתי-נגוע במכוון. `index.html`/`accessibility.html`/`terms.html` בגרמנית **הומרו מהיקף בסבב הראשון** (לא הופיעו ברשימת-הקבצים שנמסרה) ואז **נבנו בפועל בסבב שני**, בעקבות בקשה נפרדת - שני הסבבים אושרו ופורסמו יחד ב-`d862a33`.
- מראי-המקום נשארים עם תאריך-ההעלאה של המאמר (לא תאריך-הדוח) - בכוונה, החלטה פתוחה (action item 41).
- 2026-08-31–2026-09-02 (3 ימים) **לא ייסגרו כרגע** - החלטה פתוחה, לא השתנתה.

## עבודה שטרם הושלמה

1. **מנגנון-דילוג ב-`fetch_channel()`** (message_id/hash ידועים-כפולים-או-פגומים) - פותר יחד את action items 24/38/39. עד שקיים: `id=205` (Economist כפול) ממתין להחלטה, ראו "הערות פתיחה".
2. **כלל-קבוע להרצות-בדיעבד שחוצות חצות** (action item 43, חדש 21.9) - טופל פעם אחת כחריגה-ידנית-מאושרת. המשתמש ציין שיישנה. אפשרויות פתוחות: שעת-חיתוך קבועה בקוד (למשל לפני 03:00 מקומי → יום קודם), פרמטר `--report-date` מפורש ב-`fetch.py`, או להישאר ידני-עם-אישור.
3. **שם הסרט התיעודי** (action item 40) - "NAZA" בלטינית בדוח 19.9 העברי, שלוש פעמים באיות שונה. עדיין לא תוקן.
4. **תאריך המראי-מקום** (action item 41) - להשאיר תאריך-העלאה, או להציג תאריך-דוח - החלטת-בעלים פתוחה.
5. **תקרת שלב 1 של Synthesize** (action item 42) - לפני ריצת-backlog לספור מאמרים צפויים ולהחליט על פיצול מראש.
6. **2 עמודי WSJ Magazine (file 199, עמודים 39/63) נשארו `pending`** - יטופלו בהרצת Analysis-לקובץ-הזה-בנפרד, עלות זניחה.
7. **פער 31.8-2.9 (3 ימים)** - לא מזוהה ע"י `compute_scan_limit()` (action item 35).
8. **`analyze.py`'s `NEWSPAPER_LANGUAGES`** מכסה רק 4/10 (action item 36). **טביעת-אצבע-שחיתות שנייה** בלי tradingref.com (action item 37) - לא הוכרעה.
9. **סקריפטי בדיקת-הבריאות ושחזור-העלות** עדיין נכתבים מחדש בכל הרצה ונמצאים רק בסקראצ'פד - לשמור ב-`scripts/`. ואז action item 33 (allowlist לוגים, timeout 10 דקות, בדיקת GTK3) כתנאי-סף להרצה מתוזמנת.
10. Prompt Caching ברמת-ארגון; GitHub Actions לתזמון יומי. מפה נגישה-לקורא-מסך אך לא ניתנת-למיקוד-מקלדת (הוצהר בהצהרת-הנגישות). סינון-AND-משולב אינטראקטיבי בדף הבית (קיים דרך `topic.html`).

## בעיות ידועות (עדיין רלוונטיות בלבד - ראו PROJECT_LOG להיסטוריה המלאה)

- **"0 שורות" דו-משמעי** ב-`article_countries`/`article_conflict_zones` - מגבלה מודעת, לא באג.
- **`published_at` נשאר זמן-העלאה בלבד** ונסחף מיום-הכיסוי - לא משפיע על שיוך-דוחות, אך עדיין קובע את `compute_scan_limit()` ואת תאריך מראי-המקום.
- **WSJ Magazine מזוהה כ-Wall Street Journal** ועובר Screening (5% רלוונטי) - אותה שאלת-מוסף כמו Guardian Weekly/USA Today Sports Weekly.
- **מספור-עמודים הפוך ב-PDF העברי** - bidi, לא תוקן בכוונה. **סקשן-רפאים ריק אפשרי ב-Synthesis** (נצפה פעם אחת, בנוסף למקרה חדש ב-21.9 - ראו PROJECT_LOG 4.46). **article יחיד חשוד כ-false-positive** (`article_id=254`, "Prince Harry").
- **תקלת "רוב-לא-משויך" בשלב 1 של Synthesize נצפתה פעמיים** (18.9, 21.9) - שתי הפעמים נפתרו ע"י חזרה-על-הקריאה, לא ע"י שינוי-קוד; הסיבה עדיין לא ידועה. רק 2 מקרים - לא מספיק לקבוע דפוס.

## הצעד הבא המומלץ

**הפייפליין יציב ואין חסימה טכנית.** סדר-עדיפויות:

1. **בהרצה הבאה - לוודא שהכלל התלת-לשוני עובד בקליטה אמיתית**: דוח חדש אמור לקבל `topic_label_de`/`comparison_text_de` ישירות מ-Stage 2, בלי צורך ב-backfill. לוודא ש-`render.py` לא מדלג על הגרסה הגרמנית (הוא מדלג אם השדות `NULL`).
2. **מנגנון-דילוג ב-`fetch.py`** (#1 למעלה) - הכפול `id=205` עדיין ממתין.
3. להחליט על כלל-קבוע להרצות-בדיעבד (#2) - יישנה בוודאות.
4. להחליט על #3 (שם הסרט) ו-#4 (תאריך ציטוט) - החלטות-בעלים קצרות.
5. לשמור את סקריפטי בדיקת-הבריאות ב-`scripts/` (#9), ואז action item 33 כתנאי-סף להרצה-מתוזמנת, ואחר-כך Prompt Caching ברמת-ארגון + GitHub Actions.

**תזכורת קבועה מ-CLAUDE.md**: כל שינוי עתידי במבנה/נתיבי-פלט של `render.py`/`publish.py` מחייב regeneration מלא של הארכיון **בשלוש השפות** + `git status` מפורש לפני commit. אם השינוי לא נוגע בתוכן-שמופיע-ב-PDF (כמו קישור-פוטר בלבד - ראו `shared_chrome_css()`'s `@media print`), אפשר לדלג על רינדור-PDF (`render.WEASYPRINT_AVAILABLE = False` זמנית) ולחסוך זמן רב - אבל רק אחרי בדיקה מפורשת בקוד שאכן שום-דבר-שמשתנה לא מופיע בהדפסה, לא בהנחה.

## סשן אחרון

**תאריך:** 2026-09-21 עד 2026-09-22 (רצף ישיר: הרצה אוטונומית על 21.9, ואז הרחבת-תלת-לשוניות גדולה)

**תיאור:** (1) הרצה אוטונומית מלאה על 21.9 - חצתה חצות (התחילה בפועל ב-00:39 של 22.9), טופלה כחריגה-חד-פעמית-מאושרת; Stage 1 של Synthesize נכשל פעמיים ברצף (92% לא-משויך) לפני שהצליח בניסיון שלישי - $4.43 סה"כ, commit `f574186`+`39f3b2d`. (2) **בירור-עלות יזום** להוספת גרמנית - מדידת-עוגן אחת (תרגום דוח 13.9) העלתה $0.4763 (כולל 2 ניסיונות כושלים שדווחו בשקיפות), והובילה להחלטה: גיבוי-ארכיון מתורגם + ג'ינרוט-מקורי-קבוע מכאן ואילך. (3) **ביצוע**: בדיקת-גופן (3 שכבות-אימות, עברה), About בשלוש שפות, גיבוי 30 דוחות ($9.1044, כולל תקלת-API-חולפת שנתפסה ותוקנה באמצע), שינוי-ארכיטקטורה ל-Synthesize (Stage 2 תלת-לשוני, Stage 1 בלתי-נגוע - תשובה מפורשת לשאלת-סיכון של המשתמש), הרחבת render.py/publish.py לדוחות+about+archive+topic (סבב ראשון), ואז דף-הבית+accessibility+terms (סבב שני, בעקבות בקשה נפרדת שכללה גם ניסוח-מחדש של About בשלוש השפות ואישור-מפורש). כל שלב אומת לפני שהמשתמש אישר commit - כולל pdfplumber round-trip על PDF גרמני אמיתי, ספירת-קישורי-מתג-שפה, ואימות-ידני של המשתמש. **commit אחד ענק** (`d862a33`, 407 קבצים) אחרי אישור מפורש; **תוך-כדי** התגלה ותוקן פער-תיעוד ב-CLAUDE.md עצמו (עדיין אמר "שתי שפות", וקוד הפנה לסעיף-CLAUDE.md שלא נכתב בפועל). אומת-חי אחרי הפריסה: 200 בשלוש השפות, כולל בדיקה מפורשת שזה לא cache-ישן (תוכן-בפועל, לא רק קוד-תגובה).

**דפוס חוזר בפרויקט**: "לתעד-קודם-לפעול, לא להניח שהתיעוד קיים". קוד שנכתב הפנה ל-CLAUDE.md-סעיף-שעדיין-לא-נכתב - נתפס רק כי הביטוי המדויק (`grep`) נבדק לפני commit, לא הונח שקיים כי "בטח כתבתי את זה במקום אחד". אותו עיקרון-בדיקה שכבר חזר כמה פעמים בפרויקט (למשל 4.42: "האם ה-Economist באמת תרם לדוח" - נבדק רק בדיעבד).
