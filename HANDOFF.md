# Project Handoff

## מצב הפרויקט הנוכחי

**חמשת שלבי ה-Pipeline (Ingestion→Extraction→Analysis→Reporting→Publishing) יציבים ומוכחים על עשרות הרצות אמיתיות**, כולל מספר מבחני-בשלות אוטונומיים-לגמרי ברצף (30.8, ואז 11.9 עד 16.9 ברצף - ראו "סשן אחרון"). הארכיון כולל **25 תאריכים** (2026-08-20 עד 2026-09-16, עם פער-לא-סגור מודע ב-31.8-2.9 - ראו "בעיות ידועות") בשתי שפות מלאות, שני פורמטים (HTML+PDF), תחת `reports/{he,en}/` ו-`docs/{he,en}/`. **האתר חי**: https://geopolitics.meirshemesh.com.

**מקורות MVP פעילים בפועל: 9** (הרשימה המקורית של 10, בתוספת Washington Post, בניכוי NYT המושעה-לצמיתות-בכל-מהדורה). `compute_scan_limit()` מחשב דינמית את גודל-חלון-הסריקה של Ingestion - **אושר-בפועל שוב ושוב** (13.9-16.9) שהוא חוזר לגודל-בסיס (200) ומסתיים נקי; הבריחה-לתאריכים-עתידיים שנצפתה פעם אחת (11.9→12.9) הייתה תופעה חד-פעמית של אותו מעבר-ספציפי, לא באג-קבוע.

**האתר עומד עכשיו גם בחבילת-ציות רגולטורי בסיסית** (נגישות/גילוי-נאות-AI/תנאי-שימוש - ראו "מה הושלם" ו-PROJECT_LOG 4.38) - `accessibility.html`/`terms.html` חדשים בכל שפה, פוטר-משותף בכל 5 סוגי-העמוד, ותיקוני-WCAG-2.0-AA שנמדדו בפועל (לא הונחו).

**דף הבית (v2)**: מפה אינטראקטיבית, ציר-זמן, כרטיס-דוח-אחרון, צ'יפי-אזור+קונפליקט - הכל קורא מ-`manifest.json` בזמן-ריצה. `topic.html`/`archive.html`/`about.html`/`accessibility.html`/`terms.html` זמינים בכל שפה. **דוחות יומיים** כוללים: קישור-הורדת-PDF, תיבת-ציטוט-מדויקת, כפתור-שיתוף-קישור, תפריט-קטגוריות-דביק עם accordion, מיון-נושאים-לפי-רוחב-כיסוי אמיתי, ופוטר עם קישורי-ציות - ראו CLAUDE.md לפרטים המלאים, לא משוכפל כאן.

## מה הושלם (יכולות בשלות, לא יומן-שינויים)

- **`src/common/db.py`** - מקור-אמת יחיד לסכמה, מיגרציה אוטומטית.
- **`src/ingestion/fetch.py`** - `guess_newspaper()` (9 מקורות פעילים, NYT מושעה-לצמיתות-כל-מהדורה, Economist רק Web-Edition, SZ עם וריאנט-כתיב ללא-ניקוד), `compute_scan_limit()`, heartbeat+flood-wait logging.
- **`src/extraction/extract.py`**, **`src/analysis/screen.py`+`analyze.py`** (Haiku רשת-רחבה → Sonnet ניתוח-מעמיק+תיוג-גיאוגרפי), **`geo_tag_backfill.py`**.
- **`src/common/geo_taxonomy.py`** - 94 מדינות/8 אזורים/3 קונפליקטים.
- **`src/reporting/synthesize.py`** - ארכיטקטורת שני-שלבים, רשת-ביטחון כפולה.
- **`src/reporting/render.py`** - HTML+PDF דו-לשוני, citations, accordion, sticky-nav, מיון-לפי-כיסוי, **פוטר-משותף חדש** (`build_footer_html`/`shared_chrome_css` - כולל `:focus-visible` גלובלי).
- **`src/publishing/publish.py`** - `docs/` שלם: דוחות, `archive.html`, `about.html`, `topic.html`, **`accessibility.html`/`terms.html` חדשים**, דף-בית, `manifest.json`.
- **`src/common/about_content.py`** - נכתב מחדש במלואו (10.9); עודכן להחלפת NYT←Washington Post (12.9); **נוסף סעיף גילוי-נאות-AI חדש** (16.9, אחרי "שיטת העבודה").
- **חבילת-ציות רגולטורי** - `accessibility.html`/`terms.html` (ת"י 5568/WCAG 2.0 AA), בלי זכויות-יוצרים/אנליטיקס-חדש (אושר-בפועל שהאתר נקי מקוד-מעקב). ביקורת-WCAG מדודה (לא הונחה) מצאה ותיקנה: קישור-לוגו עם שם-נגישות שבור, מפת-SVG בלי `aria-labelledby` עצמי, ו-3 כשלי-ניגודיות אמיתיים (תג-קטגוריה, צ'יפ-נבחר במצב-כהה, תאי-ציר-זמן בעצימות-גבוהה).
- **מדידת-עלות רטרואקטיבית** - שחזור `count_tokens` ל-Screening/Analysis (Analysis כ"גבול-תחתון"); Synthesize משתמש בנתוני-usage-אמיתיים.
- **הפרדת-עלות בין ריצות** - Screening/Analysis (חסרי-סינון-לפי-תאריך) משויכים ל"סבב" הרחב; Synthesize (יש לו `--date`) תמיד ספציפי-ליום.

## קבצים מרכזיים (תפקיד נוכחי)

ראו "מבנה הקבצים המרכזיים" ב-CLAUDE.md לפירוט מלא ומעודכן - לא משוכפל כאן.

## החלטות שהתקבלו

ראו "החלטות ארכיטקטוניות קבועות" + "כללי עבודה קבועים" ב-CLAUDE.md לרשימה המלאה. לא משוכפל כאן.

**החלטות ספציפיות-לסשן שלא שייכות ל-CLAUDE.md** (זמניות/הקשריות):
- Washington Post נבחר כתחליף ל-NYT בעמוד About **באותו מיקום פוליטי בערך** - החלפה-ישירה בלי שינוי-ניסוח (אישור מפורש).
- כתובת-יצירת-קשר בהצהרת-הנגישות (`meir@meirshemesh.com`) ניתנה במפורש ע"י בעל-האתר - לא הומצאה.
- תוכן-קליטה (Washington Post ל-14.9, LA Times שני ל-13.9) שנקלט תוך-כדי backfill של 15.9 **לא הוכל** בדוחות 13.9/14.9 הקיימים - ממתין להחלטת המשתמש אם לפתוח-מחדש (עדיין פתוח, ראו "עבודה שטרם הושלמה").
- 2026-08-31–2026-09-02 (3 ימים) **לא ייסגרו כרגע** - עדיין החלטה פתוחה, לא השתנתה.

## עבודה שטרם הושלמה

1. **החלטה ממתינה: לפתוח-מחדש 13.9/14.9?** תוכן-catch-up (Washington Post ל-14.9, LA Times שני-ושונה ל-13.9) כבר עבר Extraction/Screening/Analysis במלואו ויושב ב-DB, אך לא הוכל בדוחות הקיימים (עקרון "לא לפתוח ארכיון-יציב בלי אישור מפורש"). לא הוחלט עדיין.
2. **פער 31.8-2.9 (3 ימים)** - לא מזוהה אוטומטית ע"י `compute_scan_limit()` (PROJECT_LOG action item 35).
3. **`analyze.py`'s own `NEWSPAPER_LANGUAGES`** מכסה רק 4/10 מהרשימה המקורית - אינרטי-מאומת (action item 36).
4. **פתרון-כללי לגזירת תאריך-שער משם-קובץ** (במקום `published_at`) - **6 מקרים כעת, 4 מקורות שונים** - עדיין לא ממומש למרות שחוזר על עצמו הרבה.
5. **טביעת-אצבע-שחיתות שנייה, בלי tradingref.com** (0 תווים+תמונה) - action item 37, עדיין לא-הוכרע אם חד-פעמי או שיטתי (מובחן מ-action item 38 למטה - זו שאלה על **סוג-השחיתות**, לא על **חזרה-של-אותה-הודעה**).
6. **פער-תהליך: מחיקת-DB של קובץ פגום לא מונעת הורדה-חוזרת בתוך חלון-הסריקה** - action item 38, **אושש פעמיים** (15.9, 16.9) עם `message_id` זהה מאומת בשתי הפעמים - אותה הודעה ממש (message_id 29577), לא מקרים נפרדים. עדיין לא ממומש מנגנון-דילוג.
7. allowlist-לוגים-ידועים + timeout-10-דקות-לאימות-פריסה (PROJECT_LOG action item 33).
8. Prompt Caching ברמת-ארגון; GitHub Actions לתזמון יומי; טסטים (`tests/` ריק).
9. מפה נגישה-לקורא-מסך אך עדיין לא ניתנת-למיקוד-מקלדת - הוצהר-בכנות בהצהרת-הנגישות עצמה כפער-ידוע, לא הוסתר.
10. סינון-AND-משולב אינטראקטיבי בדף הבית עצמו (קיים דרך `topic.html`, לא כ-UI על העמוד).

## בעיות ידועות (עדיין רלוונטיות בלבד - ראו PROJECT_LOG להיסטוריה המלאה)

- **"0 שורות" דו-משמעי** ב-`article_countries`/`article_conflict_zones` - מגבלה מודעת, לא באג.
- **`published_at` לשבועונים/מהדורות-סופ"ש** לא תמיד תואם את יום-הכיסוי הנומינלי - נצפה 6 פעמים, 4 מקורות.
- **מספור-עמודים הפוך ב-PDF העברי** - bidi, לא תוקן בכוונה.
- **סקשן-רפאים ריק אפשרי ב-Synthesis** (נצפה פעם אחת) - ממתין להצטברות-מקרים.
- **article יחיד חשוד כ-false-positive** (`article_id=254`, "Prince Harry") - רעש-ידוע-בודד.
- **קובץ-WSJ פגום (message_id 29577, מקורי מ-14.9) ממשיך לחזור** בכל הרצה שבה הוא עדיין בתוך חלון-הסריקה (נצפה 3 פעמים: 14.9-16.9) - מוסר בכל פעם לפי הפרוטוקול, לא יעצור-מעצמו עד שיצא מהחלון.

## הצעד הבא המומלץ

**הפייפליין יציב לתפעול-שוטף, כולל הרצות-אוטונומיות-לגמרי חוזרות (6 ברצף: 11.9-16.9)** - אין חסימה טכנית. סדר-עדיפויות:

1. **להחליט על פתיחה-מחדש של 13.9/14.9** (#1 למעלה) - תוכן כבר מוכן ב-DB, רק ממתין להחלטה.
2. מנגנון-דילוג ל-message_id-ים ידועים-פגומים ב-`fetch_channel()` (#6 למעלה) - חוסך זמן-הורדה חוזר על תוכן שכבר ידוע כפגום.
3. פתרון-כללי לתאריך-שער-משם-קובץ ב-`fetch.py` (#4 למעלה) - 6 מקרים, 4 מקורות - חוזר מספיק כדי להצדיק קוד כללי.
4. להמשיך תפעול-שוטף עד שהאמון יבשיל לגמרי, ואז לגשת ל-#7 (allowlist+timeout) כתנאי-סף להרצה-מתוזמנת-אמיתית.
5. Prompt Caching ברמת-ארגון + GitHub Actions.

**תזכורת קבועה מ-CLAUDE.md**: כל שינוי עתידי במבנה/נתיבי-פלט של `render.py`/`publish.py` מחייב regeneration מלא של הארכיון + `git status` מפורש לפני commit.

## סשן אחרון

**תאריך:** 2026-09-16

**תיאור:** המשך ישיר מה-`/handoff` הקודם (`f77b18c`, שכיסה 11.9-14.9 בפירוט - ראו היסטוריה מלאה שם/ב-PROJECT_LOG אם נדרש, לא משוכפל כאן). כלל:

1. **הרצה אוטונומית ל-15.9** - Ingestion הביא 11 קבצים: 8 באמת ל-15.9, ו-3 catch-up לימים כבר-מפורסמים (Washington Post ל-14.9, LA-Times-שני-ושונה ל-13.9 - **לא** כפילות, hash/גודל שונים, ו"אותו-קובץ-WSJ-פגום-מ-14.9" שחזר). **ממצא מתודולוגי חשוב**: לפני שנרשם כ"מקרה שני" של כשל-WSJ, נבדק ונמצא שמחיקת-DB לא מונעת הורדה-חוזרת בתוך חלון-הסריקה - action item חדש (38). תוכן-ה-catch-up הוכל ב-DB אך **לא** בדוחות הקיימים (ממתין להחלטה). `commit` (`cff9c6c`) + PROJECT_LOG (`fef5675`).
2. **חבילת-ציות רגולטורי** (נגישות/גילוי-נאות-AI/תנאי-שימוש) - שלב-0 בדק בפועל שאין קוד-מעקב באתר (אושר, לא הונח). ביקורת-WCAG 2.0 AA אמיתית (סוכן-משנה חילץ ערכים מדויקים, יחסי-ניגודיות חושבו בנוסחה המלאה) מצאה ותיקנה 2 כשלי-שם-נגישות ו-3 כשלי-ניגודיות אמיתיים. נבנו `accessibility.html`/`terms.html` + פוטר-משותף חדש בכל 5 סוגי-העמוד + סעיף-גילוי-נאות ב-`about_content.py`. כל 24 התאריכים אז נבנו-מחדש, אומת ב-6 צילומי-מסך + בדיקה-תוכנתית של קישורי-הפוטר בכל סוג-עמוד. `commit` (`49e7643`).
3. **הרצה אוטונומית ל-16.9** - יום מלא (8/8 מקורות אפשריים). קובץ-ה-WSJ-הפגום חזר **בשלישית** - הפעם `message_id` הושווה במפורש ואומת **זהה** (29577) לזה שנרשם קודם - אישוש חד-משמעי (לא רק סביר) שזו אותה הודעה, לא שלוש תקלות נפרדות. אימות-חי כלל בדיקה מפורשת שהפוטר החדש מופיע גם על דוח-חדש-לגמרי (לא רק עמודים-שכבר-עודכנו) וששני קישורי-הפוטר מחזירים 200 בפועל. `commit` (`3e65092`) + PROJECT_LOG (`0ba082e`).

**דפוס חוזר בסשן**: אימות-לפני-מסקנה חזר שוב - "האם זה מקרה-שני של כשל" הפך ל"האם זו אותה הודעה" ברגע ש-`message_id` נבדק בפועל (לא הונח), ומנע רישום-שגוי של ראיה-לכאורה-שיטתית שהייתה בעצם אותה תקלה חוזרת. עיקרון עקבי לאורך כל הפרויקט: מדידה, לא הנחה - גם כשההנחה (`"עוד מקרה של אותו סוג באג"`) נשמעת סבירה ביותר.
