# Project Handoff

## מצב הפרויקט הנוכחי

**חמשת שלבי ה-Pipeline (Ingestion→Extraction→Analysis→Reporting→Publishing) יציבים ומוכחים על עשרות הרצות אמיתיות**, כולל כמה מבחני-בשלות אוטונומיים-לגמרי (30.8, ואז 11.9/12.9/13.9/14.9 ברצף - ראו "סשן אחרון"). הארכיון כולל **23 תאריכים** (2026-08-20 עד 2026-09-14, עם פער-לא-סגור מודע ב-31.8-2.9 - ראו "בעיות ידועות") בשתי שפות מלאות, שני פורמטים (HTML+PDF), תחת `reports/{he,en}/` ו-`docs/{he,en}/`. **האתר חי**: https://geopolitics.meirshemesh.com.

**מקורות MVP פעילים בפועל: 9** (הרשימה המקורית של 10, בתוספת Washington Post, בניכוי NYT המושעה). NYT **מושעה לצמיתות בכל מהדורה** (לא רק International כפי שהיה קודם) - ניסיון מעבר למהדורה-הביתית ב-12.9 נבדק-תוכן-בפועל ונמצא פגום באותה מידה בדיוק. **The Washington Post נוסף** במקומו כמקור-MVP חדש (נבדק-תוכן ואומת תקין לפני הוספה) - ראו CLAUDE.md ו-PROJECT_LOG 4.34. `compute_scan_limit()` מחשב דינמית את גודל-חלון-הסריקה של Ingestion; **אושר-בפועל פעמיים** (13.9, 14.9) שהוא חוזר לגודל-בסיס (200) ומסתיים נקי אחרי שיום אחד עבר תקין - הבריחה-לתאריכים-עתידיים שנצפתה ב-11.9→12.9 הייתה תופעה חד-פעמית של אותו מעבר, לא באג-קבוע בפונקציה עצמה.

**דף הבית (v2)**: מפה אינטראקטיבית, ציר-זמן, כרטיס-דוח-אחרון, צ'יפי-אזור+קונפליקט - הכל קורא מ-`manifest.json` בזמן-ריצה. `topic.html`/`archive.html`/`about.html` זמינים בכל שפה. **דוחות יומיים** כוללים: קישור-הורדת-PDF, תיבת-ציטוט-מדויקת, כפתור-שיתוף-קישור, תפריט-קטגוריות-דביק עם accordion, ומיון-נושאים-לפי-רוחב-כיסוי אמיתי (`section_coverage()`) - ראו CLAUDE.md לפרטים המלאים, לא משוכפל כאן.

## מה הושלם (יכולות בשלות, לא יומן-שינויים)

- **`src/common/db.py`** - מקור-אמת יחיד לסכמה, מיגרציה אוטומטית.
- **`src/ingestion/fetch.py`** - `guess_newspaper()` (9 מקורות פעילים - הרשימה המקורית של 10 + Washington Post, בניכוי NYT המושעה-לצמיתות-כל-מהדורה; Economist רק Web-Edition, SZ עם וריאנט-כתיב ללא-ניקוד), `compute_scan_limit()`, heartbeat+flood-wait logging.
- **`src/extraction/extract.py`**, **`src/analysis/screen.py`+`analyze.py`** (Haiku רשת-רחבה → Sonnet ניתוח-מעמיק+תיוג-גיאוגרפי), **`geo_tag_backfill.py`**.
- **`src/common/geo_taxonomy.py`** - 94 מדינות/8 אזורים/3 קונפליקטים.
- **`src/reporting/synthesize.py`** - ארכיטקטורת שני-שלבים, רשת-ביטחון כפולה.
- **`src/reporting/render.py`** - HTML+PDF דו-לשוני, citations, accordion, sticky-nav, מיון-לפי-כיסוי.
- **`src/publishing/publish.py`** - `docs/` שלם: דוחות, `archive.html`, `about.html`, `topic.html`, דף-בית, `manifest.json`.
- **`src/common/about_content.py`** - נכתב מחדש במלואו (10.9); עודכן שוב (12.9) להחלפת New York Times ← Washington Post ברשימת-המקורות-האמריקאיים (אישור מפורש, ניסוח לא-שונה חוץ מהחלפת-השם).
- **מדידת-עלות רטרואקטיבית** - שחזור `count_tokens` ל-Screening/Analysis (Analysis כ"גבול-תחתון", חסר טוקני-חשיבה-אדפטיבית); Synthesize משתמש בנתוני-usage-אמיתיים שהוא כבר מדפיס בעצמו.
- **הפרדת-עלות בין ריצות**: כשמקור-חדש (Washington Post) עובר backfill שחוצה כמה תאריכים, Screening/Analysis (חסרי-יכולת-סינון-לפי-תאריך) משויכים ל"סבב" כולו; Synthesize (יש לו `--date`) תמיד ספציפי-ליום. תועד ונשמר במפורש כדי לא לספור עלות פעמיים בין ימים סמוכים (ראו PROJECT_LOG 4.34-4.36).

## קבצים מרכזיים (תפקיד נוכחי)

ראו "מבנה הקבצים המרכזיים" ב-CLAUDE.md לפירוט מלא ומעודכן - לא משוכפל כאן.

## החלטות שהתקבלו

ראו "החלטות ארכיטקטוניות קבועות" + "כללי עבודה קבועים" ב-CLAUDE.md לרשימה המלאה. לא משוכפל כאן.

**החלטות ספציפיות-לסשן שלא שייכות ל-CLAUDE.md** (זמניות/הקשריות):
- Washington Post נבחר כתחליף ל-NYT בעמוד About **באותו מיקום פוליטי בערך** (מרכז-שמאל/מיינסטרים) - הוצג למשתמש כשאלת-איזון-פוליטי מפורשת, והוא בחר החלפה-ישירה בלי שינוי-ניסוח.
- קובץ WSJ הפגום מ-14.9 (0 תווי-טקסט + תמונה בודדת בכל 40 עמודים) נמחק (DB+דיסק) לפי הפרוטוקול הקבוע - **לא** תועד כ"אותה תופעה כמו tradingref.com" למרות הדמיון, כי חתימת-הטקסט שונה (0 תווים, לא 14) - זו טביעת-אצבע נבדלת, במעקב בלבד (action item 37), לא פעולה.
- 2026-08-31–2026-09-02 (3 ימים) **לא ייסגרו כרגע** - עדיין החלטה פתוחה, לא השתנתה.

## עבודה שטרם הושלמה

1. **פער 31.8-2.9 (3 ימים)** - לא מזוהה אוטומטית ע"י `compute_scan_limit()` (PROJECT_LOG action item 35). לא הוחלט אם שווה סריקה-ממוקדת-ידנית.
2. **`analyze.py`'s own `NEWSPAPER_LANGUAGES`** (לא זה שב-`render.py`!) מכסה רק 4/10 מהרשימה המקורית (טרם עודכן ל-Washington Post) - אינרטי-מאומת (action item 36).
3. `fetch.py` לא רושם שמות-קבצים שנדחו כ-`skipped_not_mvp`.
4. **פתרון-כללי לגזירת תאריך-שער משם-קובץ** (במקום `published_at`) - נצפה **שוב** (Economist, ה-Web Edition "_1209" שהועלה ב-11.9 ושויך ל-11.9 במקום ל-12.9 - תוכן לא אבד, רק שויך ליום הלא-נכון) - עדיין לא ממומש, למרות שכבר חוזר על עצמו כמה פעמים.
5. **טביעת-אצבע-שחיתות שנייה, בלי tradingref.com** (0 תווים+תמונה, נצפתה אצל WSJ ב-14.9 - ראו "החלטות") - action item 37, לעקוב אם חוזרת.
6. allowlist-לוגים-ידועים + timeout-10-דקות-לאימות-פריסה (תנאי-סף לסוכן-מתוזמן-אמיתי-בלי-פיקוח - PROJECT_LOG action item 33).
7. Prompt Caching ברמת-ארגון; GitHub Actions לתזמון יומי; טסטים (`tests/` ריק).
8. מפה נגישה-לקורא-מסך אך עדיין לא ניתנת-למיקוד-מקלדת - הרחבה נפרדת, לא כלולה בתיקון-הנגישות הקיים.
9. סינון-AND-משולב אינטראקטיבי בדף הבית עצמו (קיים דרך `topic.html`, לא כ-UI על העמוד).

## בעיות ידועות (עדיין רלוונטיות בלבד - ראו PROJECT_LOG להיסטוריה המלאה)

- **"0 שורות" דו-משמעי** ב-`article_countries`/`article_conflict_zones` - מגבלה מודעת, לא באג.
- **`published_at` לשבועונים/מהדורות-סופ"ש** לא תמיד תואם את יום-הכיסוי הנומינלי - נצפה **6 פעמים** כעת, 4 מקורות שונים (WSJ Weekend, Telegraph, Guardian, Economist) - ראו "עבודה שטרם הושלמה" #4.
- **מספור-עמודים הפוך ב-PDF העברי** - bidi, לא תוקן בכוונה.
- **סקשן-רפאים ריק אפשרי ב-Synthesis** (נצפה פעם אחת) - ממתין להצטברות-מקרים.
- **article יחיד חשוד כ-false-positive** (`article_id=254`, "Prince Harry") - רעש-ידוע-בודד.
- **WSJ, 14.9**: קובץ-חדש פגום מקצה-לקצה (טביעת-אצבע שונה מ-tradingref.com) - הפעם הראשונה שנצפה אצל מקור זה. הוסר, לא תוקן, במעקב.

## הצעד הבא המומלץ

**הפייפליין יציב לתפעול-שוטף, כולל הרצות-אוטונומיות-לגמרי חוזרות (4 ברצף: 11.9-14.9)** - אין חסימה טכנית. סדר-עדיפויות:

1. פתרון-כללי לתאריך-שער-משם-קובץ ב-`fetch.py` - **6 מקרים כעת, 4 מקורות** - חוזר מספיק כדי להצדיק קוד כללי במקום עוד תיקון-נקודתי בכל פעם שמישהו שם לב.
2. להמשיך תפעול-שוטף (אוטונומי, כפי שהוכח ב-4 הימים האחרונים) עד שהאמון יבשיל לגמרי, ואז לגשת ל-#6 למעלה (allowlist+timeout) כתנאי-סף להרצה-מתוזמנת-אמיתית.
3. להחליט אם/מתי לסגור את פער 31.8-2.9 - לא דחוף, אך פתוח.
4. Prompt Caching ברמת-ארגון + GitHub Actions.

**תזכורת קבועה מ-CLAUDE.md**: כל שינוי עתידי במבנה/נתיבי-פלט של `render.py`/`publish.py` מחייב regeneration מלא של הארכיון + `git status` מפורש לפני commit.

## סשן אחרון

**תאריך:** 2026-09-14

**תיאור:** סשן ארוך, המשך ישיר מהסשן הקודם (שהסתיים ב-`b88cb35`, תיקון-SZ ל-10.9). כלל:

1. **`/handoff` על הסשן הקודם** - CLAUDE.md/HANDOFF.md עודכנו, `commit` (`c1bc046`).
2. **הרצה אוטונומית-לגמרי ל-11.9** (כמו מבחן 30.8) - 0 כשלים, 8 מקורות, 246 מאמרים, 109 נושאים. `commit` (`4cdaae6`) + עדכון-PROJECT_LOG נפרד (`6ce1f09`).
3. **מעבר NYT→Washington Post (12.9)** - ניסיון ראשוני לעבור ל-NYT-מהדורה-ביתית נבדק-תוכן-בפועל (pdfplumber, לא רק שם-קובץ) ונמצא **פגום באותה מידה** כמו הבינלאומית (6/6→עוד 2 קבצים). Washington Post נבדק כאלטרנטיבה, אומת-תקין, נוסף. תקלת-ביניים (about.html נאפה עם טקסט-זמני-שגוי) נתפסה ותוקנה **לפני** הצגה למשתמש. שני commits נפרדים לפי בקשה: דוח+קוד (`561784b`), עמוד-About (`49d6be2`).
4. **הרצה אוטונומית ל-13.9** - מקרה-מבחן ל"עבודה כבר-שולמה מסבב-קודם": Washington Post's backfill כבר כיסה את 13.9 בסבב הקודם - Screening/Analysis היו no-op מאומת (לא הנחה), רק Synthesize+Render+Publish רצו בפועל. `commit` (`fa5b3e4`).
5. **בירור-אבחון (Economist/Der Spiegel חסרים מ-12.9/13.9)** - התברר: Der Spiegel פשוט לא פורסם (לא באג); Economist **כן** פורסם אך עם `published_at` יום-לפני (11.9) - התוכן לא אבד, רק שויך ליום הלא-נכון. דיווח בלבד, לא תוקן.
6. **הרצה אוטונומית ל-14.9** - קובץ-WSJ חדש נמצא פגום מקצה-לקצה (0 תווים+תמונה בכל 40 עמודים) - **הפעם הראשונה שנצפה אצל WSJ**. הוסר לפי הפרוטוקול. `commit` (`c22da7d`).
7. **תיעוד-רטרואקטיבי** (`e79275f`, `434bd9d`) - PROJECT_LOG לא עודכן בזמן-אמת אחרי שלב 3-6 בפועל; נתפס ותוקן: §4.34 (NYT/WaPo), §4.35 (13.9), §4.36 (WSJ), טבלת-מעקב עודכנה ל-14.9, action item 27 נסגר סופית.
8. **action item 37** (`6ccd6b2`) - המשתמש ביקש לתעד את חתימת-השחיתות-השונה של WSJ כנקודת-מעקב מפורשת (לא לפעול), כדי לא לשכוח אם היא תחזור.

**דפוס חוזר בסשן**: כמה פעמים "תבדוק שוב"/"תבדוק תוכן בפועל, לא רק שם-קובץ" לפני אישור - בכל מקרה זה מנע החלטה שגויה (המעבר ל-NYT-ביתית היה נכשל בלי הבדיקה) או חשף פער-תיעוד אמיתי (PROJECT_LOG לא עודכן בזמן-אמת). כשמריצים כמה ימים ברצף באופן אוטונומי, קל לדחות תיעוד "לסוף" ואז לשכוח - ראוי לעדכן PROJECT_LOG **תוך-כדי** כל הרצה בעתיד, לא רק ב-`/handoff`.
