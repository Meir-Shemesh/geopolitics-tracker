# Project Handoff

## מצב הפרויקט הנוכחי

**חמשת שלבי ה-Pipeline (Ingestion→Extraction→Analysis→Reporting→Publishing) יציבים ומוכחים על עשרות הרצות אמיתיות**, כולל מבחן-בשלות אוטונומי-לגמרי אחד (30.8.2026) ומספר הרצות-שגרה-רגילות אחריו. הארכיון כולל **19 תאריכים** (2026-08-20 עד 2026-09-10, עם פער-לא-סגור מודע ב-31.8-2.9 - ראו "בעיות ידועות") בשתי שפות מלאות, שני פורמטים (HTML+PDF), תחת `reports/{he,en}/` ו-`docs/{he,en}/`. **האתר חי**: https://geopolitics.meirshemesh.com (גם `github.io` הישן ממשיך לעבוד, מפנה 301).

עשרה מקורות MVP; **NYT International מושעה לחלוטין** מהורדה (`guess_newspaper()` מחזיר `None` תמיד - פגום 6/6 גיליונות שנצפו). `compute_scan_limit()` מחשב דינמית את גודל-חלון-הסריקה של Ingestion לפי הפער מאז ההורדה האחרונה - לא קבוע (200) יותר, אך **לא** מזהה חורים לא-רציפים באמצע ההיסטוריה (ראו "בעיות ידועות").

**דף הבית (v2)**: מפה אינטראקטיבית (SVG מוטמע, מתויג לנגישות בזמן-build - ראו CLAUDE.md), ציר-זמן, כרטיס-דוח-אחרון, צ'יפי-אזור+קונפליקט - הכל קורא מ-`manifest.json` בזמן-ריצה. `topic.html` (עמוד-תצוגה נושאי חוצה-דוחות, סינון-AND דרך URL) ו-`archive.html` (ארכיון כרונולוגי) זמינים בכל שפה. `about.html` + מסמך-רקע עצמאי (`scripts/build_background_doc.py`) - תוכן משותף דרך `src/common/about_content.py`, **נכתב מחדש במלואו** ב-2026-09-10 (גרסה מעודכנת, לא תוספת - כולל פרק חדש "מגבלות ידועות").

**דוחות יומיים** כוללים כעת (בנוסף להשוואה הדו-לשונית הבסיסית): קישור-הורדת-PDF, תיבת-ציטוט-מדויקת לכל section (עיתון+תאריך+עמוד), כפתור-שיתוף-קישור, ותפריט-קטגוריות-דביק עם accordion (ברירת-מחדל פתוח) - **נושאים ממוינים לפי רוחב-כיסוי אמיתי** (`section_coverage()`: מספר-מקורות ומספר-שפות, לא סדר-קיבוץ-שרירותי) - ראו CLAUDE.md לפרטים המלאים.

## מה הושלם (יכולות בשלות, לא יומן-שינויים)

- **`src/common/db.py`** - מקור-אמת יחיד לסכמה (`TABLE_COLUMNS`/`TABLE_CONSTRAINTS`, מיגרציה אוטומטית).
- **`src/ingestion/fetch.py`** - `guess_newspaper()` (10 מקורות, NYT מושעה, Economist רק Web-Edition, SZ עם 4 וריאנטי-כתיב כולל ללא-ניקוד), `compute_scan_limit()`, heartbeat+flood-wait logging לכל הורדה.
- **`src/extraction/extract.py`**, **`src/analysis/screen.py`+`analyze.py`** (שני-שלבים: Haiku רשת-רחבה → Sonnet ניתוח-מעמיק+תיוג-גיאוגרפי), **`geo_tag_backfill.py`** (כלי-עזר קבוע ל-backfill).
- **`src/common/geo_taxonomy.py`** - 94 מדינות/8 אזורים/3 קונפליקטים, רשימה סגורה.
- **`src/reporting/synthesize.py`** - ארכיטקטורת שני-שלבים (קיבוץ-לנושאים → השוואה-מקבילית עם Prompt Caching), רשת-ביטחון כפולה (fallback+retry).
- **`src/reporting/render.py`** - HTML+PDF דו-לשוני, citations, accordion, sticky-nav, מיון-לפי-כיסוי, הדגשה-ויזואלית לנושאים-רחבים.
- **`src/publishing/publish.py`** - `docs/` שלם: דוחות, `archive.html`, `about.html`, `topic.html`, דף-בית, `manifest.json` (כולל תיוג-נגישות-מפה).
- **`src/common/about_content.py`** - נכתב מחדש במלואו (2026-09-10).
- **מדידת-עלות רטרואקטיבית מדויקת** - שחזור `count_tokens` לשלבים שלא לכדו `response.usage` (Screening/Synthesize), או wrapper-ייעודי שכן לוכד usage אמיתי (Analysis, כשנדרשת דיוק מלא כולל thinking tokens).

## קבצים מרכזיים (תפקיד נוכחי)

ראו "מבנה הקבצים המרכזיים" ב-CLAUDE.md לפירוט מלא ומעודכן - לא משוכפל כאן. תוספות טכניות מהסשן האחרון: `section_coverage()`/`NEWSPAPER_LANGUAGES` ב-`render.py` (מיון+badge); `.section-header`/`.section-body`/`.category-nav`/`.share-link-btn` (accordion+sticky-nav+שיתוף) ב-`render.py`; `_load_map_svg_inline(lang, countries)` ב-`publish.py` (תיוג-נגישות); `about_content.py` (תוכן מלא חדש).

## החלטות שהתקבלו

ראו "החלטות ארכיטקטוניות קבועות" + "כללי עבודה קבועים" ב-CLAUDE.md לרשימה המלאה והמעודכנת (כולל תוספות הסשן האחרון: מיון-נושאים-לפי-כיסוי, חריגת-נגישות-המפה, עיקרון-סריקה-רטרואקטיבית-לפני-תיקון-קוד, הבחנת תיקון-בזמן-אמת-מול-פתיחה-מחדש-של-ארכיון). לא משוכפל כאן.

**החלטות ספציפיות-לסשן שלא שייכות ל-CLAUDE.md** (זמניות/הקשריות):
- `about_content.py`: `doc_meta` עודכן ל"ספטמבר 2026" למרות שלא היה חלק מהטקסט המאושר (המשתמש אישר מפורשות).
- שם עיתונים ב-`about_content.py` החדש כתובים בלי "The" (למשל "Guardian מול Telegraph") - שונה מ-`NEWSPAPER_DISPLAY_NAMES` הפורמלי; זה טקסט-פרוזה מאושר-כפי-שהוא, לא סטייה שדורשת תיקון.
- 2026-08-31–2026-09-02 (3 ימים) **לא ייסגרו כרגע** - המשתמש בחר במפורש לא לנסות, יוחלט בעתיד.

## עבודה שטרם הושלמה

1. **פער 31.8-2.9 (3 ימים)** - לא מזוהה אוטומטית ע"י `compute_scan_limit()` (ראו PROJECT_LOG action item 35). לא הוחלט אם שווה סריקה-ממוקדת-ידנית.
2. **Guardian/Daily Telegraph/Die Welt** טרם פורסמו בערוץ ל-9.9/10.9 (אומת ישירות, לא בעיית-זיהוי) - יתפסו בהרצה עתידית כשיפורסמו.
3. **`analyze.py`'s own `NEWSPAPER_LANGUAGES`** (לא זה שב-`render.py`!) מכסה רק 4/10 מקורות - 861 מאמרים קיימים עם `articles.language=''`. **אינרטי לגמרי כרגע** (שום קוד לא קורא את העמודה) - לא תוקן, action item בלבד.
4. `fetch.py` לא רושם שמות-קבצים שנדחו כ-`skipped_not_mvp` (רק ספירה).
5. פתרון-כללי לגזירת תאריך-שער לשבועונים/מהדורות-סופ"ש מתוך שם-הקובץ (במקום `published_at`) - נצפה 5 פעמים ב-3 מקורות שונים, טרם ממומש.
6. allowlist-לוגים-ידועים + timeout-10-דקות-לאימות-פריסה (שני המרכיבים-החסרים לפני סוכן-מתוזמן-אמיתי-בלי-פיקוח - ראו PROJECT_LOG action item 33).
7. Prompt Caching ברמת-ארגון; GitHub Actions לתזמון יומי; טסטים (`tests/` ריק).
8. מפה נגישה-לקורא-מסך אך עדיין **לא ניתנת-למיקוד-מקלדת** (`role="img"`, לא `role="button"`+`tabindex`) - הוחלט במפורש שזו הרחבה נפרדת וגדולה יותר, לא כלולה בתיקון-הנגישות הנוכחי.
9. סינון-AND-משולב אינטראקטיבי בדף הבית עצמו (קיים דרך `topic.html`, לא כ-UI על העמוד).

## בעיות ידועות (עדיין רלוונטיות בלבד - ראו PROJECT_LOG להיסטוריה המלאה)

- **"0 שורות" דו-משמעי** ב-`article_countries`/`article_conflict_zones` - לא מבחין בין "טרם תויג" ל"תויג, באמת ריק". מגבלה מודעת, לא באג.
- **USA Today, 2026-09-10**: 7 עמודים סומנו רלוונטיים ב-Screening, 0 מאמרים הופקו ב-Analysis. אובחן כשיפוט-מודל-סביר (רוב התוכן ביתי/גבולי), לא כשל - עמוד אחד (9/11 והשפעתו על מוסלמים-אמריקאים) הוא המקרה הכי-גבולי, לא נבדק שוב.
- **`published_at` לשבועונים/מהדורות-סופ"ש** לא תמיד תואם את יום-הכיסוי הנומינלי (נצפה 5 פעמים, 3 מקורות) - ראו "עבודה שטרם הושלמה" #5.
- **מספור-עמודים הפוך ב-PDF העברי** ("14 / 1") - bidi על ה-footer counter, לא תוקן בכוונה.
- **סקשן-רפאים ריק אפשרי ב-Synthesis** (נצפה פעם אחת, 25.8) - לא נבנה מנגנון-מניעה, ממתין להצטברות-מקרים.
- **article יחיד חשוד כ-false-positive** ב-Analysis (`article_id=254`, "Prince Harry") - לא תוקן, רעש-ידוע-בודד.

## הצעד הבא המומלץ

**הפייפליין יציב לתפעול-שוטף רגיל** - אין חסימה טכנית. סדר-עדיפויות:

1. להמשיך תפעול-שוטף (הרצה יומית ~לא-אוטונומית, כפי שהתבצע בסשן האחרון) עד שהאמון בפעולה-בלי-פיקוח יבשיל, ואז לגשת ל-#6 למעלה (allowlist+timeout) כתנאי-סף להרצה-מתוזמנת-אמיתית.
2. פתרון-כללי לתאריך-שער-משם-קובץ ב-`fetch.py` (5 מקרים כבר, ראו #5 למעלה) - מספיק חוזר כדי להצדיק קוד כללי במקום עוד תיקון-DB נקודתי.
3. להחליט אם/מתי לסגור את פער 31.8-2.9 (#1 למעלה) - לא דחוף, אך פתוח.
4. Prompt Caching ברמת-ארגון + GitHub Actions - כשההרצה היומית הופכת קבועה-דיה שכדאי להשקיע בתשתית.

**תזכורת קבועה מ-CLAUDE.md**: כל שינוי עתידי במבנה/נתיבי-פלט של `render.py`/`publish.py` מחייב regeneration מלא של הארכיון + `git status` מפורש לפני commit.

## סשן אחרון

**תאריך:** 2026-09-10

**תיאור:** סשן ארוך ורב-חלקים, המשך ישיר מסשן קודם (שהסתיים באמצע עדכון-עלות לאחר Screening/Analysis על מחזור-נתונים בן 6 ימים). כלל:

1. **השלמת מחזור 6-הימים** (30.8+3.9-8.9): Analysis (0 כשלים, $15.51), Synthesize (0 כשלים/retry, ~$3.47), Render+Publish, אומת ויזואלית, `commit` (`83ae40e`).
2. **מיון-נושאים-לפי-כיסוי** (`section_coverage()`) - תוכנן, יושם, נבדק (כולל תיקון תקלה אמיתית: תוכן-דלוף-מתגית-XML בשתי sections, לא קשור למיון עצמו - נסרק רטרואקטיבית ואומת כחד-פעמי), regeneration מלא ל-17 תאריכים, `commit` (`227feb6`).
3. **שיפורי UI לדוח** (שיתוף-קישור, אפורדנס-ציטוטים, ניווט-דביק+accordion, היררכיה-ויזואלית) - תוכנן, יושם, **נבדק בפועל עם לחיצות אמיתיות** (Playwright: clipboard, toggle, keyboard, expand-all, nav-jump) - תפס ותיקן באג-סקרול-מכולה בניווט-הדביק. `commit` (`f879b83`).
4. **נגישות-מפת-דף-הבית** - חשף שני ממצאים אמפיריים לפני-כתיבת-קוד (מבנה-SVG כפול `<g>`/`<path>`, ו-`aria-label` דורש `role` תקין - נתפס ע"י axe-core). הושלם `role="img"` (לא `"button"` - נמנע מטענת-נגישות-שקרית). `commit` (`9ef8c63`).
5. **הרצת-שגרה על 9.9+10.9** (לא-אוטונומית, עצירת-אישור לפני commit כמתוכנן) - כולל בירור-אבחון של USA Today (0 מאמרים, שיפוט-מודל סביר) ו-Guardian/Telegraph/SZ/Welt (נעדרים מהערוץ). `commit` (`c1b2cfd`).
6. **כתיבה-מחדש מלאה של `about_content.py`** - תוכנן (מיפוי-פרקים מלא לפני קוד), יושם, נבנה-מחדש (מסמך עצמאי+`about.html`), **צילומי-מסך נשלחו ישירות למשתמש** (`SendUserFile`, לא רק תיאור) לפי בקשה מפורשת. `commit` (`d77eb80`).
7. **בירור המשך על USA Today/Guardian** הוביל לגילוי-אמיתי: SZ **כן** היה בערוץ ל-10.9, אך `guess_newspaper()` דחה וריאנט-כתיב-ללא-ניקוד ("Suddeutsche") - אומת ריצה-חוזרת + סריקה-רטרואקטיבית-מלאה (868 הודעות, מ-17.8) שאישרה חד-פעמיות (17/18 עברו נכון היסטורית). תוקן, דוח 10.9 נבנה-מחדש (2→3 מקורות, 30→48 מאמרים). `commit` (`b88cb35`).

**דפוס חוזר בסשן**: כמה פעמים המשתמש ביקש "תבדוק שוב"/"תסביר בדיוק" לפני שאישר commit - בכל מקרה הבדיקה החוזרת אכן מצאה משהו (בין אם אישוש-ההנחה הקיימת, ובין אם תיקון-מדידה עצמי כמו טעות-scope בספירת article_ids-מרובי-section). זה תואם את העיקרון הכללי של הפרויקט: מדידה, לא הנחה - גם (ובמיוחד) כשההנחה נשמעת סבירה.
