# Project Handoff

## מצב הפרויקט הנוכחי

**חמשת שלבי ה-Pipeline (Ingestion→Extraction→Analysis→Reporting→Publishing) יציבים ומוכחים על עשרות הרצות אמיתיות**, כולל **9 הרצות אוטונומיות-לגמרי ברצף (11.9 עד 19.9, כולל commit+push ואימות-חי)** - ראו "סשן אחרון". הארכיון כולל **28 תאריכים** (2026-08-20 עד 2026-09-19, עם פער-לא-סגור מודע ב-31.8-2.9 - ראו "בעיות ידועות") בשתי שפות מלאות, שני פורמטים (HTML+PDF), תחת `reports/{he,en}/` ו-`docs/{he,en}/`. **האתר חי**: https://geopolitics.meirshemesh.com (פריסה אחרי push נמדדה ב-19.9: 45 שניות).

**מקורות MVP פעילים בפועל: 10** (הרשימה המקורית של 10, בניכוי NYT המושעה-לצמיתות-בכל-מהדורה, בתוספת Washington Post). **תיקון (19.9)**: התיעוד אמר "9" מאז הוספת WaPo - טעות-ספירה, אומתה מול 10 הערכים שמחזיר `guess_newspaper()`. בהתאם, "יום מלא 9/9" שדווח ב-18.9 היה בפועל 9/10: ה-Economist (שבועון, נוכח רק בימי-הגעת-הגיליון) הגיע באותו יום ונעדר מהדוח בגלל שיוך-תאריך שגוי (ראו "בעיות ידועות"). `compute_scan_limit()` מחשב דינמית את חלון-הסריקה וחוזר לגודל-בסיס (200) בהרצות שוטפות.

**האתר עומד בחבילת-ציות רגולטורי בסיסית** (נגישות/גילוי-נאות-AI/תנאי-שימוש, PROJECT_LOG 4.38) ובעל פאביקון בכל עמוד. **דף הבית (v2)**: מפה אינטראקטיבית, ציר-זמן, כרטיס-דוח-אחרון, צ'יפי-אזור+קונפליקט - הכל קורא מ-`manifest.json` בזמן-ריצה. `topic.html`/`archive.html`/`about.html`/`accessibility.html`/`terms.html` זמינים בכל שפה. פרטי הדוחות היומיים והאתר - ראו CLAUDE.md, לא משוכפל כאן.

## מה הושלם (יכולות בשלות, לא יומן-שינויים)

- **`src/common/db.py`** - מקור-אמת יחיד לסכמה, מיגרציה אוטומטית.
- **`src/ingestion/fetch.py`** - `guess_newspaper()` (10 מקורות פעילים, NYT מושעה-לצמיתות-כל-מהדורה, Economist רק Web-Edition, SZ עם וריאנט-כתיב ללא-ניקוד), `compute_scan_limit()`, heartbeat+flood-wait logging.
- **`src/extraction/extract.py`**, **`src/analysis/screen.py`+`analyze.py`** (Haiku רשת-רחבה → Sonnet ניתוח-מעמיק+תיוג-גיאוגרפי; `screen.py --file` / `analyze.py --file-id` מאפשרים הרצה לקובץ בודד), **`geo_tag_backfill.py`**.
- **`src/common/geo_taxonomy.py`** - 94 מדינות/8 אזורים/3 קונפליקטים.
- **`src/reporting/synthesize.py`** - ארכיטקטורת שני-שלבים, רשת-ביטחון כפולה. **`render.py`** - HTML+PDF דו-לשוני, citations, accordion, sticky-nav, מיון-לפי-כיסוי, פוטר-משותף, favicon. **`src/publishing/publish.py`** - `docs/` שלם: דוחות, archive/about/topic/accessibility/terms, דף-בית, `manifest.json`.
- **`src/common/about_content.py`** - תוכן About משותף, כולל סעיף גילוי-נאות-AI.
- **מדידת-עלות** - שחזור `count_tokens` ל-Screening/Analysis (Analysis כ"גבול-תחתון", בלי thinking), Synthesize מ-usage אמיתי. תמחור מאומת: Sonnet 5 = $2/$10 למיליון טוקן, Haiku 4.5 = $1/$5. **עלויות יומיות מדודות**: 17.9 - $4.47; 18.9 - $9.43; 19.9 - $5.01 (תלוי בעיקר בהיקף Screening; ה-Economist לבדו ~40% מהעמודים).
- **הרצה אוטונומית מלאה** מוכחת 9 פעמים ברצף, עם guardrail-בטיחות מכני לפני push (`git status`/שמות-חשודים/מספר-קבצים) ובדיקת-בריאות יזומה לפני Screening (סקריפטים אד-הוק בסקראצ'פד של הסשן - לא שמורים בריפו, נכתבים מחדש בכל הרצה).

## קבצים שנוצרו או שונו בסשן האחרון (19.9)

- **דוח 19.9**: `reports/{he,en}/report_2026-09-19_*.{html,pdf}` + עותקים ב-`docs/{he,en}/` (8 קבצים חדשים), `archive.html` ×5 ו-`manifest.json` ×2 עודכנו - commit `004f028`.
- **`PROJECT_LOG.md`** - סעיף 4.43 חדש, שורת 19.9 בטבלת-המעקב, תיקון-מפורש לטענה שגויה ב-4.42 (Economist), עדכון action items 24/29/38, פריטים חדשים 39/40 - commit `64ad447`.
- **`CLAUDE.md`** - תיקון "9→10 מקורות" (4 מקומות) ושני כללי-עבודה קבועים חדשים (hash מול DB + בדיקת שיוך-תאריך אחרי Analysis) - נסגר בסשן הסגירה.
- **`HANDOFF.md`** - עודכן במלואו (היה מיושן: סשן אחרון 17.9, "25 תאריכים", "9 מקורות").

## החלטות שהתקבלו

ראו "החלטות ארכיטקטוניות קבועות" + "כללי עבודה קבועים" ב-CLAUDE.md לרשימה המלאה. לא משוכפל כאן.

**החלטות ספציפיות-לסשן שלא שייכות ל-CLAUDE.md** (זמניות/הקשריות):
- Washington Post נבחר כתחליף ל-NYT בעמוד About **באותו מיקום פוליטי בערך** - החלפה-ישירה בלי שינוי-ניסוח (אישור מפורש).
- כתובת-יצירת-קשר בהצהרת-הנגישות (`meir@meirshemesh.com`) ניתנה במפורש ע"י בעל-האתר - לא הומצאה.
- 2026-08-31–2026-09-02 (3 ימים) **לא ייסגרו כרגע** - עדיין החלטה פתוחה, לא השתנתה.
- **19.9 - עצירה-ודיווח במקום תיקון-והמשך** (לפי הגדרת ההרצה האוטונומית): (א) ה-classifier של Claude Code חסם מחיקת קובץ-כפול (Economist id=200) - **לא עקפתי** בכלי אחר; במקום זאת Screening הורץ בנפרד לכל קובץ לגיטימי (`--file`). (ב) `published_at` של ה-Economist (id=191) **לא שונה** ו-`synthesize --force` לא הורץ על 17.9/18.9/19.9. (ג) איות שם-הסרט "NAZA" בדוח העברי **לא תוקן** (בעל-האתר קבע נז"א ב-`4d2bb8f`). שלושתן ממתינות להחלטת בעל-האתר.

## עבודה שטרם הושלמה

1. **החלטה על 163 מאמרי Economist ("TE-2026-09-19", file 191)** - נותחו ב-18.9 (עלות שולמה), שויכו ל-17.9 לפי `published_at` ולא נכללו באף דוח. אפשרות: לשנות `published_at` ל-19.9 + `synthesize --force` על 19.9 (משנה את הדוח שפורסם: 5→6 מקורות, ~74→~237 מאמרים). PROJECT_LOG action item 29.
2. **שורת id=200 (Economist כפול, זהה-בייטים ל-191) עדיין ב-DB ועל הדיסק** (`downloaded_files` + 356 `extracted_pages` + PDF + עותק-טקסט) כי מחיקתה נחסמה. **עד שתטופל - כל הרצה חייבת `screen.py --file <תת-מחרוזת>` לכל קובץ בנפרד, או מחיקה קודם** (אחרת ~$1.7 מבוזבז על 356 עמודים כפולים). PROJECT_LOG action item 39.
3. **מנגנון-דילוג ב-`fetch_channel()`** (message_id/hash ידועים-כפולים-או-פגומים) - **פותר יחד** את מחיקת-DB-שלא-מונעת-הורדה-חוזרת (action item 38, 5 הופעות של message_id 29577) ואת כפילות-Economist (action item 24, 3 מקרים - הסף שנקבע נחצה) ומבטל את הצורך במחיקה ידנית.
4. **פתרון-כללי לגזירת תאריך-שער משם-קובץ** (במקום `published_at`) - **8 מקרים, 4 מקורות** (action item 29), עם מימד חדש שעולה כסף: תוכן שנותח ולא נכלל באף דוח. הצעה נלווית: אזהרה קבועה בסוף כל הרצה על מאמרים חדשים שלא משויכים ליום-הדוח (כעת רק כלל-עבודה ב-CLAUDE.md, לא קוד).
5. **החלטה על שם-הסרט התיעודי** (action item 40) - "NAZA" בלטינית בדוח 19.9 העברי, פעם שלישית ובאיות שונה בכל פעם; אם מתקנים - שיטת `4d2bb8f`; אם ממשיך - מיפוי-שמות-קבוע בפרומפט של `synthesize.py`.
6. **2 עמודי WSJ Magazine (file 199, עמודים 39/63) נשארו `pending` ב-Analysis** (`KeyError 'articles'`) - יטופלו אוטומטית בהרצת Analysis הבאה, עלות זניחה.
7. **פער 31.8-2.9 (3 ימים)** - לא מזוהה אוטומטית ע"י `compute_scan_limit()` (action item 35).
8. **`analyze.py`'s own `NEWSPAPER_LANGUAGES`** מכסה רק 4/10 מהמקורות - אינרטי-מאומת (action item 36).
9. **טביעת-אצבע-שחיתות שנייה, בלי tradingref.com** (0 תווים+תמונה) - action item 37, עדיין לא-הוכרע.
10. allowlist-לוגים-ידועים + timeout-10-דקות-לאימות-פריסה + בדיקת-זמינות-GTK3/WeasyPrint לפני הרצה (action item 33) - בפועל כל שלושה בוצעו ידנית ב-19.9 (`import weasyprint` הצליח; אימות-חי עם תקרת-10-דקות; הכל מחוץ לקוד).
11. Prompt Caching ברמת-ארגון; GitHub Actions לתזמון יומי; טסטים (`tests/` ריק).
12. מפה נגישה-לקורא-מסך אך עדיין לא ניתנת-למיקוד-מקלדת - הוצהר בכנות בהצהרת-הנגישות. סינון-AND-משולב אינטראקטיבי בדף הבית עצמו (קיים דרך `topic.html`).

## בעיות ידועות (עדיין רלוונטיות בלבד - ראו PROJECT_LOG להיסטוריה המלאה)

- **"0 שורות" דו-משמעי** ב-`article_countries`/`article_conflict_zones` - מגבלה מודעת, לא באג.
- **`published_at` לשבועונים/מהדורות-סופ"ש/מוספים לא תמיד תואם את יום-הכיסוי הנומינלי** - 8 מקרים, 4 מקורות. השלכה מדודה: 163 מאמרי Economist + 3 מאמרים נוספים (WSJ Magazine, Guardian) נותחו ב-18.9-19.9 ולא נכללו באף דוח. בשאילתת-בדיקה נראו גם שאריות-ישנות של מאמרים-ללא-דוח מ-24-27.8 ו-6-11.9 (Washington Post catch-up) - **לא נחקרו**.
- **דוח 18.9 דווח כ"יום מלא 9/9" - למעשה 9/10**: ה-Economist (גיליון 19.9 שהגיע ב-18.9) נעדר ממנו (ראו למעלה); הטענה ב-PROJECT_LOG 4.42 שה-Economist תרם לדוח 18.9 תוקנה שם. (בדוח 17.9 היעדרו טבעי - הגיליון טרם הגיע.)
- **WSJ Magazine (מוסף-רבעוני-לייפסטייל) מזוהה כ-Wall Street Journal** (5.9 ו-19.9) ועובר Screening (116 עמ', 5% רלוונטי) - אותה שאלת-מוסף כמו Guardian Weekly/USA Today Sports Weekly (PROJECT_LOG פריט 8).
- **מספור-עמודים הפוך ב-PDF העברי** - bidi, לא תוקן בכוונה.
- **סקשן-רפאים ריק אפשרי ב-Synthesis** (נצפה פעם אחת) - ממתין להצטברות-מקרים.
- **article יחיד חשוד כ-false-positive** (`article_id=254`, "Prince Harry") - רעש-ידוע-בודד.
- **קובץ-WSJ פגום (message_id 29577) - נפתר מעצמו**: יצא מחלון-הסריקה ב-19.9 אחרי 5 הופעות (14.9-18.9); הוסר מרשימה זו.

## הצעד הבא המומלץ

**הפייפליין יציב לתפעול-שוטף ואין חסימה טכנית**, אבל 19.9 חשף בעיה שוטפת שעולה כסף. סדר-עדיפויות:

1. **להחליט על 1-2 למעלה** (Economist 163 + שורת id=200) - החלטת-נתונים של בעל-האתר, לפני ההרצה הבאה (אחרת ההרצה הבאה חייבת `--file`).
2. **לממש #3 ו-#4 יחד ב-`fetch.py`** (דילוג ל-message_id/hash ידוע + תאריך-שער משם-הקובץ) - שניהם חוזרים על עצמם מספיק פעמים (3 ו-8 מקרים) כדי להצדיק קוד קבוע, ומבטלים את הצורך במחיקות-ידניות ובהסקה-בזמן-אמת.
3. להחליט על #5 (שם הסרט) - תיקון-חד-פעמי או מיפוי-שמות בפרומפט.
4. להמשיך תפעול-שוטף; לשמור את סקריפטי בדיקת-הבריאות ושחזור-העלות בריפו (`scripts/`) במקום לכתוב אותם מחדש בכל הרצה, ואז לגשת ל-#10 כתנאי-סף להרצה-מתוזמנת-אמיתית. אחר-כך Prompt Caching ברמת-ארגון + GitHub Actions.

**תזכורת קבועה מ-CLAUDE.md**: כל שינוי עתידי במבנה/נתיבי-פלט של `render.py`/`publish.py` מחייב regeneration מלא של הארכיון + `git status` מפורש לפני commit.

## סשן אחרון

**תאריך:** 2026-09-19 (הסגירה בוצעה אחרי חצות, 20.9)

**תיאור:** הרצה אוטונומית מלאה תשיעית ברצף על דוח 19.9 - Ingestion (8 קבצים) → Extraction → בדיקת-בריאות → Screening (527 עמ', 130 רלוונטיים) → Analysis (129 עמ', 77 מאמרים) → Synthesize (74 מאמרים, 5 מקורות, 39 נושאים, יחס 1.90) → Render → Publish → commit+push (`004f028`) → אימות-חי (45 שניות) וצילומי-מסך של שתי השפות מהאתר החי. 0 כשלי-שלב; עלות $5.01. חלק שני: עדכון PROJECT_LOG (`64ad447`).

**מה היה חריג**: ה-classifier חסם מחיקת-כפול (Economist) והריצה לא עקפה; התגלה ש-163 מאמרי Economist מ-18.9 לא נכללו באף דוח (`published_at`), ובעקבות זה גם שמספר-המקורות הפעילים בתיעוד היה שגוי (9 במקום 10). ה-message_id 29577 לא חזר (יצא מהחלון). כל אלה מתועדים ב-PROJECT_LOG 4.43 ובפריטים 24/29/38/39/40 - **שום נתון שפורסם לא שונה על דעת עצמי**.

**דפוס חוזר**: "בדוק בפועל, אל תניח" - הפעם, ההנחה שה-Economist תרם לדוח 18.9 (נכתבה ב-4.42 בלי בדיקה מול `report_section_articles`) התבררה כשגויה רק כשנבדקה מפורשות; וספירת-המקורות "9" שרדה שבוע אחרי שהפכה לשגויה (מאז הוספת WaPo ב-12.9), כי אף אחד לא השווה אותה לקוד.
